{
  "body": {
    "raw": "# Lesson 20.9 — Caching, Object Storage, CDN & API Gateway\r\n\r\n> Part 20 · Enterprise Capstone · Difficulty: ⚫ · *Capstone*\r\n>\r\n> **Prerequisites:** [Part 6 Caching], [4.3.2 Object/Blob Storage], [18.4 CDN & Edge], [12.6 API Gateway/BFF], [3.3.1 Load Balancing], [20.4 Portfolio/Ledger].\r\n> **Unlocks:** [20.10 Reliability], [20.11 Security].\r\n\r\n---\r\n\r\n## 1. Learning Objectives\r\n\r\nAfter this lesson you will be able to:\r\n\r\n- Design the **edge + delivery + caching + object-storage** layers of the platform (the read-performance + static-asset infrastructure).\r\n- Place **caching** (Part 6) correctly: cache **derived read models** (portfolio views, market prices), **never** authorize money off a cache (20.4).\r\n- Use **object storage** (4.3.2) for documents/statements/reports and a **CDN** (18.4) for static + cacheable content.\r\n- Design the **API gateway + BFF** (12.6) as the single edge: routing, authN, rate limiting, aggregation.\r\n- Handle deep dives: **cache invalidation** for financial data, **what's cacheable vs not**, and **secure document delivery**.\r\n\r\n---\r\n\r\n## 2. Motivation\r\n\r\nThe platform is **read-heavy** (portfolio dashboards — 20.2), serves **large static/document content** (statements, tax docs, reports), and needs a **clean, secure edge** for many client types (web/mobile/advisor/API). This lesson assembles the **caching (Part 6), object storage (4.3.2), CDN (18.4), and API gateway (12.6)** layers — the infrastructure that makes reads fast and delivery efficient **without compromising the correctness of money** (the golden rule: cache derived views, never the authoritative ledger for authorization).\r\n\r\n---\r\n\r\n## 3. The design (framework — 1.3.1)\r\n\r\n### 3.1 Caching (Part 6) — what and where\r\n\r\n`[CS]` Cache **derived, read-mostly** data; be careful with money `[BP]`:\r\n- **Cache these:** portfolio **read-model** views (positions/balances/valuations — 20.4/20.7), **latest market prices** (20.5), search results (20.8), reference data (instrument metadata), session/authz decisions (carefully — 20.3).\r\n- **Topologies (6.2):** CDN/edge (static) → API/gateway cache → **distributed cache (Redis — 6.6)** for shared read models → local caches. Cache-aside (6.3) for most read models.\r\n- **The golden rule** (20.4): **never authorize a money operation off a cached balance** — reads for **display** can be cached (eventually consistent — 10.5), but **authorization/execution checks the authoritative ledger**. Confusing display-cache with truth is a correctness bug.\r\n- **Invalidation** (6.5 — one of the hard things): financial read models update from **events** (CDC — 9.8) → **event-driven invalidation/refresh** of caches (not just TTL) so a trade reflects quickly; use short TTLs + versioned keys for prices. Balance freshness (users expect near-real-time portfolios) against load.\r\n- `[BP]` **Cache derived views aggressively (Part 6), invalidate via events (9.8), never cache the authorization truth.**\r\n\r\n### 3.2 Object storage (4.3.2)\r\n\r\n`[BP]`\r\n- **Documents/statements/tax reports/uploads/KYC docs** → **object/blob storage** (4.3.2 — S3-style): cheap, durable, scalable, lifecycle-managed (tiering hot→cold→archive — 4.3.2).\r\n- **Immutable + versioned** where needed (statements are records — audit); **encrypted at rest** (15.4); **access-controlled** (a client's docs are private — 20.3).\r\n- **Secure delivery:** serve via **signed, expiring URLs** (not public buckets) so only authorized users fetch a document — a common, important pattern.\r\n- `[BP]` **Object storage for documents/reports** — durable, tiered, encrypted, access-controlled, delivered via signed URLs.\r\n\r\n### 3.3 CDN & edge (18.4)\r\n\r\n`[BP]`\r\n- **CDN** (18.4/3.3.3) for **static assets** (app bundles, images) and **cacheable content** (public reference data, and cached documents via signed URLs at the edge).\r\n- **Edge benefits:** low global latency, origin offload, **DDoS absorption + WAF** (18.4/15.5) — the security first tier (20.11).\r\n- **Careful:** **personalized/sensitive financial data is NOT edge-cached** publicly — only static/public or per-user-authorized (signed) content. Never cache a portfolio at a shared edge.\r\n- `[BP]` **CDN for static + public + authorized-document delivery + DDoS/WAF edge** — not for private financial views.\r\n\r\n### 3.4 API gateway + BFF (12.6)\r\n\r\n`[BP]`\r\n- **API gateway** (12.6): the **single edge** for all client traffic — **routing** to services, **authN/token validation** (20.3), **rate limiting** (15.7/20.11), request/response shaping, and observability (20.12). North-south entry.\r\n- **BFF (Backend-for-Frontend)** (12.6): per-client-type gateways (web/mobile/advisor/public-API) that **aggregate** downstream calls into client-shaped responses (e.g., a dashboard = portfolio + prices + recs in one call), reducing round-trips (17.4) + tailoring payloads.\r\n- **Load balancing** (3.3.1) in front; TLS termination at the edge (15.4).\r\n- `[BP]` **Gateway = single secure edge (routing/authN/rate-limiting); BFF = client-tailored aggregation.**\r\n\r\n### 3.5 Deep dives + bottlenecks\r\n\r\n`[BP]`\r\n- **Cache correctness** (§3.1): display-cache vs authorization-truth — the golden rule (20.4); event-driven invalidation (9.8) for freshness.\r\n- **Read scaling** (7.5): cache + read replicas dissolve the read-heavy load (20.2); the gateway/BFF must scale statelessly (7.2).\r\n- **Secure delivery:** signed expiring URLs for documents; no public buckets; no edge-caching private data.\r\n- **Gateway as a critical path:** it's on every request → must be **HA + fast + not a bottleneck** (scale horizontally, avoid heavy logic); rate limiting protects backends (20.11).\r\n- **Bottleneck:** without caching, the ledger/DB would be hammered by reads → **cache-first + replicas** (Part 6/7.5) is the dominant relief (20.2); object storage + CDN offload large content.\r\n- `[BP]` **The lesson:** the read/delivery infrastructure = **cache derived views (Part 6, event-invalidated — never the auth truth) + object storage for documents (4.3.2, encrypted, signed-URL delivery) + CDN for static/public/DDoS-WAF edge (18.4) + API gateway/BFF as the single secure, aggregating edge (12.6)** — making reads fast and delivery efficient while keeping money correct.\r\n\r\n---\r\n\r\n## 4. Visual Intuition\r\n\r\n```mermaid\r\nflowchart TB\r\n    C[\"Clients (web/mobile/advisor/API)\"] --> CDN[\"CDN/edge (18.4): static + DDoS/WAF (20.11)\"]\r\n    CDN --> LB[\"Load balancer (3.3.1) + TLS termination\"]\r\n    LB --> GW[\"API Gateway + BFF (12.6): route + authN (20.3) + rate limit (20.11) + aggregate\"]\r\n    GW --> SVC[\"Services\"]\r\n    SVC --> CACHE[(\"Distributed cache (Redis — 6.6): derived read models + latest prices\")]\r\n    CACHE -. event-driven invalidation (CDC — 9.8) .- LEDGER[(\"Ledger events (20.4/20.7)\")]\r\n    SVC --> OBJ[(\"Object storage (4.3.2): documents/statements — encrypted, signed URLs\")]\r\n    note[\"Cache derived views (never the auth truth — 20.4). Object storage for docs (signed URLs). CDN for static/public + WAF. Gateway = single secure edge.\"]\r\n```\r\n\r\n---\r\n\r\n## 5. Real-World Analogy\r\n\r\nThink of a **bank's branch front, mailroom, and information boards**.\r\n\r\n- **Caching = the info boards showing recent balances and prices:** they refresh **moments after** something changes, so customers get fast answers without a clerk digging in the vault each time. But — the golden rule — **the teller never pays out based on the board**; to actually move money, they **check the official ledger**. The board is for looking; the vault is for doing.\r\n- **Object storage = the document archive/mailroom:** statements, tax forms, and uploaded IDs are filed in a **big, cheap, durable archive**, with old ones moved to deep storage. You don't get them by walking into the archive — you're handed a **single-use claim ticket** (a signed, expiring link) that only works for **your** documents.\r\n- **CDN = neighborhood pickup lockers:** the bank's **public brochures and app** are stocked in lockers all over the city for instant pickup, and those lockers **absorb the crowd** (DDoS) at the edge. But your **private statement** is never left in a shared public locker — only static or personally-authorized items go to the edge.\r\n- **API gateway + BFF = the front desk that routes you and bundles your errands:** one **secure entrance** checks your ID and directs you, and a helpful concierge **bundles several requests** (\"here's your dashboard: balances, prices, and suggestions, all at once\") so you don't queue at five counters.\r\n\r\n---\r\n\r\n## 6. Industry Example\r\n\r\n- **Redis distributed cache for read models** `[CONV]`: caching derived views + latest prices, event-invalidated (§3.1, 6.6/9.8). *(Representative.)*\r\n- **S3-style object storage + signed URLs** `[CONV]`: durable encrypted document storage with secure delivery (§3.2, 4.3.2). *(Representative.)*\r\n- **CDN with WAF/DDoS at the edge** `[CONV]`: static/public delivery + security first tier (§3.3, 18.4/15.5). *(Representative.)*\r\n- **API gateway + BFF** `[CONV]`: single secure edge + client-tailored aggregation (§3.4, 12.6). *(Representative.)*\r\n\r\n---\r\n\r\n## 7. Implementation Details\r\n\r\n- **Caching:** distributed cache (Redis — 6.6) for derived read models + latest prices; cache-aside (6.3); **event-driven invalidation** (9.8); **never cache the authorization truth** (20.4) (§3.1).\r\n- **Object storage** (4.3.2): documents/statements — encrypted (15.4), lifecycle-tiered, access-controlled (20.3), **signed expiring URLs** (§3.2).\r\n- **CDN** (18.4): static + public + authorized-document edge delivery + WAF/DDoS (20.11); no private-data edge caching (§3.3).\r\n- **API gateway + BFF** (12.6): routing, authN (20.3), rate limiting (20.11), aggregation; LB (3.3.1) + TLS termination; stateless scale (7.2) (§3.4).\r\n\r\n---\r\n\r\n## 8–14. (Condensed)\r\n\r\n**Advantages:** fast read-heavy dashboards (cache + replicas); cheap durable document storage; low-latency static delivery + edge security; clean single secure edge + reduced round-trips (BFF).\r\n**Disadvantages/cautions:** cache invalidation is hard (event-driven + TTL); risk of caching stale/authoritative data (golden rule); gateway is a critical path (HA); signed-URL + access-control complexity for docs.\r\n**When NOT to:** don't cache/authorize money off a cache; don't edge-cache private financial data; don't put heavy logic in the gateway; don't serve documents from public buckets.\r\n**Common mistakes:** authorizing trades off cached balances (correctness bug); public buckets leaking documents; edge-caching personalized data; no event-driven invalidation (stale portfolios); gateway as an overloaded monolith.\r\n**Interview Qs:** 🟢 What do you cache and what do you never cache? 🟡 How do you invalidate financial read-model caches (events vs TTL)? 🔴 How do you deliver private documents securely (signed URLs, no public/edge)? Gateway vs BFF? ⚫ Design the full read/delivery infra: caching (with the golden rule), object storage, CDN, gateway/BFF — and defend the correctness boundaries.\r\n**Production pitfalls:** stale portfolio caches; authorization off stale cache; document leakage (misconfigured buckets/URLs); gateway saturation; edge caching sensitive data.\r\n**Optimizations:** event-driven cache invalidation (9.8); short-TTL + versioned keys for prices; read replicas (7.5); tiered object storage (4.3.2); BFF aggregation (17.4); edge WAF/DDoS (18.4).\r\n\r\n---\r\n\r\n## 15. Summary\r\n\r\nThe platform is **read-heavy** (portfolio dashboards — 20.2), serves **large document content** (statements, tax docs, KYC uploads), and needs a **clean secure edge** for many client types — so this lesson assembles the **caching (Part 6), object storage (4.3.2), CDN (18.4), and API gateway/BFF (12.6)** layers that make reads fast and delivery efficient **without compromising money correctness**. **Caching** targets **derived, read-mostly** data — portfolio **read-model** views (20.4/20.7), **latest market prices** (20.5), search results (20.8), reference data — across the standard topologies (CDN → gateway cache → distributed Redis — 6.6 → local), typically cache-aside (6.3); the **golden rule** (20.4) is **never authorize a money operation off a cached balance** — display reads can be cached (eventually consistent — 10.5) but **authorization/execution must check the authoritative ledger** (confusing display-cache with truth is a correctness bug) — and because financial read models update from **events**, use **event-driven invalidation/refresh** (CDC — 9.8), not just TTL, so trades reflect quickly. **Object storage** (4.3.2, S3-style) holds **documents/statements/tax reports/uploads** — cheap, durable, **lifecycle-tiered** (hot→cold→archive), **immutable/versioned** where records demand, **encrypted at rest** (15.4), **access-controlled** (20.3), and delivered via **signed, expiring URLs** (never public buckets). The **CDN** (18.4) serves **static assets + public/cacheable content + authorized documents (signed URLs)** at low global latency with **origin offload + DDoS absorption + WAF** (the security first tier — 20.11) — but **personalized/sensitive financial data is never edge-cached** publicly. The **API gateway** (12.6) is the **single secure edge** (routing, authN/token validation — 20.3, **rate limiting** — 15.7/20.11, observability — 20.12), fronted by a **load balancer + TLS termination** (3.3.1/15.4), and **BFFs** (per client type) **aggregate** downstream calls into client-shaped responses (a dashboard = portfolio + prices + recs in one call — reducing round-trips — 17.4). **Deep dives:** cache correctness (display vs auth-truth — the golden rule), read scaling (cache + replicas dissolve the read-heavy load — 7.5; gateway/BFF scale statelessly — 7.2), secure document delivery (signed URLs, no public/edge for private data), and the gateway as an **HA critical path**. The **bottleneck** — reads hammering the ledger/DB — is relieved by **cache-first + replicas** (the dominant lever — 20.2), with object storage + CDN offloading large content. In one line: **cache derived views (event-invalidated, never the auth truth) + object storage for documents (encrypted, signed-URL delivery) + CDN for static/public/DDoS-WAF edge + API gateway/BFF as the single secure aggregating edge**.\r\n\r\n---\r\n\r\n## 16. Revision Notes (flashcard-ready)\r\n\r\n- **Q:** What do you cache vs never cache? **A:** Cache derived read views (portfolio, prices, search); never authorize money off a cache — check the authoritative ledger (20.4).\r\n- **Q:** How invalidate financial caches? **A:** Event-driven (CDC — 9.8) refresh + short TTL/versioned keys — not TTL alone — so trades reflect quickly.\r\n- **Q:** Where do documents live + how delivered? **A:** Object storage (4.3.2) — encrypted, tiered, access-controlled; delivered via signed, expiring URLs (no public buckets).\r\n- **Q:** What goes on the CDN? **A:** Static assets + public/cacheable content + authorized docs (signed URLs) + WAF/DDoS; never private financial data at a shared edge.\r\n- **Q:** API gateway role? **A:** Single secure edge — routing, authN, rate limiting, observability; HA critical path, keep it thin.\r\n- **Q:** BFF? **A:** Per-client-type gateway that aggregates downstream calls into client-shaped responses (fewer round-trips — 17.4).\r\n- **Q:** Dominant read-scaling lever? **A:** Cache-first + read replicas (Part 6/7.5) — dissolves the read-heavy portfolio load.\r\n- **Q:** Golden rule? **A:** Cache for display, never for authorization; the ledger is the truth (20.4).\r\n\r\n---\r\n\r\n## 17. Further Reading + Knowledge-Graph Links\r\n\r\n**Foundations:** [Part 6 Caching] · [4.3.2 Object Storage] · [18.4 CDN & Edge] · [12.6 API Gateway/BFF] · [3.3.1 Load Balancing] · [9.8 CDC] · [20.4 Ledger].\r\n**External:** Redis; S3 + signed URLs; CDN/WAF; API-gateway/BFF patterns. *(Representative.)*\r\n\r\n> **Knowledge-graph:** `Part 6 cache` + `4.3.2 object storage` + `18.4 CDN` + `12.6 gateway` → **`20.9 read/delivery infra`** (cache derived views event-invalidated + object storage signed-URLs + CDN edge + gateway/BFF) → never cache the auth truth (20.4).\r\n",
    "html": "<h1>Lesson 20.9 — Caching, Object Storage, CDN &#x26; API Gateway</h1>\n<blockquote>\n<p>Part 20 · Enterprise Capstone · Difficulty: ⚫ · <em>Capstone</em></p>\n<p><strong>Prerequisites:</strong> [Part 6 Caching], [4.3.2 Object/Blob Storage], [18.4 CDN &#x26; Edge], [12.6 API Gateway/BFF], [3.3.1 Load Balancing], [20.4 Portfolio/Ledger].\r\n<strong>Unlocks:</strong> [20.10 Reliability], [20.11 Security].</p>\n</blockquote>\n<hr>\n<h2>1. Learning Objectives</h2>\n<p>After this lesson you will be able to:</p>\n<ul>\n<li>Design the <strong>edge + delivery + caching + object-storage</strong> layers of the platform (the read-performance + static-asset infrastructure).</li>\n<li>Place <strong>caching</strong> (Part 6) correctly: cache <strong>derived read models</strong> (portfolio views, market prices), <strong>never</strong> authorize money off a cache (20.4).</li>\n<li>Use <strong>object storage</strong> (4.3.2) for documents/statements/reports and a <strong>CDN</strong> (18.4) for static + cacheable content.</li>\n<li>Design the <strong>API gateway + BFF</strong> (12.6) as the single edge: routing, authN, rate limiting, aggregation.</li>\n<li>Handle deep dives: <strong>cache invalidation</strong> for financial data, <strong>what's cacheable vs not</strong>, and <strong>secure document delivery</strong>.</li>\n</ul>\n<hr>\n<h2>2. Motivation</h2>\n<p>The platform is <strong>read-heavy</strong> (portfolio dashboards — 20.2), serves <strong>large static/document content</strong> (statements, tax docs, reports), and needs a <strong>clean, secure edge</strong> for many client types (web/mobile/advisor/API). This lesson assembles the <strong>caching (Part 6), object storage (4.3.2), CDN (18.4), and API gateway (12.6)</strong> layers — the infrastructure that makes reads fast and delivery efficient <strong>without compromising the correctness of money</strong> (the golden rule: cache derived views, never the authoritative ledger for authorization).</p>\n<hr>\n<h2>3. The design (framework — 1.3.1)</h2>\n<h3>3.1 Caching (Part 6) — what and where</h3>\n<p><code>[CS]</code> Cache <strong>derived, read-mostly</strong> data; be careful with money <code>[BP]</code>:</p>\n<ul>\n<li><strong>Cache these:</strong> portfolio <strong>read-model</strong> views (positions/balances/valuations — 20.4/20.7), <strong>latest market prices</strong> (20.5), search results (20.8), reference data (instrument metadata), session/authz decisions (carefully — 20.3).</li>\n<li><strong>Topologies (6.2):</strong> CDN/edge (static) → API/gateway cache → <strong>distributed cache (Redis — 6.6)</strong> for shared read models → local caches. Cache-aside (6.3) for most read models.</li>\n<li><strong>The golden rule</strong> (20.4): <strong>never authorize a money operation off a cached balance</strong> — reads for <strong>display</strong> can be cached (eventually consistent — 10.5), but <strong>authorization/execution checks the authoritative ledger</strong>. Confusing display-cache with truth is a correctness bug.</li>\n<li><strong>Invalidation</strong> (6.5 — one of the hard things): financial read models update from <strong>events</strong> (CDC — 9.8) → <strong>event-driven invalidation/refresh</strong> of caches (not just TTL) so a trade reflects quickly; use short TTLs + versioned keys for prices. Balance freshness (users expect near-real-time portfolios) against load.</li>\n<li><code>[BP]</code> <strong>Cache derived views aggressively (Part 6), invalidate via events (9.8), never cache the authorization truth.</strong></li>\n</ul>\n<h3>3.2 Object storage (4.3.2)</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Documents/statements/tax reports/uploads/KYC docs</strong> → <strong>object/blob storage</strong> (4.3.2 — S3-style): cheap, durable, scalable, lifecycle-managed (tiering hot→cold→archive — 4.3.2).</li>\n<li><strong>Immutable + versioned</strong> where needed (statements are records — audit); <strong>encrypted at rest</strong> (15.4); <strong>access-controlled</strong> (a client's docs are private — 20.3).</li>\n<li><strong>Secure delivery:</strong> serve via <strong>signed, expiring URLs</strong> (not public buckets) so only authorized users fetch a document — a common, important pattern.</li>\n<li><code>[BP]</code> <strong>Object storage for documents/reports</strong> — durable, tiered, encrypted, access-controlled, delivered via signed URLs.</li>\n</ul>\n<h3>3.3 CDN &#x26; edge (18.4)</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>CDN</strong> (18.4/3.3.3) for <strong>static assets</strong> (app bundles, images) and <strong>cacheable content</strong> (public reference data, and cached documents via signed URLs at the edge).</li>\n<li><strong>Edge benefits:</strong> low global latency, origin offload, <strong>DDoS absorption + WAF</strong> (18.4/15.5) — the security first tier (20.11).</li>\n<li><strong>Careful:</strong> <strong>personalized/sensitive financial data is NOT edge-cached</strong> publicly — only static/public or per-user-authorized (signed) content. Never cache a portfolio at a shared edge.</li>\n<li><code>[BP]</code> <strong>CDN for static + public + authorized-document delivery + DDoS/WAF edge</strong> — not for private financial views.</li>\n</ul>\n<h3>3.4 API gateway + BFF (12.6)</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>API gateway</strong> (12.6): the <strong>single edge</strong> for all client traffic — <strong>routing</strong> to services, <strong>authN/token validation</strong> (20.3), <strong>rate limiting</strong> (15.7/20.11), request/response shaping, and observability (20.12). North-south entry.</li>\n<li><strong>BFF (Backend-for-Frontend)</strong> (12.6): per-client-type gateways (web/mobile/advisor/public-API) that <strong>aggregate</strong> downstream calls into client-shaped responses (e.g., a dashboard = portfolio + prices + recs in one call), reducing round-trips (17.4) + tailoring payloads.</li>\n<li><strong>Load balancing</strong> (3.3.1) in front; TLS termination at the edge (15.4).</li>\n<li><code>[BP]</code> <strong>Gateway = single secure edge (routing/authN/rate-limiting); BFF = client-tailored aggregation.</strong></li>\n</ul>\n<h3>3.5 Deep dives + bottlenecks</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Cache correctness</strong> (§3.1): display-cache vs authorization-truth — the golden rule (20.4); event-driven invalidation (9.8) for freshness.</li>\n<li><strong>Read scaling</strong> (7.5): cache + read replicas dissolve the read-heavy load (20.2); the gateway/BFF must scale statelessly (7.2).</li>\n<li><strong>Secure delivery:</strong> signed expiring URLs for documents; no public buckets; no edge-caching private data.</li>\n<li><strong>Gateway as a critical path:</strong> it's on every request → must be <strong>HA + fast + not a bottleneck</strong> (scale horizontally, avoid heavy logic); rate limiting protects backends (20.11).</li>\n<li><strong>Bottleneck:</strong> without caching, the ledger/DB would be hammered by reads → <strong>cache-first + replicas</strong> (Part 6/7.5) is the dominant relief (20.2); object storage + CDN offload large content.</li>\n<li><code>[BP]</code> <strong>The lesson:</strong> the read/delivery infrastructure = <strong>cache derived views (Part 6, event-invalidated — never the auth truth) + object storage for documents (4.3.2, encrypted, signed-URL delivery) + CDN for static/public/DDoS-WAF edge (18.4) + API gateway/BFF as the single secure, aggregating edge (12.6)</strong> — making reads fast and delivery efficient while keeping money correct.</li>\n</ul>\n<hr>\n<h2>4. Visual Intuition</h2>\n<pre><code class=\"language-mermaid\">flowchart TB\r\n    C[\"Clients (web/mobile/advisor/API)\"] --> CDN[\"CDN/edge (18.4): static + DDoS/WAF (20.11)\"]\r\n    CDN --> LB[\"Load balancer (3.3.1) + TLS termination\"]\r\n    LB --> GW[\"API Gateway + BFF (12.6): route + authN (20.3) + rate limit (20.11) + aggregate\"]\r\n    GW --> SVC[\"Services\"]\r\n    SVC --> CACHE[(\"Distributed cache (Redis — 6.6): derived read models + latest prices\")]\r\n    CACHE -. event-driven invalidation (CDC — 9.8) .- LEDGER[(\"Ledger events (20.4/20.7)\")]\r\n    SVC --> OBJ[(\"Object storage (4.3.2): documents/statements — encrypted, signed URLs\")]\r\n    note[\"Cache derived views (never the auth truth — 20.4). Object storage for docs (signed URLs). CDN for static/public + WAF. Gateway = single secure edge.\"]\n</code></pre>\n<hr>\n<h2>5. Real-World Analogy</h2>\n<p>Think of a <strong>bank's branch front, mailroom, and information boards</strong>.</p>\n<ul>\n<li><strong>Caching = the info boards showing recent balances and prices:</strong> they refresh <strong>moments after</strong> something changes, so customers get fast answers without a clerk digging in the vault each time. But — the golden rule — <strong>the teller never pays out based on the board</strong>; to actually move money, they <strong>check the official ledger</strong>. The board is for looking; the vault is for doing.</li>\n<li><strong>Object storage = the document archive/mailroom:</strong> statements, tax forms, and uploaded IDs are filed in a <strong>big, cheap, durable archive</strong>, with old ones moved to deep storage. You don't get them by walking into the archive — you're handed a <strong>single-use claim ticket</strong> (a signed, expiring link) that only works for <strong>your</strong> documents.</li>\n<li><strong>CDN = neighborhood pickup lockers:</strong> the bank's <strong>public brochures and app</strong> are stocked in lockers all over the city for instant pickup, and those lockers <strong>absorb the crowd</strong> (DDoS) at the edge. But your <strong>private statement</strong> is never left in a shared public locker — only static or personally-authorized items go to the edge.</li>\n<li><strong>API gateway + BFF = the front desk that routes you and bundles your errands:</strong> one <strong>secure entrance</strong> checks your ID and directs you, and a helpful concierge <strong>bundles several requests</strong> (\"here's your dashboard: balances, prices, and suggestions, all at once\") so you don't queue at five counters.</li>\n</ul>\n<hr>\n<h2>6. Industry Example</h2>\n<ul>\n<li><strong>Redis distributed cache for read models</strong> <code>[CONV]</code>: caching derived views + latest prices, event-invalidated (§3.1, 6.6/9.8). <em>(Representative.)</em></li>\n<li><strong>S3-style object storage + signed URLs</strong> <code>[CONV]</code>: durable encrypted document storage with secure delivery (§3.2, 4.3.2). <em>(Representative.)</em></li>\n<li><strong>CDN with WAF/DDoS at the edge</strong> <code>[CONV]</code>: static/public delivery + security first tier (§3.3, 18.4/15.5). <em>(Representative.)</em></li>\n<li><strong>API gateway + BFF</strong> <code>[CONV]</code>: single secure edge + client-tailored aggregation (§3.4, 12.6). <em>(Representative.)</em></li>\n</ul>\n<hr>\n<h2>7. Implementation Details</h2>\n<ul>\n<li><strong>Caching:</strong> distributed cache (Redis — 6.6) for derived read models + latest prices; cache-aside (6.3); <strong>event-driven invalidation</strong> (9.8); <strong>never cache the authorization truth</strong> (20.4) (§3.1).</li>\n<li><strong>Object storage</strong> (4.3.2): documents/statements — encrypted (15.4), lifecycle-tiered, access-controlled (20.3), <strong>signed expiring URLs</strong> (§3.2).</li>\n<li><strong>CDN</strong> (18.4): static + public + authorized-document edge delivery + WAF/DDoS (20.11); no private-data edge caching (§3.3).</li>\n<li><strong>API gateway + BFF</strong> (12.6): routing, authN (20.3), rate limiting (20.11), aggregation; LB (3.3.1) + TLS termination; stateless scale (7.2) (§3.4).</li>\n</ul>\n<hr>\n<h2>8–14. (Condensed)</h2>\n<p><strong>Advantages:</strong> fast read-heavy dashboards (cache + replicas); cheap durable document storage; low-latency static delivery + edge security; clean single secure edge + reduced round-trips (BFF).\r\n<strong>Disadvantages/cautions:</strong> cache invalidation is hard (event-driven + TTL); risk of caching stale/authoritative data (golden rule); gateway is a critical path (HA); signed-URL + access-control complexity for docs.\r\n<strong>When NOT to:</strong> don't cache/authorize money off a cache; don't edge-cache private financial data; don't put heavy logic in the gateway; don't serve documents from public buckets.\r\n<strong>Common mistakes:</strong> authorizing trades off cached balances (correctness bug); public buckets leaking documents; edge-caching personalized data; no event-driven invalidation (stale portfolios); gateway as an overloaded monolith.\r\n<strong>Interview Qs:</strong> 🟢 What do you cache and what do you never cache? 🟡 How do you invalidate financial read-model caches (events vs TTL)? 🔴 How do you deliver private documents securely (signed URLs, no public/edge)? Gateway vs BFF? ⚫ Design the full read/delivery infra: caching (with the golden rule), object storage, CDN, gateway/BFF — and defend the correctness boundaries.\r\n<strong>Production pitfalls:</strong> stale portfolio caches; authorization off stale cache; document leakage (misconfigured buckets/URLs); gateway saturation; edge caching sensitive data.\r\n<strong>Optimizations:</strong> event-driven cache invalidation (9.8); short-TTL + versioned keys for prices; read replicas (7.5); tiered object storage (4.3.2); BFF aggregation (17.4); edge WAF/DDoS (18.4).</p>\n<hr>\n<h2>15. Summary</h2>\n<p>The platform is <strong>read-heavy</strong> (portfolio dashboards — 20.2), serves <strong>large document content</strong> (statements, tax docs, KYC uploads), and needs a <strong>clean secure edge</strong> for many client types — so this lesson assembles the <strong>caching (Part 6), object storage (4.3.2), CDN (18.4), and API gateway/BFF (12.6)</strong> layers that make reads fast and delivery efficient <strong>without compromising money correctness</strong>. <strong>Caching</strong> targets <strong>derived, read-mostly</strong> data — portfolio <strong>read-model</strong> views (20.4/20.7), <strong>latest market prices</strong> (20.5), search results (20.8), reference data — across the standard topologies (CDN → gateway cache → distributed Redis — 6.6 → local), typically cache-aside (6.3); the <strong>golden rule</strong> (20.4) is <strong>never authorize a money operation off a cached balance</strong> — display reads can be cached (eventually consistent — 10.5) but <strong>authorization/execution must check the authoritative ledger</strong> (confusing display-cache with truth is a correctness bug) — and because financial read models update from <strong>events</strong>, use <strong>event-driven invalidation/refresh</strong> (CDC — 9.8), not just TTL, so trades reflect quickly. <strong>Object storage</strong> (4.3.2, S3-style) holds <strong>documents/statements/tax reports/uploads</strong> — cheap, durable, <strong>lifecycle-tiered</strong> (hot→cold→archive), <strong>immutable/versioned</strong> where records demand, <strong>encrypted at rest</strong> (15.4), <strong>access-controlled</strong> (20.3), and delivered via <strong>signed, expiring URLs</strong> (never public buckets). The <strong>CDN</strong> (18.4) serves <strong>static assets + public/cacheable content + authorized documents (signed URLs)</strong> at low global latency with <strong>origin offload + DDoS absorption + WAF</strong> (the security first tier — 20.11) — but <strong>personalized/sensitive financial data is never edge-cached</strong> publicly. The <strong>API gateway</strong> (12.6) is the <strong>single secure edge</strong> (routing, authN/token validation — 20.3, <strong>rate limiting</strong> — 15.7/20.11, observability — 20.12), fronted by a <strong>load balancer + TLS termination</strong> (3.3.1/15.4), and <strong>BFFs</strong> (per client type) <strong>aggregate</strong> downstream calls into client-shaped responses (a dashboard = portfolio + prices + recs in one call — reducing round-trips — 17.4). <strong>Deep dives:</strong> cache correctness (display vs auth-truth — the golden rule), read scaling (cache + replicas dissolve the read-heavy load — 7.5; gateway/BFF scale statelessly — 7.2), secure document delivery (signed URLs, no public/edge for private data), and the gateway as an <strong>HA critical path</strong>. The <strong>bottleneck</strong> — reads hammering the ledger/DB — is relieved by <strong>cache-first + replicas</strong> (the dominant lever — 20.2), with object storage + CDN offloading large content. In one line: <strong>cache derived views (event-invalidated, never the auth truth) + object storage for documents (encrypted, signed-URL delivery) + CDN for static/public/DDoS-WAF edge + API gateway/BFF as the single secure aggregating edge</strong>.</p>\n<hr>\n<h2>16. Revision Notes (flashcard-ready)</h2>\n<ul>\n<li><strong>Q:</strong> What do you cache vs never cache? <strong>A:</strong> Cache derived read views (portfolio, prices, search); never authorize money off a cache — check the authoritative ledger (20.4).</li>\n<li><strong>Q:</strong> How invalidate financial caches? <strong>A:</strong> Event-driven (CDC — 9.8) refresh + short TTL/versioned keys — not TTL alone — so trades reflect quickly.</li>\n<li><strong>Q:</strong> Where do documents live + how delivered? <strong>A:</strong> Object storage (4.3.2) — encrypted, tiered, access-controlled; delivered via signed, expiring URLs (no public buckets).</li>\n<li><strong>Q:</strong> What goes on the CDN? <strong>A:</strong> Static assets + public/cacheable content + authorized docs (signed URLs) + WAF/DDoS; never private financial data at a shared edge.</li>\n<li><strong>Q:</strong> API gateway role? <strong>A:</strong> Single secure edge — routing, authN, rate limiting, observability; HA critical path, keep it thin.</li>\n<li><strong>Q:</strong> BFF? <strong>A:</strong> Per-client-type gateway that aggregates downstream calls into client-shaped responses (fewer round-trips — 17.4).</li>\n<li><strong>Q:</strong> Dominant read-scaling lever? <strong>A:</strong> Cache-first + read replicas (Part 6/7.5) — dissolves the read-heavy portfolio load.</li>\n<li><strong>Q:</strong> Golden rule? <strong>A:</strong> Cache for display, never for authorization; the ledger is the truth (20.4).</li>\n</ul>\n<hr>\n<h2>17. Further Reading + Knowledge-Graph Links</h2>\n<p><strong>Foundations:</strong> [Part 6 Caching] · [4.3.2 Object Storage] · [18.4 CDN &#x26; Edge] · [12.6 API Gateway/BFF] · [3.3.1 Load Balancing] · [9.8 CDC] · [20.4 Ledger].\r\n<strong>External:</strong> Redis; S3 + signed URLs; CDN/WAF; API-gateway/BFF patterns. <em>(Representative.)</em></p>\n<blockquote>\n<p><strong>Knowledge-graph:</strong> <code>Part 6 cache</code> + <code>4.3.2 object storage</code> + <code>18.4 CDN</code> + <code>12.6 gateway</code> → <strong><code>20.9 read/delivery infra</code></strong> (cache derived views event-invalidated + object storage signed-URLs + CDN edge + gateway/BFF) → never cache the auth truth (20.4).</p>\n</blockquote>"
  },
  "_id": "lessons/part-20-capstone/20.9-caching-object-storage-cdn-gateway.md",
  "_raw": {
    "sourceFilePath": "lessons/part-20-capstone/20.9-caching-object-storage-cdn-gateway.md",
    "sourceFileName": "20.9-caching-object-storage-cdn-gateway.md",
    "sourceFileDir": "lessons/part-20-capstone",
    "contentType": "markdown",
    "flattenedPath": "lessons/part-20-capstone/20.9-caching-object-storage-cdn-gateway"
  },
  "type": "Lesson",
  "title": "Caching, Object Storage, CDN & API Gateway",
  "description": "",
  "slug": "part-20-capstone/20.9-caching-object-storage-cdn-gateway",
  "url": "/learn/part-20-capstone/20.9-caching-object-storage-cdn-gateway",
  "readingTime": 10,
  "flashcards": [
    {
      "q": "What do you cache vs never cache?",
      "a": "Cache derived read views (portfolio, prices, search); never authorize money off a cache — check the authoritative ledger (20.4)."
    },
    {
      "q": "How invalidate financial caches?",
      "a": "Event-driven (CDC — 9.8) refresh + short TTL/versioned keys — not TTL alone — so trades reflect quickly."
    },
    {
      "q": "Where do documents live + how delivered?",
      "a": "Object storage (4.3.2) — encrypted, tiered, access-controlled; delivered via signed, expiring URLs (no public buckets)."
    },
    {
      "q": "What goes on the CDN?",
      "a": "Static assets + public/cacheable content + authorized docs (signed URLs) + WAF/DDoS; never private financial data at a shared edge."
    },
    {
      "q": "API gateway role?",
      "a": "Single secure edge — routing, authN, rate limiting, observability; HA critical path, keep it thin."
    },
    {
      "q": "BFF?",
      "a": "Per-client-type gateway that aggregates downstream calls into client-shaped responses (fewer round-trips — 17.4)."
    },
    {
      "q": "Dominant read-scaling lever?",
      "a": "Cache-first + read replicas (Part 6/7.5) — dissolves the read-heavy portfolio load."
    },
    {
      "q": "Golden rule?",
      "a": "Cache for display, never for authorization; the ledger is the truth (20.4)."
    }
  ],
  "metadata": {
    "partNum": 0,
    "lessonId": "",
    "difficulty": "foundational",
    "prerequisites": [
      "[Part 6 Caching]",
      "[4.3.2 Object/Blob Storage]",
      "[18.4 CDN & Edge]",
      "[12.6 API Gateway/BFF]",
      "[3.3.1 Load Balancing]",
      "[20.4 Portfolio/Ledger]."
    ],
    "unlocks": [
      "[20.10 Reliability]",
      "[20.11 Security]."
    ]
  }
}