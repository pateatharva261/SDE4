{
  "body": {
    "raw": "# Lesson 20.4 — Portfolio, Transactions & the Ledger\r\n\r\n> Part 20 · Enterprise Capstone · Difficulty: ⚫ · *Capstone*\r\n>\r\n> **Prerequisites:** [19.2.3 Payment System], [18.3 Ledger/Distributed SQL], [5.2.1 ACID], [11.5 Idempotency], [10.7 CAP], [20.1 Contexts].\r\n> **Unlocks:** [20.6 Sagas], [20.7 CQRS/Event Sourcing].\r\n\r\n---\r\n\r\n## 1. Learning Objectives\r\n\r\nAfter this lesson you will be able to:\r\n\r\n- Design the **Portfolio** and **Ledger/Transactions** contexts (20.1) — the **financial heart** of the platform.\r\n- Apply the **double-entry ledger** (18.3/19.2.3) as the immutable, ACID, auditable source of truth for money + holdings.\r\n- Guarantee **exactly-once effects** for trades/transfers via **idempotency** (11.5) — no double-execution.\r\n- Separate the **write model** (the ledger — strongly consistent, CP — 10.7) from the **read model** (portfolio views — cached, eventually consistent) — a CQRS setup (20.7).\r\n- Reason about **consistency boundaries**: what must be transactional together vs what can be eventual.\r\n\r\n---\r\n\r\n## 2. Motivation\r\n\r\nThis is where **money lives** — the lesson the whole platform's correctness-first priority (20.1/20.2) was built for. A wrong balance, a lost trade, or a double-executed transfer is unacceptable and possibly illegal. So the Portfolio/Ledger contexts apply the **payment-system discipline** (19.2.3) at platform scale: **idempotency, an immutable double-entry ledger, ACID, CP, and audit**.\r\n\r\n---\r\n\r\n## 3. The design (framework — 1.3.1)\r\n\r\n### 3.1 Portfolio vs Ledger (two models)\r\n\r\n`[BP]`\r\n- **Ledger (write model — source of truth):** the **immutable, append-only, double-entry** record of every financial event (trades, transfers, fees, dividends). It is **the** authority on money + positions (18.3/19.2.3).\r\n- **Portfolio (read model — derived):** the **current view** clients/advisors see — positions, balances, valuations, performance. **Derived from the ledger** (+ market data for valuation — 20.5), **cached** and **eventually consistent** (10.5) — a **materialized read model** (CQRS — 20.7).\r\n- `[BP]` **Split write (CP ledger) from read (cached, eventual portfolio)** — the central structural decision (CQRS — 20.7). The books are exact; the dashboard is a fast derived projection.\r\n\r\n### 3.2 The double-entry ledger (18.3/19.2.3)\r\n\r\n`[CS]` The financial source of truth `[BP]`:\r\n- Every transaction = **balanced debit + credit** entries (money/assets move *between* accounts; totals sum to zero → self-checking — 18.3). Buying a stock: debit cash, credit the security position (+ a fee entry). This makes the system **auditable + verifiable** (the books must balance).\r\n- **Immutable / append-only:** never update or delete an entry; **corrections are new compensating entries** (event-sourcing flavor — 20.7). Gives a **complete, tamper-evident audit trail** (15.8/SOX).\r\n- **ACID + strongly consistent** (5.2.1): a transaction's entries commit atomically; balances are correct at commit. Store in a **strongly-consistent DB** — a relational or **globally-distributed SQL** store (18.3 — Spanner/Cockroach lineage) for ACID at scale. **CP** (10.7): prefer refusing over risking an incorrect balance.\r\n- `[BP]` **The ledger is sacred: immutable, balanced, ACID, CP, audited** — exactly as in 19.2.3, now the platform's core.\r\n\r\n### 3.3 Executing transactions — idempotency + exactly-once effects (11.5)\r\n\r\n`[BP]`\r\n- Every money-moving operation (trade, transfer, payment) carries an **idempotency key** (11.5): a **retry** (client timeout, network glitch — 8.1.1) **returns the original result, never re-executes** → **exactly-once effects** (at-least-once + idempotency — 9.4). No double-buys, no double-charges.\r\n- **A trade is a workflow**, often spanning services (validate → reserve funds → place order → settle → post to ledger → update portfolio) that **can't be one ACID transaction** across services → a **saga** (20.6/11.7) with compensations, and a **transactional outbox** (Part 9/12.5) to atomically post-and-emit — **no dual writes**.\r\n- `[BP]` **Idempotency + saga + outbox** make trades safe and recoverable (details in 20.6).\r\n\r\n### 3.4 Consistency boundaries (the design judgment)\r\n\r\n`[CS]` What must be **transactionally together** vs **eventual** `[BP]`:\r\n- **Transactional (strong, same context):** the double-entry ledger entries of a single transaction (debits + credits must commit together — an invariant — 12.4). Account balances that back a **hard invariant** (can't overdraw) need strong consistency / a real transaction (or a saga with reservation + compensation across contexts).\r\n- **Eventual (derived, cross-context):** the **portfolio read view**, valuations (depend on market data — 20.5), reporting (20.7), search indexes (20.8), notifications (19.1.4) — all **derived** and **eventually consistent** (10.5), updated from **ledger events** (Part 9/CDC — 9.8).\r\n- `[BP]` **Keep invariant-bound data together (strong); let derived data be eventual** (12.4/18.6). This is the core consistency judgment — over-doing strong consistency kills scale; under-doing it corrupts money.\r\n\r\n### 3.5 HLD\r\n\r\n`[BP]`\r\n- **Transaction/trade service** (idempotent — §3.3) → orchestrates the workflow (saga — 20.6) → **posts to the ledger** (ACID, immutable — §3.2) → emits **ledger events** (outbox — Part 9).\r\n- **Ledger store:** strongly-consistent distributed SQL (18.3); append-only; partitioned by account (7.3) keeping per-account transactions ACID.\r\n- **Portfolio read model:** consumes ledger events + market data → maintains **materialized positions/balances/valuations** in a fast read store + cache (Part 6/20.9); serves the read-heavy dashboard (20.2).\r\n- **Reporting/audit** (20.7): the immutable ledger + event stream = statements, tax, regulatory reports, and the audit trail.\r\n- `[BP]` **The lesson:** portfolio/ledger = **immutable double-entry ACID CP ledger (write, source of truth) + idempotent exactly-once transactions (saga + outbox) + a derived, cached, eventually-consistent portfolio read model (CQRS)**, with **invariant-bound data kept strongly consistent** and **derived data eventual**. The payment discipline (19.2.3) at platform scale.\r\n\r\n---\r\n\r\n## 4. Visual Intuition\r\n\r\n```mermaid\r\nflowchart TB\r\n    TX[\"Trade/transfer request {idempotencyKey}\"] --> IDEM{\"Seen key? (11.5)\"}\r\n    IDEM -->|yes| RET[\"Return original result\"]\r\n    IDEM -->|no| SAGA[\"Transaction workflow (saga — 20.6): validate→reserve→execute→settle\"]\r\n    SAGA --> LEDGER[(\"Double-entry ledger: immutable, ACID, CP, partitioned by account (18.3)\")]\r\n    LEDGER -. ledger events (outbox — Part 9/9.8 CDC) .-> READ[\"Portfolio read model (materialized, cached, eventual — CQRS 20.7)\"]\r\n    LEDGER -. events .-> REP[\"Reporting / audit (20.7)\"]\r\n    MD[\"Market data (20.5)\"] --> READ\r\n    READ --> DASH[\"Client/advisor dashboard (read-heavy — 20.2)\"]\r\n    note[\"Write=CP immutable ledger (source of truth). Read=derived eventual portfolio. Invariant-bound data strong; derived data eventual.\"]\r\n```\r\n\r\n---\r\n\r\n## 5. Real-World Analogy\r\n\r\nThink of a **brokerage's official books versus the account statement you see online**.\r\n\r\n- **The ledger = the official books, in ink, balanced, audited:** every trade is recorded as **matching debits and credits** (cash out, shares in) that must balance to the penny. Nothing is ever erased — a mistake becomes a **new correcting entry**. Auditors can reconstruct everything. This is the **truth**.\r\n- **Your online portfolio = a printed statement generated from those books:** it's fast and convenient, but it's a **snapshot derived from the ledger** (plus current prices). If it lags the official books by a moment, that's fine — it's a *view*, not the source of truth.\r\n- **Idempotency key = the order confirmation number:** if your \"buy\" click times out and you retry, the broker sees the same confirmation number and **doesn't buy twice** — you get the original fill.\r\n- **Consistency boundaries = what must balance instantly vs what can catch up:** the debit and credit of a single trade must post **together** (or not at all) — that's sacred. But your **performance chart, tax report, and search index** can update **a little later** — they're derived and can catch up.\r\n- **Why not make the fast statement the source of truth?** Because a fast, mutable statement can't be audited or trusted with money — the slow, immutable, balanced books must reign.\r\n\r\n---\r\n\r\n## 6. Industry Example\r\n\r\n- **Double-entry immutable ledger** `[CONV]`: the financial source of truth in brokerages/banks (§3.2, 18.3/19.2.3). *(Representative.)*\r\n- **Globally-distributed SQL for ACID at scale** `[CONV]`: Spanner/Cockroach-lineage stores for the ledger (§3.2, 18.3). *(Representative.)*\r\n- **Idempotency keys for exactly-once trades** `[CONV]`: safe retries on money movement (§3.3, 11.5). *(Representative.)*\r\n- **CQRS: derived portfolio read model from ledger events** `[CONV]`: materialized views updated via CDC/events (§3.1/3.4, 20.7/9.8). *(Representative.)*\r\n\r\n---\r\n\r\n## 7. Implementation Details\r\n\r\n- **Ledger:** immutable double-entry, ACID, CP, strongly-consistent distributed SQL (18.3), partitioned by account (7.3) (§3.2).\r\n- **Transactions:** idempotency keys → exactly-once effects (11.5); workflow as a saga (20.6/11.7) + transactional outbox (Part 9/12.5) (§3.3).\r\n- **Portfolio read model:** materialized positions/balances/valuations from ledger events + market data (20.5), cached (Part 6/20.9), eventually consistent (10.5) — CQRS (20.7) (§3.1/3.5).\r\n- **Consistency boundaries:** invariant-bound (ledger entries, hard balances) strong; derived (portfolio/reporting/search/notifications) eventual (12.4/18.6) (§3.4).\r\n\r\n---\r\n\r\n## 8–14. (Condensed)\r\n\r\n**Advantages:** correctness + auditability (immutable balanced ACID ledger); safe retries (idempotency); fast read dashboard (cached derived model); clean scale (strong only where needed, eventual elsewhere).\r\n**Disadvantages/cautions:** CQRS adds read/write divergence lag (eventual reads); ledger's strong consistency caps write throughput (accepted); saga complexity for cross-service trades (20.6); derived views must be rebuildable.\r\n**When NOT to:** don't make the read model the source of truth; don't use eventual consistency for ledger entries/hard invariants; don't skip idempotency on money moves.\r\n**Common mistakes:** mutable/updatable balances (no audit); reading balances off a stale cache to authorize a trade (must check the ledger); dual writes (use outbox); one giant transaction across contexts (use sagas); over-applying strong consistency everywhere (kills scale).\r\n**Interview Qs:** 🟢 Why a double-entry immutable ledger? 🟡 How do you prevent double-execution of a trade (idempotency)? 🔴 Write model vs read model (CQRS) — what's strong vs eventual? ⚫ Full portfolio/ledger design: ledger, idempotent saga+outbox transactions, derived read model, consistency boundaries.\r\n**Production pitfalls:** ledger imbalance from partial failures (reconcile — 19.2.3); stale read model misinforming decisions; authorizing off cached balances; saga compensation gaps; hot account partitions (7.4).\r\n**Optimizations:** partition ledger by account (7.3) keeping per-account ACID; materialized read views + cache (Part 6); event-driven read-model updates (CDC — 9.8); reconciliation jobs (19.2.3); snapshot balances for fast valuation.\r\n\r\n---\r\n\r\n## 15. Summary\r\n\r\nThe **Portfolio** and **Ledger/Transactions** contexts are the **financial heart** of the platform — the reason the whole correctness-first priority (20.1/20.2) exists — and they apply the **payment-system discipline (19.2.3) at platform scale**. The structure splits **two models**: the **ledger (write model, source of truth)** is the **immutable, append-only, double-entry** record of every financial event, and the **portfolio (read model)** is the **current view** clients see (positions, balances, valuations), **derived from the ledger + market data**, **cached and eventually consistent** — a **CQRS** split (20.7). The **double-entry ledger** (18.3) records each transaction as **balanced debit + credit** entries (money/assets move between accounts, totals sum to zero → **self-checking**), is **immutable/append-only** (corrections are new compensating entries → tamper-evident audit trail for SOX — 15.8), and is **ACID + strongly consistent + CP** (10.7) stored in a **globally-distributed SQL** engine (18.3) for ACID at scale — **sacred: immutable, balanced, ACID, CP, audited**. Every money-moving operation carries an **idempotency key** so retries **return the original result rather than re-executing** → **exactly-once effects** (11.5/9.4 — no double-buys); and because a trade is a **multi-service workflow** that can't be one ACID transaction, it runs as a **saga** (20.6/11.7) with compensations plus a **transactional outbox** (Part 9/12.5) to atomically post-and-emit (**no dual writes**). The key **consistency judgment**: keep **invariant-bound data transactionally together and strongly consistent** (a transaction's ledger entries must commit atomically; hard balances that can't overdraw), but let **derived, cross-context data be eventual** (the portfolio read view, valuations from market data, reporting, search indexes, notifications — all updated from **ledger events via CDC/outbox** — 9.8) — over-applying strong consistency kills scale, under-applying it corrupts money (12.4/18.6). The **HLD**: an idempotent transaction service orchestrates the saga and **posts to the ACID immutable ledger** (partitioned by account — 7.3, keeping per-account transactions ACID) emitting **ledger events**; a **portfolio read model** consumes those events + market data into **materialized, cached, eventually-consistent** positions/balances/valuations serving the read-heavy dashboard (20.2); and reporting/audit (20.7) derive from the same immutable ledger + event stream. In one line: **immutable double-entry ACID CP ledger (write) + idempotent exactly-once transactions (saga + outbox) + derived cached eventual portfolio read model (CQRS), with invariant-bound data strong and derived data eventual**.\r\n\r\n---\r\n\r\n## 16. Revision Notes (flashcard-ready)\r\n\r\n- **Q:** Write model vs read model? **A:** Ledger = immutable ACID CP source of truth (write); Portfolio = derived, cached, eventually-consistent view (read) — CQRS.\r\n- **Q:** Ledger properties? **A:** Double-entry balanced, immutable/append-only, ACID, strongly consistent, CP, auditable (18.3).\r\n- **Q:** How correct a mistake in an immutable ledger? **A:** New compensating entry — never update/delete.\r\n- **Q:** Prevent double-execution of a trade? **A:** Idempotency key → return original result on retry (exactly-once effects — 11.5).\r\n- **Q:** Why is a trade a saga? **A:** Multi-service workflow (validate→reserve→execute→settle→post) can't be one ACID transaction → saga + compensations + outbox (20.6).\r\n- **Q:** What's strong vs eventual? **A:** Strong: ledger entries + hard-invariant balances. Eventual: portfolio view, valuations, reporting, search, notifications (derived).\r\n- **Q:** How is the portfolio view updated? **A:** From ledger events (CDC/outbox — 9.8) + market data → materialized, cached.\r\n- **Q:** Can you authorize a trade off a cached balance? **A:** No — check the authoritative ledger; caches are for display, not authorization.\r\n- **Q:** Ledger scaling? **A:** Partition by account (7.3) keeping per-account transactions ACID; reconcile (19.2.3).\r\n\r\n---\r\n\r\n## 17. Further Reading + Knowledge-Graph Links\r\n\r\n**Foundations:** [19.2.3 Payment System] · [18.3 Ledger/Distributed SQL] · [5.2.1 ACID] · [11.5 Idempotency] · [20.6 Sagas] · [20.7 CQRS/Event Sourcing] · [9.8 CDC/Outbox].\r\n**External:** Double-entry accounting; Spanner/CockroachDB; CQRS/event-sourcing literature. *(Representative.)*\r\n\r\n> **Knowledge-graph:** `18.3 ledger` + `11.5 idempotency` + `CQRS` + `12.4 consistency boundaries` → **`20.4 portfolio & ledger`** (immutable ACID CP ledger + idempotent saga transactions + derived eventual read model) → feeds 20.6/20.7.\r\n",
    "html": "<h1>Lesson 20.4 — Portfolio, Transactions &#x26; the Ledger</h1>\n<blockquote>\n<p>Part 20 · Enterprise Capstone · Difficulty: ⚫ · <em>Capstone</em></p>\n<p><strong>Prerequisites:</strong> [19.2.3 Payment System], [18.3 Ledger/Distributed SQL], [5.2.1 ACID], [11.5 Idempotency], [10.7 CAP], [20.1 Contexts].\r\n<strong>Unlocks:</strong> [20.6 Sagas], [20.7 CQRS/Event Sourcing].</p>\n</blockquote>\n<hr>\n<h2>1. Learning Objectives</h2>\n<p>After this lesson you will be able to:</p>\n<ul>\n<li>Design the <strong>Portfolio</strong> and <strong>Ledger/Transactions</strong> contexts (20.1) — the <strong>financial heart</strong> of the platform.</li>\n<li>Apply the <strong>double-entry ledger</strong> (18.3/19.2.3) as the immutable, ACID, auditable source of truth for money + holdings.</li>\n<li>Guarantee <strong>exactly-once effects</strong> for trades/transfers via <strong>idempotency</strong> (11.5) — no double-execution.</li>\n<li>Separate the <strong>write model</strong> (the ledger — strongly consistent, CP — 10.7) from the <strong>read model</strong> (portfolio views — cached, eventually consistent) — a CQRS setup (20.7).</li>\n<li>Reason about <strong>consistency boundaries</strong>: what must be transactional together vs what can be eventual.</li>\n</ul>\n<hr>\n<h2>2. Motivation</h2>\n<p>This is where <strong>money lives</strong> — the lesson the whole platform's correctness-first priority (20.1/20.2) was built for. A wrong balance, a lost trade, or a double-executed transfer is unacceptable and possibly illegal. So the Portfolio/Ledger contexts apply the <strong>payment-system discipline</strong> (19.2.3) at platform scale: <strong>idempotency, an immutable double-entry ledger, ACID, CP, and audit</strong>.</p>\n<hr>\n<h2>3. The design (framework — 1.3.1)</h2>\n<h3>3.1 Portfolio vs Ledger (two models)</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Ledger (write model — source of truth):</strong> the <strong>immutable, append-only, double-entry</strong> record of every financial event (trades, transfers, fees, dividends). It is <strong>the</strong> authority on money + positions (18.3/19.2.3).</li>\n<li><strong>Portfolio (read model — derived):</strong> the <strong>current view</strong> clients/advisors see — positions, balances, valuations, performance. <strong>Derived from the ledger</strong> (+ market data for valuation — 20.5), <strong>cached</strong> and <strong>eventually consistent</strong> (10.5) — a <strong>materialized read model</strong> (CQRS — 20.7).</li>\n<li><code>[BP]</code> <strong>Split write (CP ledger) from read (cached, eventual portfolio)</strong> — the central structural decision (CQRS — 20.7). The books are exact; the dashboard is a fast derived projection.</li>\n</ul>\n<h3>3.2 The double-entry ledger (18.3/19.2.3)</h3>\n<p><code>[CS]</code> The financial source of truth <code>[BP]</code>:</p>\n<ul>\n<li>Every transaction = <strong>balanced debit + credit</strong> entries (money/assets move <em>between</em> accounts; totals sum to zero → self-checking — 18.3). Buying a stock: debit cash, credit the security position (+ a fee entry). This makes the system <strong>auditable + verifiable</strong> (the books must balance).</li>\n<li><strong>Immutable / append-only:</strong> never update or delete an entry; <strong>corrections are new compensating entries</strong> (event-sourcing flavor — 20.7). Gives a <strong>complete, tamper-evident audit trail</strong> (15.8/SOX).</li>\n<li><strong>ACID + strongly consistent</strong> (5.2.1): a transaction's entries commit atomically; balances are correct at commit. Store in a <strong>strongly-consistent DB</strong> — a relational or <strong>globally-distributed SQL</strong> store (18.3 — Spanner/Cockroach lineage) for ACID at scale. <strong>CP</strong> (10.7): prefer refusing over risking an incorrect balance.</li>\n<li><code>[BP]</code> <strong>The ledger is sacred: immutable, balanced, ACID, CP, audited</strong> — exactly as in 19.2.3, now the platform's core.</li>\n</ul>\n<h3>3.3 Executing transactions — idempotency + exactly-once effects (11.5)</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li>Every money-moving operation (trade, transfer, payment) carries an <strong>idempotency key</strong> (11.5): a <strong>retry</strong> (client timeout, network glitch — 8.1.1) <strong>returns the original result, never re-executes</strong> → <strong>exactly-once effects</strong> (at-least-once + idempotency — 9.4). No double-buys, no double-charges.</li>\n<li><strong>A trade is a workflow</strong>, often spanning services (validate → reserve funds → place order → settle → post to ledger → update portfolio) that <strong>can't be one ACID transaction</strong> across services → a <strong>saga</strong> (20.6/11.7) with compensations, and a <strong>transactional outbox</strong> (Part 9/12.5) to atomically post-and-emit — <strong>no dual writes</strong>.</li>\n<li><code>[BP]</code> <strong>Idempotency + saga + outbox</strong> make trades safe and recoverable (details in 20.6).</li>\n</ul>\n<h3>3.4 Consistency boundaries (the design judgment)</h3>\n<p><code>[CS]</code> What must be <strong>transactionally together</strong> vs <strong>eventual</strong> <code>[BP]</code>:</p>\n<ul>\n<li><strong>Transactional (strong, same context):</strong> the double-entry ledger entries of a single transaction (debits + credits must commit together — an invariant — 12.4). Account balances that back a <strong>hard invariant</strong> (can't overdraw) need strong consistency / a real transaction (or a saga with reservation + compensation across contexts).</li>\n<li><strong>Eventual (derived, cross-context):</strong> the <strong>portfolio read view</strong>, valuations (depend on market data — 20.5), reporting (20.7), search indexes (20.8), notifications (19.1.4) — all <strong>derived</strong> and <strong>eventually consistent</strong> (10.5), updated from <strong>ledger events</strong> (Part 9/CDC — 9.8).</li>\n<li><code>[BP]</code> <strong>Keep invariant-bound data together (strong); let derived data be eventual</strong> (12.4/18.6). This is the core consistency judgment — over-doing strong consistency kills scale; under-doing it corrupts money.</li>\n</ul>\n<h3>3.5 HLD</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Transaction/trade service</strong> (idempotent — §3.3) → orchestrates the workflow (saga — 20.6) → <strong>posts to the ledger</strong> (ACID, immutable — §3.2) → emits <strong>ledger events</strong> (outbox — Part 9).</li>\n<li><strong>Ledger store:</strong> strongly-consistent distributed SQL (18.3); append-only; partitioned by account (7.3) keeping per-account transactions ACID.</li>\n<li><strong>Portfolio read model:</strong> consumes ledger events + market data → maintains <strong>materialized positions/balances/valuations</strong> in a fast read store + cache (Part 6/20.9); serves the read-heavy dashboard (20.2).</li>\n<li><strong>Reporting/audit</strong> (20.7): the immutable ledger + event stream = statements, tax, regulatory reports, and the audit trail.</li>\n<li><code>[BP]</code> <strong>The lesson:</strong> portfolio/ledger = <strong>immutable double-entry ACID CP ledger (write, source of truth) + idempotent exactly-once transactions (saga + outbox) + a derived, cached, eventually-consistent portfolio read model (CQRS)</strong>, with <strong>invariant-bound data kept strongly consistent</strong> and <strong>derived data eventual</strong>. The payment discipline (19.2.3) at platform scale.</li>\n</ul>\n<hr>\n<h2>4. Visual Intuition</h2>\n<pre><code class=\"language-mermaid\">flowchart TB\r\n    TX[\"Trade/transfer request {idempotencyKey}\"] --> IDEM{\"Seen key? (11.5)\"}\r\n    IDEM -->|yes| RET[\"Return original result\"]\r\n    IDEM -->|no| SAGA[\"Transaction workflow (saga — 20.6): validate→reserve→execute→settle\"]\r\n    SAGA --> LEDGER[(\"Double-entry ledger: immutable, ACID, CP, partitioned by account (18.3)\")]\r\n    LEDGER -. ledger events (outbox — Part 9/9.8 CDC) .-> READ[\"Portfolio read model (materialized, cached, eventual — CQRS 20.7)\"]\r\n    LEDGER -. events .-> REP[\"Reporting / audit (20.7)\"]\r\n    MD[\"Market data (20.5)\"] --> READ\r\n    READ --> DASH[\"Client/advisor dashboard (read-heavy — 20.2)\"]\r\n    note[\"Write=CP immutable ledger (source of truth). Read=derived eventual portfolio. Invariant-bound data strong; derived data eventual.\"]\n</code></pre>\n<hr>\n<h2>5. Real-World Analogy</h2>\n<p>Think of a <strong>brokerage's official books versus the account statement you see online</strong>.</p>\n<ul>\n<li><strong>The ledger = the official books, in ink, balanced, audited:</strong> every trade is recorded as <strong>matching debits and credits</strong> (cash out, shares in) that must balance to the penny. Nothing is ever erased — a mistake becomes a <strong>new correcting entry</strong>. Auditors can reconstruct everything. This is the <strong>truth</strong>.</li>\n<li><strong>Your online portfolio = a printed statement generated from those books:</strong> it's fast and convenient, but it's a <strong>snapshot derived from the ledger</strong> (plus current prices). If it lags the official books by a moment, that's fine — it's a <em>view</em>, not the source of truth.</li>\n<li><strong>Idempotency key = the order confirmation number:</strong> if your \"buy\" click times out and you retry, the broker sees the same confirmation number and <strong>doesn't buy twice</strong> — you get the original fill.</li>\n<li><strong>Consistency boundaries = what must balance instantly vs what can catch up:</strong> the debit and credit of a single trade must post <strong>together</strong> (or not at all) — that's sacred. But your <strong>performance chart, tax report, and search index</strong> can update <strong>a little later</strong> — they're derived and can catch up.</li>\n<li><strong>Why not make the fast statement the source of truth?</strong> Because a fast, mutable statement can't be audited or trusted with money — the slow, immutable, balanced books must reign.</li>\n</ul>\n<hr>\n<h2>6. Industry Example</h2>\n<ul>\n<li><strong>Double-entry immutable ledger</strong> <code>[CONV]</code>: the financial source of truth in brokerages/banks (§3.2, 18.3/19.2.3). <em>(Representative.)</em></li>\n<li><strong>Globally-distributed SQL for ACID at scale</strong> <code>[CONV]</code>: Spanner/Cockroach-lineage stores for the ledger (§3.2, 18.3). <em>(Representative.)</em></li>\n<li><strong>Idempotency keys for exactly-once trades</strong> <code>[CONV]</code>: safe retries on money movement (§3.3, 11.5). <em>(Representative.)</em></li>\n<li><strong>CQRS: derived portfolio read model from ledger events</strong> <code>[CONV]</code>: materialized views updated via CDC/events (§3.1/3.4, 20.7/9.8). <em>(Representative.)</em></li>\n</ul>\n<hr>\n<h2>7. Implementation Details</h2>\n<ul>\n<li><strong>Ledger:</strong> immutable double-entry, ACID, CP, strongly-consistent distributed SQL (18.3), partitioned by account (7.3) (§3.2).</li>\n<li><strong>Transactions:</strong> idempotency keys → exactly-once effects (11.5); workflow as a saga (20.6/11.7) + transactional outbox (Part 9/12.5) (§3.3).</li>\n<li><strong>Portfolio read model:</strong> materialized positions/balances/valuations from ledger events + market data (20.5), cached (Part 6/20.9), eventually consistent (10.5) — CQRS (20.7) (§3.1/3.5).</li>\n<li><strong>Consistency boundaries:</strong> invariant-bound (ledger entries, hard balances) strong; derived (portfolio/reporting/search/notifications) eventual (12.4/18.6) (§3.4).</li>\n</ul>\n<hr>\n<h2>8–14. (Condensed)</h2>\n<p><strong>Advantages:</strong> correctness + auditability (immutable balanced ACID ledger); safe retries (idempotency); fast read dashboard (cached derived model); clean scale (strong only where needed, eventual elsewhere).\r\n<strong>Disadvantages/cautions:</strong> CQRS adds read/write divergence lag (eventual reads); ledger's strong consistency caps write throughput (accepted); saga complexity for cross-service trades (20.6); derived views must be rebuildable.\r\n<strong>When NOT to:</strong> don't make the read model the source of truth; don't use eventual consistency for ledger entries/hard invariants; don't skip idempotency on money moves.\r\n<strong>Common mistakes:</strong> mutable/updatable balances (no audit); reading balances off a stale cache to authorize a trade (must check the ledger); dual writes (use outbox); one giant transaction across contexts (use sagas); over-applying strong consistency everywhere (kills scale).\r\n<strong>Interview Qs:</strong> 🟢 Why a double-entry immutable ledger? 🟡 How do you prevent double-execution of a trade (idempotency)? 🔴 Write model vs read model (CQRS) — what's strong vs eventual? ⚫ Full portfolio/ledger design: ledger, idempotent saga+outbox transactions, derived read model, consistency boundaries.\r\n<strong>Production pitfalls:</strong> ledger imbalance from partial failures (reconcile — 19.2.3); stale read model misinforming decisions; authorizing off cached balances; saga compensation gaps; hot account partitions (7.4).\r\n<strong>Optimizations:</strong> partition ledger by account (7.3) keeping per-account ACID; materialized read views + cache (Part 6); event-driven read-model updates (CDC — 9.8); reconciliation jobs (19.2.3); snapshot balances for fast valuation.</p>\n<hr>\n<h2>15. Summary</h2>\n<p>The <strong>Portfolio</strong> and <strong>Ledger/Transactions</strong> contexts are the <strong>financial heart</strong> of the platform — the reason the whole correctness-first priority (20.1/20.2) exists — and they apply the <strong>payment-system discipline (19.2.3) at platform scale</strong>. The structure splits <strong>two models</strong>: the <strong>ledger (write model, source of truth)</strong> is the <strong>immutable, append-only, double-entry</strong> record of every financial event, and the <strong>portfolio (read model)</strong> is the <strong>current view</strong> clients see (positions, balances, valuations), <strong>derived from the ledger + market data</strong>, <strong>cached and eventually consistent</strong> — a <strong>CQRS</strong> split (20.7). The <strong>double-entry ledger</strong> (18.3) records each transaction as <strong>balanced debit + credit</strong> entries (money/assets move between accounts, totals sum to zero → <strong>self-checking</strong>), is <strong>immutable/append-only</strong> (corrections are new compensating entries → tamper-evident audit trail for SOX — 15.8), and is <strong>ACID + strongly consistent + CP</strong> (10.7) stored in a <strong>globally-distributed SQL</strong> engine (18.3) for ACID at scale — <strong>sacred: immutable, balanced, ACID, CP, audited</strong>. Every money-moving operation carries an <strong>idempotency key</strong> so retries <strong>return the original result rather than re-executing</strong> → <strong>exactly-once effects</strong> (11.5/9.4 — no double-buys); and because a trade is a <strong>multi-service workflow</strong> that can't be one ACID transaction, it runs as a <strong>saga</strong> (20.6/11.7) with compensations plus a <strong>transactional outbox</strong> (Part 9/12.5) to atomically post-and-emit (<strong>no dual writes</strong>). The key <strong>consistency judgment</strong>: keep <strong>invariant-bound data transactionally together and strongly consistent</strong> (a transaction's ledger entries must commit atomically; hard balances that can't overdraw), but let <strong>derived, cross-context data be eventual</strong> (the portfolio read view, valuations from market data, reporting, search indexes, notifications — all updated from <strong>ledger events via CDC/outbox</strong> — 9.8) — over-applying strong consistency kills scale, under-applying it corrupts money (12.4/18.6). The <strong>HLD</strong>: an idempotent transaction service orchestrates the saga and <strong>posts to the ACID immutable ledger</strong> (partitioned by account — 7.3, keeping per-account transactions ACID) emitting <strong>ledger events</strong>; a <strong>portfolio read model</strong> consumes those events + market data into <strong>materialized, cached, eventually-consistent</strong> positions/balances/valuations serving the read-heavy dashboard (20.2); and reporting/audit (20.7) derive from the same immutable ledger + event stream. In one line: <strong>immutable double-entry ACID CP ledger (write) + idempotent exactly-once transactions (saga + outbox) + derived cached eventual portfolio read model (CQRS), with invariant-bound data strong and derived data eventual</strong>.</p>\n<hr>\n<h2>16. Revision Notes (flashcard-ready)</h2>\n<ul>\n<li><strong>Q:</strong> Write model vs read model? <strong>A:</strong> Ledger = immutable ACID CP source of truth (write); Portfolio = derived, cached, eventually-consistent view (read) — CQRS.</li>\n<li><strong>Q:</strong> Ledger properties? <strong>A:</strong> Double-entry balanced, immutable/append-only, ACID, strongly consistent, CP, auditable (18.3).</li>\n<li><strong>Q:</strong> How correct a mistake in an immutable ledger? <strong>A:</strong> New compensating entry — never update/delete.</li>\n<li><strong>Q:</strong> Prevent double-execution of a trade? <strong>A:</strong> Idempotency key → return original result on retry (exactly-once effects — 11.5).</li>\n<li><strong>Q:</strong> Why is a trade a saga? <strong>A:</strong> Multi-service workflow (validate→reserve→execute→settle→post) can't be one ACID transaction → saga + compensations + outbox (20.6).</li>\n<li><strong>Q:</strong> What's strong vs eventual? <strong>A:</strong> Strong: ledger entries + hard-invariant balances. Eventual: portfolio view, valuations, reporting, search, notifications (derived).</li>\n<li><strong>Q:</strong> How is the portfolio view updated? <strong>A:</strong> From ledger events (CDC/outbox — 9.8) + market data → materialized, cached.</li>\n<li><strong>Q:</strong> Can you authorize a trade off a cached balance? <strong>A:</strong> No — check the authoritative ledger; caches are for display, not authorization.</li>\n<li><strong>Q:</strong> Ledger scaling? <strong>A:</strong> Partition by account (7.3) keeping per-account transactions ACID; reconcile (19.2.3).</li>\n</ul>\n<hr>\n<h2>17. Further Reading + Knowledge-Graph Links</h2>\n<p><strong>Foundations:</strong> [19.2.3 Payment System] · [18.3 Ledger/Distributed SQL] · [5.2.1 ACID] · [11.5 Idempotency] · [20.6 Sagas] · [20.7 CQRS/Event Sourcing] · [9.8 CDC/Outbox].\r\n<strong>External:</strong> Double-entry accounting; Spanner/CockroachDB; CQRS/event-sourcing literature. <em>(Representative.)</em></p>\n<blockquote>\n<p><strong>Knowledge-graph:</strong> <code>18.3 ledger</code> + <code>11.5 idempotency</code> + <code>CQRS</code> + <code>12.4 consistency boundaries</code> → <strong><code>20.4 portfolio &#x26; ledger</code></strong> (immutable ACID CP ledger + idempotent saga transactions + derived eventual read model) → feeds 20.6/20.7.</p>\n</blockquote>"
  },
  "_id": "lessons/part-20-capstone/20.4-portfolio-transactions-ledger.md",
  "_raw": {
    "sourceFilePath": "lessons/part-20-capstone/20.4-portfolio-transactions-ledger.md",
    "sourceFileName": "20.4-portfolio-transactions-ledger.md",
    "sourceFileDir": "lessons/part-20-capstone",
    "contentType": "markdown",
    "flattenedPath": "lessons/part-20-capstone/20.4-portfolio-transactions-ledger"
  },
  "type": "Lesson",
  "title": "Portfolio, Transactions & the Ledger",
  "description": "",
  "slug": "part-20-capstone/20.4-portfolio-transactions-ledger",
  "url": "/learn/part-20-capstone/20.4-portfolio-transactions-ledger",
  "readingTime": 10,
  "flashcards": [
    {
      "q": "Write model vs read model?",
      "a": "Ledger = immutable ACID CP source of truth (write); Portfolio = derived, cached, eventually-consistent view (read) — CQRS."
    },
    {
      "q": "Ledger properties?",
      "a": "Double-entry balanced, immutable/append-only, ACID, strongly consistent, CP, auditable (18.3)."
    },
    {
      "q": "How correct a mistake in an immutable ledger?",
      "a": "New compensating entry — never update/delete."
    },
    {
      "q": "Prevent double-execution of a trade?",
      "a": "Idempotency key → return original result on retry (exactly-once effects — 11.5)."
    },
    {
      "q": "Why is a trade a saga?",
      "a": "Multi-service workflow (validate→reserve→execute→settle→post) can't be one ACID transaction → saga + compensations + outbox (20.6)."
    },
    {
      "q": "What's strong vs eventual?",
      "a": "Strong: ledger entries + hard-invariant balances. Eventual: portfolio view, valuations, reporting, search, notifications (derived)."
    },
    {
      "q": "How is the portfolio view updated?",
      "a": "From ledger events (CDC/outbox — 9.8) + market data → materialized, cached."
    },
    {
      "q": "Can you authorize a trade off a cached balance?",
      "a": "No — check the authoritative ledger; caches are for display, not authorization."
    },
    {
      "q": "Ledger scaling?",
      "a": "Partition by account (7.3) keeping per-account transactions ACID; reconcile (19.2.3)."
    }
  ],
  "metadata": {
    "partNum": 0,
    "lessonId": "",
    "difficulty": "foundational",
    "prerequisites": [
      "[19.2.3 Payment System]",
      "[18.3 Ledger/Distributed SQL]",
      "[5.2.1 ACID]",
      "[11.5 Idempotency]",
      "[10.7 CAP]",
      "[20.1 Contexts]."
    ],
    "unlocks": [
      "[20.6 Sagas]",
      "[20.7 CQRS/Event Sourcing]."
    ]
  }
}