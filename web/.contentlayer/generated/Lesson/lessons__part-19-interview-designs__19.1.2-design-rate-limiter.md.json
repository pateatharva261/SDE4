{
  "body": {
    "raw": "# Lesson 19.1.2 — Design a Rate Limiter\r\n\r\n> Part 19 · Module 19.1 (Volume 1) · Difficulty: 🟡🔴 · *Interview design*\r\n>\r\n> **Prerequisites:** [2.4.3 Concurrency], [2.4.4 LLD (rate limiter)], [6.6 Distributed Caching (Redis)], [11.4 Load Shedding], [15.7 Rate Limiting], [1.3.1 Framework].\r\n> **Unlocks:** [19.2.5 Distributed Lock], [Part 20 Capstone].\r\n\r\n---\r\n\r\n## 1. Learning Objectives\r\n\r\nAfter this lesson you will be able to:\r\n\r\n- Design a **distributed rate limiter** end-to-end (framework — 1.3.1): requirements → API → algorithm → distributed enforcement → deep dives.\r\n- Choose the **algorithm** (token bucket / leaky bucket / fixed window / sliding window — 15.7/2.4.4) and justify it.\r\n- Solve the **distributed-state problem**: enforcing a **global** limit across many instances (shared Redis — 6.6) with accuracy vs performance tradeoffs.\r\n- Handle deep dives: **where to enforce** (gateway — 12.6), **dimensions** (15.7), **the counter store as a dependency/SPOF**, **atomicity** (race conditions), and **graceful client behavior** (429 + Retry-After).\r\n- Reuse the concepts from **15.7** (rate limiting as reliability + security) in a concrete design.\r\n\r\n---\r\n\r\n## 2. Problem statement\r\n\r\nDesign a **rate limiter**: restrict how many requests a client may make in a time window (e.g., \"100 requests/minute per user\"), rejecting excess with **HTTP 429**. It's both a **reliability** control (protect capacity — 11.4) and a **security** control (throttle abuse — 15.7), and a classic interview design that hinges on the **algorithm** and the **distributed-state** problem. (This applies 15.7 — rate limiting concepts — as an end-to-end design; and 2.4.4 — the LLD.)\r\n\r\n---\r\n\r\n## 3. The design (framework — 1.3.1)\r\n\r\n### 3.1 Requirements\r\n\r\n`[BP]`\r\n- **Functional:** limit requests per client per window; **reject excess** (429 + `Retry-After`); configurable rules (per-user/IP/API-key/endpoint — 15.7); ideally distributed (works across many instances).\r\n- **Non-functional:** **low latency + low overhead** (it's on every request's hot path → must be cheap), **accurate** (enforce the limit, not wildly over/under), **highly available** (a broken limiter shouldn't break the API — fail-open vs fail-closed — §3.6), **scalable**.\r\n- `[BP]` **Key signals:** it's **on every request** (must be fast + HA) and must enforce a **global** limit across a **distributed** fleet (§3.4 — the crux). Drive to **algorithm + distributed state**.\r\n\r\n### 3.2 API / placement\r\n\r\n`[BP]`\r\n- Typically a **middleware/component at the API gateway** (12.6) or a **sidecar** (13.6/12.7 mesh) — enforce at the **edge**, rejecting excess **before** it consumes backend resources (11.4 load shedding — cheapest — 15.7).\r\n- Interface: `allow(clientId, rule) → allowed | denied(retryAfter)`.\r\n- `[BP]` **Enforce early** (gateway) + optionally per-service (defense in depth — 15.7).\r\n\r\n### 3.3 Algorithm (15.7/2.4.4)\r\n\r\n`[CS]` The core choice (15.7 §3.2) `[BP]`:\r\n- **Token bucket (recommended default):** a bucket of N tokens, refilled at a rate; each request consumes one; empty → reject. **Allows bursts** (up to N) while capping the average → flexible, common. Needs **2 values per client**: token count + last-refill timestamp.\r\n- **Leaky bucket:** fixed-rate drain (smooths output); good for steady downstream protection.\r\n- **Fixed window:** count per window; simple but **boundary bursts** (2× at edges).\r\n- **Sliding window (log or counter):** accurate, no boundary problem, but more memory/compute (sliding-window-counter is a cheap approximation).\r\n- `[BP]` **Recommendation: token bucket** (burst-tolerant, cheap — 2 values/client) for general API rate limiting; **sliding-window-counter** if you need accuracy without the fixed-window boundary problem. (Detailed algorithm/object-model in 2.4.4.)\r\n\r\n### 3.4 Distributed enforcement (the crux)\r\n\r\n`[CS]` The hard part: enforce a **global** limit across **many instances** (7.1) `[CS]`:\r\n- **The problem:** if each instance counts locally, a client hitting different instances can **exceed the intended global limit** (N instances → up to N× the limit — 15.7).\r\n- **Solution: a shared counter store** — typically **Redis** (6.6 — fast, in-memory, atomic ops): all instances read/update the **same counter** per client → a **global** view.\r\n- **Atomicity (critical):** the read-modify-write of the counter must be **atomic** to avoid race conditions (two requests both reading \"99, allowed\" then both incrementing to 101 — over-limit). Use **atomic Redis operations** (INCR, or a **Lua script** for token-bucket check-and-decrement atomically) → no races (11.5 CAS-like).\r\n- **Performance:** the shared store adds a **network round-trip per request** (latency + a dependency on the hot path) → mitigate with: **atomic single-op** designs, **pipelining** (17.4), **local pre-check + async sync** (approximate — trade accuracy for speed), or **sharding** the counter store.\r\n- `[BP]` **The distributed-state tradeoff:** a **strictly-global, atomic** shared counter (accurate but adds latency + a SPOF dependency) vs **local/approximate** limits (fast but less accurate). Discuss the tradeoff; the shared-Redis atomic approach is the common answer.\r\n\r\n### 3.5 Data model\r\n\r\n`[BP]`\r\n- Per client: `{ clientId → tokens, lastRefill }` (token bucket) in Redis (6.6), with a **TTL** (expire idle clients → bound memory).\r\n- Rules config: `{ rule → limit, window, dimension }`.\r\n- `[BP]` Redis holds **soft state** (counters, TTL'd) — losing it means resetting limits (acceptable — fail-open — §3.6).\r\n\r\n### 3.6 Deep dives + bottlenecks\r\n\r\n`[BP]`\r\n- **Atomicity / race conditions** (§3.4): atomic Redis ops / Lua script — the key correctness concern.\r\n- **The counter store as a SPOF/bottleneck:** Redis is on **every request** → must be **HA** (6.6 — replication/cluster — 6.6) + **fast**; **shard** the counter store (7.3) for throughput; a slow/down Redis affects all requests → **fail-open** (allow on error — availability over strict limiting) vs **fail-closed** (deny — security over availability) — a deliberate choice (15.1/11.4). **Usually fail-open** for a rate limiter (don't break the API if the limiter is down) — unless it's a critical security control.\r\n- **Dimensions** (15.7): per-user + per-IP + per-key + per-endpoint + global — combine (15.7); different rules per dimension.\r\n- **Client behavior:** return **429 + `Retry-After`** so well-behaved clients **back off** (backpressure — 9.9); publish limits (headers: remaining/reset).\r\n- **Multiple rules:** a request may match several rules (per-user AND per-IP) → check all, deny if any exceeded.\r\n- **Accuracy vs performance** (§3.4): strict-atomic-global vs approximate-local.\r\n- `[BP]` **The two hard parts: the algorithm (token bucket) + distributed atomic state (shared Redis).** The bottleneck is the **counter store** → HA + shard + atomic ops + fail-open.\r\n\r\n---\r\n\r\n## 4. Visual Intuition\r\n\r\n```mermaid\r\nflowchart LR\r\n    REQ[\"Request (clientId)\"] --> GW[\"Rate limiter @ gateway (12.6) / sidecar\"]\r\n    GW --> REDIS{\"Atomic check-and-decrement in shared Redis (6.6): token available?\"}\r\n    REDIS -->|yes| ALLOW[\"Allow → backend\"]\r\n    REDIS -->|no| DENY[\"429 + Retry-After (back off — 9.9)\"]\r\n    REDIS -.HA + sharded (7.3); on error → fail-open (usually).-> NOTE[\"Counter store = the bottleneck/SPOF\"]\r\n    note[\"Token bucket (burst-tolerant) + atomic global counter (shared Redis) — accuracy vs latency tradeoff\"]\r\n```\r\n\r\n---\r\n\r\n## 5. Real-World Analogy\r\n\r\nThink of a **club with multiple entrances** enforcing \"each guest may enter at most 100 times an hour.\"\r\n\r\n- **Token bucket = a stamp card refilled steadily:** each guest has an allowance (bucket of stamps) refilled over time; each entry uses a stamp; out of stamps → wait. A small **burst** (a few quick entries) is fine (bucket has slack), but the **sustained rate** is capped.\r\n- **Distributed enforcement = doors sharing one guest ledger:** with **multiple doors** (instances), a sneaky guest could enter 100 times at **each** door if the doors count separately (exceeding the global 100). So all doors **check the same central ledger** (shared Redis) — a **global** count. Crucially, two doors checking \"99, allowed\" **simultaneously** must not both admit the guest (race) → the ledger update is **atomic** (one door at a time per guest — Redis atomic op/Lua).\r\n- **The ledger is the bottleneck:** every entry consults the central ledger, so it must be **fast + always available + able to handle the load** (HA + sharded Redis). And if the ledger is **temporarily down**, do you **turn everyone away** (fail-closed) or **let everyone in** (fail-open)? Usually **let them in** — a broken counter shouldn't shut the club — unless the limit is a critical security barrier.\r\n- **429 + Retry-After = \"come back in 5 minutes\":** a turned-away guest is told **when to return** so they don't keep hammering the door.\r\n\r\n---\r\n\r\n## 6. Industry Example\r\n\r\n- **Gateway/edge rate limiting + Redis counters** `[CONV]`: enforce at the gateway (12.6) with a shared atomic Redis counter (§3.2/3.4, 6.6). *(Representative.)*\r\n- **Token bucket** `[CONV]`: the common burst-tolerant algorithm (§3.3, 15.7/2.4.4). *(Representative.)*\r\n- **Lua-script atomic token bucket** `[CONV]`: atomic check-and-decrement to avoid races (§3.4). *(Representative.)*\r\n- **429 + Retry-After + rate-limit headers** `[CONV]`: standard client-backoff signaling (§3.6, 9.9). *(Representative.)*\r\n- **Fail-open on limiter failure** `[CONV]`: availability over strict limiting for a rate limiter (§3.6, 11.4). *(Representative.)*\r\n\r\n---\r\n\r\n## 7. Implementation Details\r\n\r\n- **Enforce at the gateway/edge** (12.6) as load shedding (11.4); defense-in-depth per-service (15.7).\r\n- **Token bucket** (2 values/client) default; sliding-window-counter for accuracy (§3.3, 2.4.4).\r\n- **Shared atomic counter in Redis** (6.6): atomic op / Lua script for check-and-decrement → **no races** (§3.4); TTL idle clients (§3.5).\r\n- **HA + shard the counter store** (6.6/7.3); **fail-open** on error (usually — §3.6); pipeline/local-pre-check to cut latency (17.4).\r\n- **Combine dimensions** (per-user/IP/key/endpoint/global — 15.7); check all matching rules.\r\n- **429 + Retry-After + rate-limit headers** (§3.6, 9.9); layer with CAPTCHA/lockout for security abuse (15.7).\r\n\r\n---\r\n\r\n## 8–14. (Advantages / disadvantages / mistakes / questions / pitfalls / optimizations)\r\n\r\n**Advantages:** simple, cheap, effective; dual reliability+security (15.7); global enforcement via shared atomic counter.\r\n**Disadvantages/cautions:** counter store is a hot-path SPOF/bottleneck (HA + shard); atomicity required (races); accuracy vs latency tradeoff; fail-open/closed choice.\r\n**Common mistakes:** local (per-instance) counting → exceeds global limit; non-atomic RMW → races/over-limit; per-IP-only (NAT/botnets — 15.7); no 429/Retry-After; fail-closed breaking the API.\r\n**Interview Qs:** 🟢 What algorithm + why token bucket? 🟡 How to enforce globally across instances (shared atomic Redis)? Why atomic? 🔴 The counter store as a SPOF — HA/shard/fail-open? Accuracy vs latency? ⚫ Full distributed rate-limiter design + dimensions + graceful behavior + the shared-state tradeoff.\r\n**Production pitfalls:** race-condition over-limit (non-atomic); Redis bottleneck/outage affecting all requests; NAT false positives (per-IP); latency from the counter round-trip.\r\n**Optimizations:** atomic single-op/Lua; pipelining (17.4); sharded HA Redis; local pre-check + async sync (approximate); combined dimensions; 429 + backoff.\r\n\r\n---\r\n\r\n## 15. Summary\r\n\r\nA **rate limiter** restricts requests per client per window, rejecting excess with **429**, serving as both a **reliability** (protect capacity — 11.4) and **security** (throttle abuse — 15.7) control — this design applies **15.7** (concepts) + **2.4.4** (LLD) end-to-end. Its **key signals**: it's **on every request** (must be **fast + low-overhead + HA**) and must enforce a **global** limit across a **distributed fleet** (7.1) — the crux. Enforce it **at the gateway/edge** (12.6, defense-in-depth per-service — 15.7) as early **load shedding** (11.4). The **algorithm** (15.7/2.4.4): **token bucket** (recommended — burst-tolerant, cheap: 2 values/client — token count + last-refill), or leaky bucket (steady rate), fixed window (simple, boundary bursts), or **sliding-window-counter** (accurate, cheap approximation). The **distributed-state problem** is the hard part: **local per-instance counting lets a client exceed the global limit** (N instances → up to N× — 15.7), so use a **shared counter store (Redis — 6.6)** for a **global** view — with **atomic** read-modify-write (atomic Redis ops / a **Lua script** for check-and-decrement) to prevent **race conditions** (two requests both reading \"allowed\" then both incrementing over-limit — 11.5 CAS-like). This adds a **round-trip per request** (latency + a hot-path dependency), traded off against accuracy (strict-atomic-global vs local/approximate). The **bottleneck/SPOF is the counter store**: Redis is on **every request** → must be **HA + fast + sharded** (6.6/7.3), and on failure you choose **fail-open** (allow — availability over strict limiting — usually preferred for a rate limiter, so a broken limiter doesn't break the API) vs **fail-closed** (deny — security over availability — for critical security limits) (15.1/11.4). **Deep dives:** **dimensions** (per-user/IP/key/endpoint/global — combine — 15.7), **client behavior** (**429 + `Retry-After`** + rate-limit headers so well-behaved clients back off — 9.9), **multiple rules** (check all matching, deny if any exceeded), and **accuracy vs performance**. The **two hard parts: the algorithm (token bucket) + distributed atomic state (shared Redis)** — with the counter store as the bottleneck (HA + shard + atomic ops + fail-open). A clean, high-leverage design reusing 15.7/2.4.4/6.6.\r\n\r\n---\r\n\r\n## 16. Revision Notes (flashcard-ready)\r\n\r\n- **Q:** Rate limiter's dual purpose? **A:** Reliability (protect capacity — 11.4) + security (throttle abuse — 15.7).\r\n- **Q:** Key signals? **A:** On every request (fast + HA) + must enforce a global limit across a distributed fleet.\r\n- **Q:** Recommended algorithm? **A:** Token bucket — burst-tolerant, cheap (2 values/client: tokens + last-refill).\r\n- **Q:** Distributed-state problem? **A:** Local counting → client exceeds the global limit; use a shared counter store (Redis) for a global view.\r\n- **Q:** Why atomicity? **A:** Non-atomic read-modify-write → races (two requests both \"allowed\" then over-limit); use atomic Redis op / Lua script.\r\n- **Q:** The bottleneck/SPOF? **A:** The counter store (Redis) — on every request → HA + fast + sharded.\r\n- **Q:** Fail-open vs fail-closed? **A:** Rate limiter usually fail-open (don't break the API if the limiter is down); fail-closed for critical security limits.\r\n- **Q:** Where to enforce? **A:** At the gateway/edge (12.6) as early load shedding (11.4); + per-service (defense in depth).\r\n- **Q:** Client behavior? **A:** 429 + Retry-After + rate-limit headers → back off (9.9).\r\n- **Q:** The two hard parts? **A:** Algorithm (token bucket) + distributed atomic state (shared Redis).\r\n\r\n---\r\n\r\n## 17. Further Reading + Knowledge-Graph Links\r\n\r\n**Foundations:** [15.7 Rate Limiting] · [2.4.4 LLD (rate limiter)] · [6.6 Distributed Caching (Redis)] · [11.4 Load Shedding] · [12.6 API Gateway] · [1.3.1 Framework].\r\n**External:** System-design rate-limiter treatments; Redis atomic ops / Lua. *(Representative.)*\r\n\r\n> **Knowledge-graph:** `15.7 rate limiting` + `2.4.4 LLD` + `6.6 Redis` + `11.4 shedding` → **`19.1.2 rate limiter`** (token bucket + shared atomic counter, counter-store bottleneck).\r\n",
    "html": "<h1>Lesson 19.1.2 — Design a Rate Limiter</h1>\n<blockquote>\n<p>Part 19 · Module 19.1 (Volume 1) · Difficulty: 🟡🔴 · <em>Interview design</em></p>\n<p><strong>Prerequisites:</strong> [2.4.3 Concurrency], [2.4.4 LLD (rate limiter)], [6.6 Distributed Caching (Redis)], [11.4 Load Shedding], [15.7 Rate Limiting], [1.3.1 Framework].\r\n<strong>Unlocks:</strong> [19.2.5 Distributed Lock], [Part 20 Capstone].</p>\n</blockquote>\n<hr>\n<h2>1. Learning Objectives</h2>\n<p>After this lesson you will be able to:</p>\n<ul>\n<li>Design a <strong>distributed rate limiter</strong> end-to-end (framework — 1.3.1): requirements → API → algorithm → distributed enforcement → deep dives.</li>\n<li>Choose the <strong>algorithm</strong> (token bucket / leaky bucket / fixed window / sliding window — 15.7/2.4.4) and justify it.</li>\n<li>Solve the <strong>distributed-state problem</strong>: enforcing a <strong>global</strong> limit across many instances (shared Redis — 6.6) with accuracy vs performance tradeoffs.</li>\n<li>Handle deep dives: <strong>where to enforce</strong> (gateway — 12.6), <strong>dimensions</strong> (15.7), <strong>the counter store as a dependency/SPOF</strong>, <strong>atomicity</strong> (race conditions), and <strong>graceful client behavior</strong> (429 + Retry-After).</li>\n<li>Reuse the concepts from <strong>15.7</strong> (rate limiting as reliability + security) in a concrete design.</li>\n</ul>\n<hr>\n<h2>2. Problem statement</h2>\n<p>Design a <strong>rate limiter</strong>: restrict how many requests a client may make in a time window (e.g., \"100 requests/minute per user\"), rejecting excess with <strong>HTTP 429</strong>. It's both a <strong>reliability</strong> control (protect capacity — 11.4) and a <strong>security</strong> control (throttle abuse — 15.7), and a classic interview design that hinges on the <strong>algorithm</strong> and the <strong>distributed-state</strong> problem. (This applies 15.7 — rate limiting concepts — as an end-to-end design; and 2.4.4 — the LLD.)</p>\n<hr>\n<h2>3. The design (framework — 1.3.1)</h2>\n<h3>3.1 Requirements</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Functional:</strong> limit requests per client per window; <strong>reject excess</strong> (429 + <code>Retry-After</code>); configurable rules (per-user/IP/API-key/endpoint — 15.7); ideally distributed (works across many instances).</li>\n<li><strong>Non-functional:</strong> <strong>low latency + low overhead</strong> (it's on every request's hot path → must be cheap), <strong>accurate</strong> (enforce the limit, not wildly over/under), <strong>highly available</strong> (a broken limiter shouldn't break the API — fail-open vs fail-closed — §3.6), <strong>scalable</strong>.</li>\n<li><code>[BP]</code> <strong>Key signals:</strong> it's <strong>on every request</strong> (must be fast + HA) and must enforce a <strong>global</strong> limit across a <strong>distributed</strong> fleet (§3.4 — the crux). Drive to <strong>algorithm + distributed state</strong>.</li>\n</ul>\n<h3>3.2 API / placement</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li>Typically a <strong>middleware/component at the API gateway</strong> (12.6) or a <strong>sidecar</strong> (13.6/12.7 mesh) — enforce at the <strong>edge</strong>, rejecting excess <strong>before</strong> it consumes backend resources (11.4 load shedding — cheapest — 15.7).</li>\n<li>Interface: <code>allow(clientId, rule) → allowed | denied(retryAfter)</code>.</li>\n<li><code>[BP]</code> <strong>Enforce early</strong> (gateway) + optionally per-service (defense in depth — 15.7).</li>\n</ul>\n<h3>3.3 Algorithm (15.7/2.4.4)</h3>\n<p><code>[CS]</code> The core choice (15.7 §3.2) <code>[BP]</code>:</p>\n<ul>\n<li><strong>Token bucket (recommended default):</strong> a bucket of N tokens, refilled at a rate; each request consumes one; empty → reject. <strong>Allows bursts</strong> (up to N) while capping the average → flexible, common. Needs <strong>2 values per client</strong>: token count + last-refill timestamp.</li>\n<li><strong>Leaky bucket:</strong> fixed-rate drain (smooths output); good for steady downstream protection.</li>\n<li><strong>Fixed window:</strong> count per window; simple but <strong>boundary bursts</strong> (2× at edges).</li>\n<li><strong>Sliding window (log or counter):</strong> accurate, no boundary problem, but more memory/compute (sliding-window-counter is a cheap approximation).</li>\n<li><code>[BP]</code> <strong>Recommendation: token bucket</strong> (burst-tolerant, cheap — 2 values/client) for general API rate limiting; <strong>sliding-window-counter</strong> if you need accuracy without the fixed-window boundary problem. (Detailed algorithm/object-model in 2.4.4.)</li>\n</ul>\n<h3>3.4 Distributed enforcement (the crux)</h3>\n<p><code>[CS]</code> The hard part: enforce a <strong>global</strong> limit across <strong>many instances</strong> (7.1) <code>[CS]</code>:</p>\n<ul>\n<li><strong>The problem:</strong> if each instance counts locally, a client hitting different instances can <strong>exceed the intended global limit</strong> (N instances → up to N× the limit — 15.7).</li>\n<li><strong>Solution: a shared counter store</strong> — typically <strong>Redis</strong> (6.6 — fast, in-memory, atomic ops): all instances read/update the <strong>same counter</strong> per client → a <strong>global</strong> view.</li>\n<li><strong>Atomicity (critical):</strong> the read-modify-write of the counter must be <strong>atomic</strong> to avoid race conditions (two requests both reading \"99, allowed\" then both incrementing to 101 — over-limit). Use <strong>atomic Redis operations</strong> (INCR, or a <strong>Lua script</strong> for token-bucket check-and-decrement atomically) → no races (11.5 CAS-like).</li>\n<li><strong>Performance:</strong> the shared store adds a <strong>network round-trip per request</strong> (latency + a dependency on the hot path) → mitigate with: <strong>atomic single-op</strong> designs, <strong>pipelining</strong> (17.4), <strong>local pre-check + async sync</strong> (approximate — trade accuracy for speed), or <strong>sharding</strong> the counter store.</li>\n<li><code>[BP]</code> <strong>The distributed-state tradeoff:</strong> a <strong>strictly-global, atomic</strong> shared counter (accurate but adds latency + a SPOF dependency) vs <strong>local/approximate</strong> limits (fast but less accurate). Discuss the tradeoff; the shared-Redis atomic approach is the common answer.</li>\n</ul>\n<h3>3.5 Data model</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li>Per client: <code>{ clientId → tokens, lastRefill }</code> (token bucket) in Redis (6.6), with a <strong>TTL</strong> (expire idle clients → bound memory).</li>\n<li>Rules config: <code>{ rule → limit, window, dimension }</code>.</li>\n<li><code>[BP]</code> Redis holds <strong>soft state</strong> (counters, TTL'd) — losing it means resetting limits (acceptable — fail-open — §3.6).</li>\n</ul>\n<h3>3.6 Deep dives + bottlenecks</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Atomicity / race conditions</strong> (§3.4): atomic Redis ops / Lua script — the key correctness concern.</li>\n<li><strong>The counter store as a SPOF/bottleneck:</strong> Redis is on <strong>every request</strong> → must be <strong>HA</strong> (6.6 — replication/cluster — 6.6) + <strong>fast</strong>; <strong>shard</strong> the counter store (7.3) for throughput; a slow/down Redis affects all requests → <strong>fail-open</strong> (allow on error — availability over strict limiting) vs <strong>fail-closed</strong> (deny — security over availability) — a deliberate choice (15.1/11.4). <strong>Usually fail-open</strong> for a rate limiter (don't break the API if the limiter is down) — unless it's a critical security control.</li>\n<li><strong>Dimensions</strong> (15.7): per-user + per-IP + per-key + per-endpoint + global — combine (15.7); different rules per dimension.</li>\n<li><strong>Client behavior:</strong> return <strong>429 + <code>Retry-After</code></strong> so well-behaved clients <strong>back off</strong> (backpressure — 9.9); publish limits (headers: remaining/reset).</li>\n<li><strong>Multiple rules:</strong> a request may match several rules (per-user AND per-IP) → check all, deny if any exceeded.</li>\n<li><strong>Accuracy vs performance</strong> (§3.4): strict-atomic-global vs approximate-local.</li>\n<li><code>[BP]</code> <strong>The two hard parts: the algorithm (token bucket) + distributed atomic state (shared Redis).</strong> The bottleneck is the <strong>counter store</strong> → HA + shard + atomic ops + fail-open.</li>\n</ul>\n<hr>\n<h2>4. Visual Intuition</h2>\n<pre><code class=\"language-mermaid\">flowchart LR\r\n    REQ[\"Request (clientId)\"] --> GW[\"Rate limiter @ gateway (12.6) / sidecar\"]\r\n    GW --> REDIS{\"Atomic check-and-decrement in shared Redis (6.6): token available?\"}\r\n    REDIS -->|yes| ALLOW[\"Allow → backend\"]\r\n    REDIS -->|no| DENY[\"429 + Retry-After (back off — 9.9)\"]\r\n    REDIS -.HA + sharded (7.3); on error → fail-open (usually).-> NOTE[\"Counter store = the bottleneck/SPOF\"]\r\n    note[\"Token bucket (burst-tolerant) + atomic global counter (shared Redis) — accuracy vs latency tradeoff\"]\n</code></pre>\n<hr>\n<h2>5. Real-World Analogy</h2>\n<p>Think of a <strong>club with multiple entrances</strong> enforcing \"each guest may enter at most 100 times an hour.\"</p>\n<ul>\n<li><strong>Token bucket = a stamp card refilled steadily:</strong> each guest has an allowance (bucket of stamps) refilled over time; each entry uses a stamp; out of stamps → wait. A small <strong>burst</strong> (a few quick entries) is fine (bucket has slack), but the <strong>sustained rate</strong> is capped.</li>\n<li><strong>Distributed enforcement = doors sharing one guest ledger:</strong> with <strong>multiple doors</strong> (instances), a sneaky guest could enter 100 times at <strong>each</strong> door if the doors count separately (exceeding the global 100). So all doors <strong>check the same central ledger</strong> (shared Redis) — a <strong>global</strong> count. Crucially, two doors checking \"99, allowed\" <strong>simultaneously</strong> must not both admit the guest (race) → the ledger update is <strong>atomic</strong> (one door at a time per guest — Redis atomic op/Lua).</li>\n<li><strong>The ledger is the bottleneck:</strong> every entry consults the central ledger, so it must be <strong>fast + always available + able to handle the load</strong> (HA + sharded Redis). And if the ledger is <strong>temporarily down</strong>, do you <strong>turn everyone away</strong> (fail-closed) or <strong>let everyone in</strong> (fail-open)? Usually <strong>let them in</strong> — a broken counter shouldn't shut the club — unless the limit is a critical security barrier.</li>\n<li><strong>429 + Retry-After = \"come back in 5 minutes\":</strong> a turned-away guest is told <strong>when to return</strong> so they don't keep hammering the door.</li>\n</ul>\n<hr>\n<h2>6. Industry Example</h2>\n<ul>\n<li><strong>Gateway/edge rate limiting + Redis counters</strong> <code>[CONV]</code>: enforce at the gateway (12.6) with a shared atomic Redis counter (§3.2/3.4, 6.6). <em>(Representative.)</em></li>\n<li><strong>Token bucket</strong> <code>[CONV]</code>: the common burst-tolerant algorithm (§3.3, 15.7/2.4.4). <em>(Representative.)</em></li>\n<li><strong>Lua-script atomic token bucket</strong> <code>[CONV]</code>: atomic check-and-decrement to avoid races (§3.4). <em>(Representative.)</em></li>\n<li><strong>429 + Retry-After + rate-limit headers</strong> <code>[CONV]</code>: standard client-backoff signaling (§3.6, 9.9). <em>(Representative.)</em></li>\n<li><strong>Fail-open on limiter failure</strong> <code>[CONV]</code>: availability over strict limiting for a rate limiter (§3.6, 11.4). <em>(Representative.)</em></li>\n</ul>\n<hr>\n<h2>7. Implementation Details</h2>\n<ul>\n<li><strong>Enforce at the gateway/edge</strong> (12.6) as load shedding (11.4); defense-in-depth per-service (15.7).</li>\n<li><strong>Token bucket</strong> (2 values/client) default; sliding-window-counter for accuracy (§3.3, 2.4.4).</li>\n<li><strong>Shared atomic counter in Redis</strong> (6.6): atomic op / Lua script for check-and-decrement → <strong>no races</strong> (§3.4); TTL idle clients (§3.5).</li>\n<li><strong>HA + shard the counter store</strong> (6.6/7.3); <strong>fail-open</strong> on error (usually — §3.6); pipeline/local-pre-check to cut latency (17.4).</li>\n<li><strong>Combine dimensions</strong> (per-user/IP/key/endpoint/global — 15.7); check all matching rules.</li>\n<li><strong>429 + Retry-After + rate-limit headers</strong> (§3.6, 9.9); layer with CAPTCHA/lockout for security abuse (15.7).</li>\n</ul>\n<hr>\n<h2>8–14. (Advantages / disadvantages / mistakes / questions / pitfalls / optimizations)</h2>\n<p><strong>Advantages:</strong> simple, cheap, effective; dual reliability+security (15.7); global enforcement via shared atomic counter.\r\n<strong>Disadvantages/cautions:</strong> counter store is a hot-path SPOF/bottleneck (HA + shard); atomicity required (races); accuracy vs latency tradeoff; fail-open/closed choice.\r\n<strong>Common mistakes:</strong> local (per-instance) counting → exceeds global limit; non-atomic RMW → races/over-limit; per-IP-only (NAT/botnets — 15.7); no 429/Retry-After; fail-closed breaking the API.\r\n<strong>Interview Qs:</strong> 🟢 What algorithm + why token bucket? 🟡 How to enforce globally across instances (shared atomic Redis)? Why atomic? 🔴 The counter store as a SPOF — HA/shard/fail-open? Accuracy vs latency? ⚫ Full distributed rate-limiter design + dimensions + graceful behavior + the shared-state tradeoff.\r\n<strong>Production pitfalls:</strong> race-condition over-limit (non-atomic); Redis bottleneck/outage affecting all requests; NAT false positives (per-IP); latency from the counter round-trip.\r\n<strong>Optimizations:</strong> atomic single-op/Lua; pipelining (17.4); sharded HA Redis; local pre-check + async sync (approximate); combined dimensions; 429 + backoff.</p>\n<hr>\n<h2>15. Summary</h2>\n<p>A <strong>rate limiter</strong> restricts requests per client per window, rejecting excess with <strong>429</strong>, serving as both a <strong>reliability</strong> (protect capacity — 11.4) and <strong>security</strong> (throttle abuse — 15.7) control — this design applies <strong>15.7</strong> (concepts) + <strong>2.4.4</strong> (LLD) end-to-end. Its <strong>key signals</strong>: it's <strong>on every request</strong> (must be <strong>fast + low-overhead + HA</strong>) and must enforce a <strong>global</strong> limit across a <strong>distributed fleet</strong> (7.1) — the crux. Enforce it <strong>at the gateway/edge</strong> (12.6, defense-in-depth per-service — 15.7) as early <strong>load shedding</strong> (11.4). The <strong>algorithm</strong> (15.7/2.4.4): <strong>token bucket</strong> (recommended — burst-tolerant, cheap: 2 values/client — token count + last-refill), or leaky bucket (steady rate), fixed window (simple, boundary bursts), or <strong>sliding-window-counter</strong> (accurate, cheap approximation). The <strong>distributed-state problem</strong> is the hard part: <strong>local per-instance counting lets a client exceed the global limit</strong> (N instances → up to N× — 15.7), so use a <strong>shared counter store (Redis — 6.6)</strong> for a <strong>global</strong> view — with <strong>atomic</strong> read-modify-write (atomic Redis ops / a <strong>Lua script</strong> for check-and-decrement) to prevent <strong>race conditions</strong> (two requests both reading \"allowed\" then both incrementing over-limit — 11.5 CAS-like). This adds a <strong>round-trip per request</strong> (latency + a hot-path dependency), traded off against accuracy (strict-atomic-global vs local/approximate). The <strong>bottleneck/SPOF is the counter store</strong>: Redis is on <strong>every request</strong> → must be <strong>HA + fast + sharded</strong> (6.6/7.3), and on failure you choose <strong>fail-open</strong> (allow — availability over strict limiting — usually preferred for a rate limiter, so a broken limiter doesn't break the API) vs <strong>fail-closed</strong> (deny — security over availability — for critical security limits) (15.1/11.4). <strong>Deep dives:</strong> <strong>dimensions</strong> (per-user/IP/key/endpoint/global — combine — 15.7), <strong>client behavior</strong> (<strong>429 + <code>Retry-After</code></strong> + rate-limit headers so well-behaved clients back off — 9.9), <strong>multiple rules</strong> (check all matching, deny if any exceeded), and <strong>accuracy vs performance</strong>. The <strong>two hard parts: the algorithm (token bucket) + distributed atomic state (shared Redis)</strong> — with the counter store as the bottleneck (HA + shard + atomic ops + fail-open). A clean, high-leverage design reusing 15.7/2.4.4/6.6.</p>\n<hr>\n<h2>16. Revision Notes (flashcard-ready)</h2>\n<ul>\n<li><strong>Q:</strong> Rate limiter's dual purpose? <strong>A:</strong> Reliability (protect capacity — 11.4) + security (throttle abuse — 15.7).</li>\n<li><strong>Q:</strong> Key signals? <strong>A:</strong> On every request (fast + HA) + must enforce a global limit across a distributed fleet.</li>\n<li><strong>Q:</strong> Recommended algorithm? <strong>A:</strong> Token bucket — burst-tolerant, cheap (2 values/client: tokens + last-refill).</li>\n<li><strong>Q:</strong> Distributed-state problem? <strong>A:</strong> Local counting → client exceeds the global limit; use a shared counter store (Redis) for a global view.</li>\n<li><strong>Q:</strong> Why atomicity? <strong>A:</strong> Non-atomic read-modify-write → races (two requests both \"allowed\" then over-limit); use atomic Redis op / Lua script.</li>\n<li><strong>Q:</strong> The bottleneck/SPOF? <strong>A:</strong> The counter store (Redis) — on every request → HA + fast + sharded.</li>\n<li><strong>Q:</strong> Fail-open vs fail-closed? <strong>A:</strong> Rate limiter usually fail-open (don't break the API if the limiter is down); fail-closed for critical security limits.</li>\n<li><strong>Q:</strong> Where to enforce? <strong>A:</strong> At the gateway/edge (12.6) as early load shedding (11.4); + per-service (defense in depth).</li>\n<li><strong>Q:</strong> Client behavior? <strong>A:</strong> 429 + Retry-After + rate-limit headers → back off (9.9).</li>\n<li><strong>Q:</strong> The two hard parts? <strong>A:</strong> Algorithm (token bucket) + distributed atomic state (shared Redis).</li>\n</ul>\n<hr>\n<h2>17. Further Reading + Knowledge-Graph Links</h2>\n<p><strong>Foundations:</strong> [15.7 Rate Limiting] · [2.4.4 LLD (rate limiter)] · [6.6 Distributed Caching (Redis)] · [11.4 Load Shedding] · [12.6 API Gateway] · [1.3.1 Framework].\r\n<strong>External:</strong> System-design rate-limiter treatments; Redis atomic ops / Lua. <em>(Representative.)</em></p>\n<blockquote>\n<p><strong>Knowledge-graph:</strong> <code>15.7 rate limiting</code> + <code>2.4.4 LLD</code> + <code>6.6 Redis</code> + <code>11.4 shedding</code> → <strong><code>19.1.2 rate limiter</code></strong> (token bucket + shared atomic counter, counter-store bottleneck).</p>\n</blockquote>"
  },
  "_id": "lessons/part-19-interview-designs/19.1.2-design-rate-limiter.md",
  "_raw": {
    "sourceFilePath": "lessons/part-19-interview-designs/19.1.2-design-rate-limiter.md",
    "sourceFileName": "19.1.2-design-rate-limiter.md",
    "sourceFileDir": "lessons/part-19-interview-designs",
    "contentType": "markdown",
    "flattenedPath": "lessons/part-19-interview-designs/19.1.2-design-rate-limiter"
  },
  "type": "Lesson",
  "title": "Design a Rate Limiter",
  "description": "",
  "slug": "part-19-interview-designs/19.1.2-design-rate-limiter",
  "url": "/learn/part-19-interview-designs/19.1.2-design-rate-limiter",
  "readingTime": 10,
  "flashcards": [
    {
      "q": "Rate limiter's dual purpose?",
      "a": "Reliability (protect capacity — 11.4) + security (throttle abuse — 15.7)."
    },
    {
      "q": "Key signals?",
      "a": "On every request (fast + HA) + must enforce a global limit across a distributed fleet."
    },
    {
      "q": "Recommended algorithm?",
      "a": "Token bucket — burst-tolerant, cheap (2 values/client: tokens + last-refill)."
    },
    {
      "q": "Distributed-state problem?",
      "a": "Local counting → client exceeds the global limit; use a shared counter store (Redis) for a global view."
    },
    {
      "q": "Why atomicity?",
      "a": "Non-atomic read-modify-write → races (two requests both \"allowed\" then over-limit); use atomic Redis op / Lua script."
    },
    {
      "q": "The bottleneck/SPOF?",
      "a": "The counter store (Redis) — on every request → HA + fast + sharded."
    },
    {
      "q": "Fail-open vs fail-closed?",
      "a": "Rate limiter usually fail-open (don't break the API if the limiter is down); fail-closed for critical security limits."
    },
    {
      "q": "Where to enforce?",
      "a": "At the gateway/edge (12.6) as early load shedding (11.4); + per-service (defense in depth)."
    },
    {
      "q": "Client behavior?",
      "a": "429 + Retry-After + rate-limit headers → back off (9.9)."
    },
    {
      "q": "The two hard parts?",
      "a": "Algorithm (token bucket) + distributed atomic state (shared Redis)."
    }
  ],
  "metadata": {
    "partNum": 0,
    "lessonId": "",
    "difficulty": "foundational",
    "prerequisites": [
      "[2.4.3 Concurrency]",
      "[2.4.4 LLD (rate limiter)]",
      "[6.6 Distributed Caching (Redis)]",
      "[11.4 Load Shedding]",
      "[15.7 Rate Limiting]",
      "[1.3.1 Framework]."
    ],
    "unlocks": [
      "[19.2.5 Distributed Lock]",
      "[Part 20 Capstone]."
    ]
  }
}