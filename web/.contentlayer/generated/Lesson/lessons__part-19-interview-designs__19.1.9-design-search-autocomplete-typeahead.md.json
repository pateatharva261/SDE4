{
  "body": {
    "raw": "# Lesson 19.1.9 — Design Search Autocomplete / Typeahead\r\n\r\n> Part 19 · Module 19.1 (Volume 1) · Difficulty: 🟡🔴 · *Interview design*\r\n>\r\n> **Prerequisites:** [Part 6 Caching], [17.2 Tail Latency], [9.6 Stream Processing], [18.7 Search], [7.3 Sharding], [1.3.1 Framework].\r\n> **Unlocks:** [19.2.7 Recommendations], [Part 20 Capstone].\r\n\r\n---\r\n\r\n## 1. Learning Objectives\r\n\r\nAfter this lesson you will be able to:\r\n\r\n- Design **autocomplete/typeahead**: given a **prefix**, return the **top-K most relevant completions** with **very low latency** (every keystroke).\r\n- Use the right data structure — a **trie (prefix tree)** with **precomputed top-K per node**, and/or an inverted-index/prefix approach (18.7).\r\n- Explain the **precompute + cache** strategy — autocomplete is **latency-critical** (17.2) → precompute top-K, cache aggressively (Part 6).\r\n- Handle **ranking** (popularity/frequency, personalization), **freshness** (update suggestions from query logs — 9.6), and **scale** (sharding — 7.3).\r\n- Recognize it as a **precomputed-top-K + low-latency-cache** design.\r\n\r\n---\r\n\r\n## 2. Problem statement\r\n\r\nDesign **search autocomplete** (typeahead): as a user **types a prefix**, instantly suggest the **top-K most likely completions** (e.g., \"sys\" → \"system design,\" \"systematic,\" ...). It fires on **every keystroke** → **extremely latency-sensitive** (17.2 — must feel instant, single-digit-to-low-ms), **read-heavy**, and **top-K-ranked** — a **precompute + cache** design distinct from full search (18.7).\r\n\r\n---\r\n\r\n## 3. The design (framework — 1.3.1)\r\n\r\n### 3.1 Requirements\r\n\r\n`[BP]`\r\n- **Functional:** given a **prefix**, return **top-K completions** (ranked by relevance/popularity); update suggestions as popularity changes.\r\n- **Non-functional:** **very low latency** (fires per keystroke → must be **~ms** — 17.2), **read-heavy** (queries ≫ updates), **scalable**, **fresh-ish** (trending queries surface — eventual is fine — 10.5).\r\n- `[BP]` **Key signal: latency-critical + read-heavy + top-K per prefix** → **precompute top-K + cache aggressively** (Part 6). This is *not* full-text search (18.7) — it's prefix → precomputed top-K.\r\n\r\n### 3.2 The data structure — trie with precomputed top-K\r\n\r\n`[CS]` The core structure `[CS]`:\r\n- **Trie (prefix tree):** a tree where each **node = a prefix**, edges = characters → walking the prefix leads to the node; the subtree under it holds all completions of that prefix. Natural for prefix queries.\r\n- **Precomputed top-K at each node:** store the **top-K completions (by rank) directly at each prefix node** → a query is: **walk to the prefix node → return its precomputed top-K** — **O(prefix length)** + a constant → **extremely fast** (no ranking at query time).\r\n- **Alternative:** precomputed **prefix → top-K** in a **KV store / cache** (Redis — 6.6) — same idea (precompute, look up), simpler to shard.\r\n- `[BP]` **The key: precompute the top-K per prefix** (offline) so the query is a **lookup, not a computation** — because it's latency-critical (17.2). Ranking happens **offline** (§3.4), not per keystroke.\r\n\r\n### 3.3 Precompute + cache (latency-critical)\r\n\r\n`[CS]` Autocomplete is **latency-critical** → **precompute + cache** `[BP]`:\r\n- **Precompute offline:** periodically (batch — 9.7) compute the **top-K per prefix** from query logs (§3.4) → build the trie / prefix→top-K map.\r\n- **Cache aggressively** (Part 6/6.6): the prefix→top-K results are **highly cacheable** (read-heavy, popular prefixes) → serve from **in-memory cache** (Redis/CDN — 6.6/18.4) → **~ms latency**. Popular prefixes → near-100% cache hit.\r\n- **Edge/CDN** (18.4): can even serve suggestions from the edge for global low latency.\r\n- `[BP]` **The strategy: precompute top-K offline + serve from cache** → queries are cache lookups (fast). Do **zero ranking at query time** — it's all precomputed. Latency (17.2) drives everything.\r\n\r\n### 3.4 Ranking + freshness (from query logs)\r\n\r\n`[CS]` How are the top-K chosen + kept fresh? `[BP]`:\r\n- **Ranking signals:** **query frequency/popularity** (how often each completion is searched), recency/trending, personalization, context.\r\n- **From query logs** (9.6): user queries are **logged as events** (18.1) → **stream/batch processing** (9.6/9.7) **aggregates query frequencies** → periodically **recompute top-K per prefix** → update the trie/cache. **Trending** queries surface via recent-window aggregation (9.6 windowing).\r\n- `[BP]` **Freshness = periodic recompute from query-log aggregation** (9.6/18.1). Autocomplete **learns from what people search** — a data pipeline (like recommendations — 18.5) feeding the precomputed top-K. Updates are **async/eventual** (10.5 — fine).\r\n\r\n### 3.5 Scale (sharding)\r\n\r\n`[BP]`\r\n- **Sharding** (7.3): the trie / prefix→top-K map is huge → **shard by prefix** (e.g., first characters) across nodes; queries route to the right shard.\r\n- **Read scaling** (Part 6/7.5): cache + replicas handle the read-heavy load.\r\n- `[BP]` Shard by prefix (7.3); cache-heavy reads (Part 6).\r\n\r\n### 3.6 Deep dives + bottlenecks\r\n\r\n`[BP]`\r\n- **Latency** (17.2): the whole design is about **ms latency** → precompute + cache + no query-time ranking; watch tail latency (17.2).\r\n- **Trie memory:** a full trie of all queries is large → **prune** (only keep prefixes/completions above a popularity threshold), compress (radix tree), or use prefix→top-K in a KV store.\r\n- **Freshness vs cost** (§3.4): recompute frequency (trending needs frequent updates); real-time updates via streaming (9.6) for trending, batch for the bulk.\r\n- **Personalization:** blend global top-K with user-specific (adds complexity — often global + light personalization).\r\n- **Correctness:** handle typos (fuzzy — like search 18.7), multiple words.\r\n- **Bottleneck:** none extreme — precompute + cache dissolves the latency concern; trie memory + freshness pipeline are the interesting parts.\r\n- `[BP]` **The lesson:** autocomplete = **precomputed top-K per prefix (trie / prefix→top-K map) + aggressive caching** (Part 6) for **ms latency** (17.2), with **ranking + freshness from query-log aggregation** (9.6/18.1) and **sharding by prefix** (7.3). Precompute + cache is the whole game; it's distinct from full search (18.7).\r\n\r\n---\r\n\r\n## 4. Visual Intuition\r\n\r\n```mermaid\r\nflowchart LR\r\n    subgraph QUERY[\"Query path (latency-critical — 17.2)\"]\r\n      TYPE[\"User types prefix 'sys'\"] --> CACHE{\"Cache (Part 6): prefix→top-K?\"}\r\n      CACHE -->|hit (popular prefix)| TOPK[\"Return precomputed top-K (~ms) — NO ranking at query time\"]\r\n      CACHE -->|miss| TRIE[\"Trie: walk to node 'sys' → precomputed top-K → cache\"]\r\n    end\r\n    subgraph OFFLINE[\"Offline (freshness)\"]\r\n      LOGS[\"Query logs → events (18.1)\"] --> AGG[\"Aggregate frequencies (stream/batch — 9.6/9.7)\"]\r\n      AGG --> BUILD[\"Recompute top-K per prefix → update trie/cache\"]\r\n    end\r\n    note[\"Precompute top-K per prefix + cache → query is a fast lookup; ranking/freshness offline from query logs\"]\r\n```\r\n\r\n---\r\n\r\n## 5. Real-World Analogy\r\n\r\nThink of a **shop assistant who finishes your sentence instantly** because they've memorized the most common requests for every opening phrase.\r\n\r\n- **Precomputed top-K = memorized popular completions per prefix:** rather than **thinking hard** about what you might want each time you start speaking (ranking at query time — too slow), the assistant has **already memorized**, for every possible opening (\"I'd like a sys...\"), the **top few most common things people ask for** (precomputed top-K per prefix). When you start typing, they **instantly recall** the memorized list — no thinking required (a lookup, not a computation).\r\n- **Cache = the most-asked phrases on the tip of the tongue:** the **most common openings** are so frequently used they're **instantly at hand** (cache) — the assistant almost never has to look anything up.\r\n- **Learning from logs = updating the memorized lists from what people actually ask:** periodically, the assistant **reviews what everyone's been asking** (query logs) and **updates the memorized completions** — so **trending requests** rise to the top (freshness from aggregation), and stale ones fade.\r\n- **Why it must be instant:** the assistant finishes your sentence **as you type each letter** — so there's **no time to think**; everything must be **pre-memorized and instantly recalled** (latency-critical → precompute + cache).\r\n\r\n---\r\n\r\n## 6. Industry Example\r\n\r\n- **Search autocomplete (Google/e-commerce)** `[CONV]`: precomputed top-K per prefix + aggressive caching for ms-latency suggestions (§3.2/3.3). *(Representative.)*\r\n- **Trie with precomputed top-K per node** `[CONV]`: the classic autocomplete data structure (§3.2). *(Representative.)*\r\n- **Ranking from query-log aggregation** `[CONV]`: frequency/trending from logged queries via stream/batch (§3.4, 9.6/18.1). *(Representative.)*\r\n- **Prefix-sharded + cached** `[CONV]`: shard by prefix + cache reads (§3.5, 7.3/Part 6). *(Representative.)*\r\n- **Distinct from full search** `[CONV]`: autocomplete = prefix→precomputed-top-K, not full-text ranking at query time (18.7). *(Representative.)*\r\n\r\n---\r\n\r\n## 7. Implementation Details\r\n\r\n- **Data structure** (§3.2): **trie with precomputed top-K per node** (or prefix→top-K in a KV store — 6.6); query = walk-to-prefix → return top-K (fast).\r\n- **Precompute + cache** (§3.3, Part 6): precompute top-K offline; **cache aggressively** (Redis/CDN — 6.6/18.4) for ms latency (17.2); **no ranking at query time**.\r\n- **Ranking + freshness** (§3.4, 9.6/18.1): aggregate query frequencies from logs (stream/batch); recompute top-K periodically; trending via recent-window aggregation (9.6); async/eventual (10.5).\r\n- **Scale** (§3.5): shard by prefix (7.3); cache + replicas for reads (7.5/Part 6).\r\n- **Memory** (§3.6): prune low-popularity prefixes/completions; compress (radix tree).\r\n- **Correctness:** fuzzy/typo handling (like 18.7); multi-word.\r\n\r\n---\r\n\r\n## 8–14. (Advantages / disadvantages / mistakes / questions / pitfalls / optimizations)\r\n\r\n**Advantages:** ms latency (precompute + cache); read-heavy → cache-friendly; ranking done offline; trending via log aggregation; scalable (prefix sharding).\r\n**Disadvantages/cautions:** trie memory (prune/compress); freshness lag (eventual); personalization complexity; precompute pipeline.\r\n**Common mistakes:** ranking at query time (too slow — should precompute); no caching (read-heavy latency-critical → must cache); full-text search engine for autocomplete (overkill/slow — use precomputed top-K); unbounded trie memory (prune); ignoring latency (the whole point — 17.2).\r\n**Interview Qs:** 🟢 What data structure + why precompute? 🟡 Precompute + cache for ms latency; ranking/freshness from logs? 🔴 Trie memory + sharding + trending updates? ⚫ Full design (trie/prefix→top-K + cache + log-aggregation freshness + sharding).\r\n**Production pitfalls:** slow suggestions (ranking at query time / no cache); trie memory blowout (no pruning); stale suggestions (infrequent recompute); tail latency spikes (17.2).\r\n**Optimizations:** precomputed top-K per prefix + aggressive cache/CDN; offline ranking from query-log aggregation (9.6); prefix sharding (7.3); prune/compress trie; trending via streaming windows (9.6).\r\n\r\n---\r\n\r\n## 15. Summary\r\n\r\n**Autocomplete/typeahead** returns the **top-K most relevant completions** for a **prefix**, firing on **every keystroke** → it is **extremely latency-critical** (17.2 — must feel instant, low-ms), **read-heavy**, and **top-K-ranked** — a **precompute + cache** design distinct from full-text search (18.7). The **key signal** — latency-critical + read-heavy + top-K per prefix — dictates **precompute the top-K per prefix + cache aggressively** so the query is a **lookup, not a computation**. The **data structure** is a **trie (prefix tree)** with the **top-K completions precomputed and stored at each prefix node** → a query **walks to the prefix node and returns its precomputed top-K** (**O(prefix length)** + constant — extremely fast, **no ranking at query time**); an equivalent is a **prefix→top-K map in a KV store/cache** (6.6 — simpler to shard). **Precompute + cache** is the strategy: **precompute top-K offline** (batch — 9.7) and **serve from an in-memory cache** (Redis/CDN — 6.6/18.4 — highly cacheable, read-heavy, popular prefixes → near-100% hit) for **~ms latency** — doing **zero ranking at query time** (latency — 17.2 — drives everything). **Ranking + freshness** come from **query logs**: user queries are **logged as events** (18.1) → **stream/batch aggregation** (9.6/9.7) computes **query frequencies** → **periodically recompute top-K per prefix** and update the trie/cache, with **trending** surfacing via recent-window aggregation (9.6) — so autocomplete **learns from what people search** (a data pipeline like recommendations — 18.5), with **async/eventual** updates (10.5 — fine). **Scale** via **sharding by prefix** (7.3) + cache/replicas for reads (Part 6/7.5). **Deep dives**: **latency** (the whole design — precompute + cache + no query-time ranking + watch tail — 17.2), **trie memory** (**prune** low-popularity prefixes/completions, compress via radix tree, or prefix→top-K in KV), **freshness vs cost** (recompute frequency; streaming for trending), **personalization** (blend global + user-specific), and **correctness** (typos/fuzzy — like 18.7, multi-word). There's **no extreme bottleneck** — precompute + cache dissolves the latency concern; **trie memory + the freshness pipeline** are the interesting parts. Autocomplete = **precomputed top-K per prefix (trie / prefix→top-K) + aggressive caching (Part 6) for ms latency (17.2) + ranking/freshness from query-log aggregation (9.6/18.1) + prefix sharding (7.3)** — precompute + cache is the whole game.\r\n\r\n---\r\n\r\n## 16. Revision Notes (flashcard-ready)\r\n\r\n- **Q:** Key signal? **A:** Latency-critical (per keystroke, ms — 17.2) + read-heavy + top-K per prefix → precompute + cache.\r\n- **Q:** Data structure? **A:** Trie (prefix tree) with precomputed top-K at each node (or prefix→top-K in a KV store/cache).\r\n- **Q:** Query path? **A:** Walk to the prefix node → return precomputed top-K → O(prefix length), NO ranking at query time.\r\n- **Q:** The core strategy? **A:** Precompute top-K offline + serve from cache → query is a fast lookup; zero query-time ranking (latency — 17.2).\r\n- **Q:** Ranking/freshness? **A:** Aggregate query frequencies from logged queries (9.6/18.1) → periodically recompute top-K; trending via recent windows.\r\n- **Q:** Why precompute (not rank live)? **A:** Fires per keystroke → must be ms → no time to rank → precompute + cache.\r\n- **Q:** Scale? **A:** Shard by prefix (7.3) + cache/replicas for reads (Part 6).\r\n- **Q:** Trie memory? **A:** Prune low-popularity prefixes/completions; compress (radix tree); or prefix→top-K in KV.\r\n- **Q:** vs full search (18.7)? **A:** Autocomplete = prefix→precomputed-top-K (lookup); full search = inverted index + query-time relevance ranking.\r\n- **Q:** Consistency of updates? **A:** Async/eventual (10.5) — periodic recompute; fine.\r\n\r\n---\r\n\r\n## 17. Further Reading + Knowledge-Graph Links\r\n\r\n**Foundations:** [Part 6 Caching] · [17.2 Tail Latency] · [9.6 Stream Processing] · [18.7 Search] · [7.3 Sharding] · [18.1 Log] · [18.4 CDN].\r\n**External:** Autocomplete/typeahead design treatments; trie data structure. *(Representative.)*\r\n\r\n> **Knowledge-graph:** trie/precomputed-top-K + `Part 6 caching` + `17.2 latency` + `9.6 log aggregation` + `7.3 prefix sharding` → **`19.1.9 autocomplete`** — precompute + cache (distinct from `18.7 full search`).\r\n",
    "html": "<h1>Lesson 19.1.9 — Design Search Autocomplete / Typeahead</h1>\n<blockquote>\n<p>Part 19 · Module 19.1 (Volume 1) · Difficulty: 🟡🔴 · <em>Interview design</em></p>\n<p><strong>Prerequisites:</strong> [Part 6 Caching], [17.2 Tail Latency], [9.6 Stream Processing], [18.7 Search], [7.3 Sharding], [1.3.1 Framework].\r\n<strong>Unlocks:</strong> [19.2.7 Recommendations], [Part 20 Capstone].</p>\n</blockquote>\n<hr>\n<h2>1. Learning Objectives</h2>\n<p>After this lesson you will be able to:</p>\n<ul>\n<li>Design <strong>autocomplete/typeahead</strong>: given a <strong>prefix</strong>, return the <strong>top-K most relevant completions</strong> with <strong>very low latency</strong> (every keystroke).</li>\n<li>Use the right data structure — a <strong>trie (prefix tree)</strong> with <strong>precomputed top-K per node</strong>, and/or an inverted-index/prefix approach (18.7).</li>\n<li>Explain the <strong>precompute + cache</strong> strategy — autocomplete is <strong>latency-critical</strong> (17.2) → precompute top-K, cache aggressively (Part 6).</li>\n<li>Handle <strong>ranking</strong> (popularity/frequency, personalization), <strong>freshness</strong> (update suggestions from query logs — 9.6), and <strong>scale</strong> (sharding — 7.3).</li>\n<li>Recognize it as a <strong>precomputed-top-K + low-latency-cache</strong> design.</li>\n</ul>\n<hr>\n<h2>2. Problem statement</h2>\n<p>Design <strong>search autocomplete</strong> (typeahead): as a user <strong>types a prefix</strong>, instantly suggest the <strong>top-K most likely completions</strong> (e.g., \"sys\" → \"system design,\" \"systematic,\" ...). It fires on <strong>every keystroke</strong> → <strong>extremely latency-sensitive</strong> (17.2 — must feel instant, single-digit-to-low-ms), <strong>read-heavy</strong>, and <strong>top-K-ranked</strong> — a <strong>precompute + cache</strong> design distinct from full search (18.7).</p>\n<hr>\n<h2>3. The design (framework — 1.3.1)</h2>\n<h3>3.1 Requirements</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Functional:</strong> given a <strong>prefix</strong>, return <strong>top-K completions</strong> (ranked by relevance/popularity); update suggestions as popularity changes.</li>\n<li><strong>Non-functional:</strong> <strong>very low latency</strong> (fires per keystroke → must be <strong>~ms</strong> — 17.2), <strong>read-heavy</strong> (queries ≫ updates), <strong>scalable</strong>, <strong>fresh-ish</strong> (trending queries surface — eventual is fine — 10.5).</li>\n<li><code>[BP]</code> <strong>Key signal: latency-critical + read-heavy + top-K per prefix</strong> → <strong>precompute top-K + cache aggressively</strong> (Part 6). This is <em>not</em> full-text search (18.7) — it's prefix → precomputed top-K.</li>\n</ul>\n<h3>3.2 The data structure — trie with precomputed top-K</h3>\n<p><code>[CS]</code> The core structure <code>[CS]</code>:</p>\n<ul>\n<li><strong>Trie (prefix tree):</strong> a tree where each <strong>node = a prefix</strong>, edges = characters → walking the prefix leads to the node; the subtree under it holds all completions of that prefix. Natural for prefix queries.</li>\n<li><strong>Precomputed top-K at each node:</strong> store the <strong>top-K completions (by rank) directly at each prefix node</strong> → a query is: <strong>walk to the prefix node → return its precomputed top-K</strong> — <strong>O(prefix length)</strong> + a constant → <strong>extremely fast</strong> (no ranking at query time).</li>\n<li><strong>Alternative:</strong> precomputed <strong>prefix → top-K</strong> in a <strong>KV store / cache</strong> (Redis — 6.6) — same idea (precompute, look up), simpler to shard.</li>\n<li><code>[BP]</code> <strong>The key: precompute the top-K per prefix</strong> (offline) so the query is a <strong>lookup, not a computation</strong> — because it's latency-critical (17.2). Ranking happens <strong>offline</strong> (§3.4), not per keystroke.</li>\n</ul>\n<h3>3.3 Precompute + cache (latency-critical)</h3>\n<p><code>[CS]</code> Autocomplete is <strong>latency-critical</strong> → <strong>precompute + cache</strong> <code>[BP]</code>:</p>\n<ul>\n<li><strong>Precompute offline:</strong> periodically (batch — 9.7) compute the <strong>top-K per prefix</strong> from query logs (§3.4) → build the trie / prefix→top-K map.</li>\n<li><strong>Cache aggressively</strong> (Part 6/6.6): the prefix→top-K results are <strong>highly cacheable</strong> (read-heavy, popular prefixes) → serve from <strong>in-memory cache</strong> (Redis/CDN — 6.6/18.4) → <strong>~ms latency</strong>. Popular prefixes → near-100% cache hit.</li>\n<li><strong>Edge/CDN</strong> (18.4): can even serve suggestions from the edge for global low latency.</li>\n<li><code>[BP]</code> <strong>The strategy: precompute top-K offline + serve from cache</strong> → queries are cache lookups (fast). Do <strong>zero ranking at query time</strong> — it's all precomputed. Latency (17.2) drives everything.</li>\n</ul>\n<h3>3.4 Ranking + freshness (from query logs)</h3>\n<p><code>[CS]</code> How are the top-K chosen + kept fresh? <code>[BP]</code>:</p>\n<ul>\n<li><strong>Ranking signals:</strong> <strong>query frequency/popularity</strong> (how often each completion is searched), recency/trending, personalization, context.</li>\n<li><strong>From query logs</strong> (9.6): user queries are <strong>logged as events</strong> (18.1) → <strong>stream/batch processing</strong> (9.6/9.7) <strong>aggregates query frequencies</strong> → periodically <strong>recompute top-K per prefix</strong> → update the trie/cache. <strong>Trending</strong> queries surface via recent-window aggregation (9.6 windowing).</li>\n<li><code>[BP]</code> <strong>Freshness = periodic recompute from query-log aggregation</strong> (9.6/18.1). Autocomplete <strong>learns from what people search</strong> — a data pipeline (like recommendations — 18.5) feeding the precomputed top-K. Updates are <strong>async/eventual</strong> (10.5 — fine).</li>\n</ul>\n<h3>3.5 Scale (sharding)</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Sharding</strong> (7.3): the trie / prefix→top-K map is huge → <strong>shard by prefix</strong> (e.g., first characters) across nodes; queries route to the right shard.</li>\n<li><strong>Read scaling</strong> (Part 6/7.5): cache + replicas handle the read-heavy load.</li>\n<li><code>[BP]</code> Shard by prefix (7.3); cache-heavy reads (Part 6).</li>\n</ul>\n<h3>3.6 Deep dives + bottlenecks</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Latency</strong> (17.2): the whole design is about <strong>ms latency</strong> → precompute + cache + no query-time ranking; watch tail latency (17.2).</li>\n<li><strong>Trie memory:</strong> a full trie of all queries is large → <strong>prune</strong> (only keep prefixes/completions above a popularity threshold), compress (radix tree), or use prefix→top-K in a KV store.</li>\n<li><strong>Freshness vs cost</strong> (§3.4): recompute frequency (trending needs frequent updates); real-time updates via streaming (9.6) for trending, batch for the bulk.</li>\n<li><strong>Personalization:</strong> blend global top-K with user-specific (adds complexity — often global + light personalization).</li>\n<li><strong>Correctness:</strong> handle typos (fuzzy — like search 18.7), multiple words.</li>\n<li><strong>Bottleneck:</strong> none extreme — precompute + cache dissolves the latency concern; trie memory + freshness pipeline are the interesting parts.</li>\n<li><code>[BP]</code> <strong>The lesson:</strong> autocomplete = <strong>precomputed top-K per prefix (trie / prefix→top-K map) + aggressive caching</strong> (Part 6) for <strong>ms latency</strong> (17.2), with <strong>ranking + freshness from query-log aggregation</strong> (9.6/18.1) and <strong>sharding by prefix</strong> (7.3). Precompute + cache is the whole game; it's distinct from full search (18.7).</li>\n</ul>\n<hr>\n<h2>4. Visual Intuition</h2>\n<pre><code class=\"language-mermaid\">flowchart LR\r\n    subgraph QUERY[\"Query path (latency-critical — 17.2)\"]\r\n      TYPE[\"User types prefix 'sys'\"] --> CACHE{\"Cache (Part 6): prefix→top-K?\"}\r\n      CACHE -->|hit (popular prefix)| TOPK[\"Return precomputed top-K (~ms) — NO ranking at query time\"]\r\n      CACHE -->|miss| TRIE[\"Trie: walk to node 'sys' → precomputed top-K → cache\"]\r\n    end\r\n    subgraph OFFLINE[\"Offline (freshness)\"]\r\n      LOGS[\"Query logs → events (18.1)\"] --> AGG[\"Aggregate frequencies (stream/batch — 9.6/9.7)\"]\r\n      AGG --> BUILD[\"Recompute top-K per prefix → update trie/cache\"]\r\n    end\r\n    note[\"Precompute top-K per prefix + cache → query is a fast lookup; ranking/freshness offline from query logs\"]\n</code></pre>\n<hr>\n<h2>5. Real-World Analogy</h2>\n<p>Think of a <strong>shop assistant who finishes your sentence instantly</strong> because they've memorized the most common requests for every opening phrase.</p>\n<ul>\n<li><strong>Precomputed top-K = memorized popular completions per prefix:</strong> rather than <strong>thinking hard</strong> about what you might want each time you start speaking (ranking at query time — too slow), the assistant has <strong>already memorized</strong>, for every possible opening (\"I'd like a sys...\"), the <strong>top few most common things people ask for</strong> (precomputed top-K per prefix). When you start typing, they <strong>instantly recall</strong> the memorized list — no thinking required (a lookup, not a computation).</li>\n<li><strong>Cache = the most-asked phrases on the tip of the tongue:</strong> the <strong>most common openings</strong> are so frequently used they're <strong>instantly at hand</strong> (cache) — the assistant almost never has to look anything up.</li>\n<li><strong>Learning from logs = updating the memorized lists from what people actually ask:</strong> periodically, the assistant <strong>reviews what everyone's been asking</strong> (query logs) and <strong>updates the memorized completions</strong> — so <strong>trending requests</strong> rise to the top (freshness from aggregation), and stale ones fade.</li>\n<li><strong>Why it must be instant:</strong> the assistant finishes your sentence <strong>as you type each letter</strong> — so there's <strong>no time to think</strong>; everything must be <strong>pre-memorized and instantly recalled</strong> (latency-critical → precompute + cache).</li>\n</ul>\n<hr>\n<h2>6. Industry Example</h2>\n<ul>\n<li><strong>Search autocomplete (Google/e-commerce)</strong> <code>[CONV]</code>: precomputed top-K per prefix + aggressive caching for ms-latency suggestions (§3.2/3.3). <em>(Representative.)</em></li>\n<li><strong>Trie with precomputed top-K per node</strong> <code>[CONV]</code>: the classic autocomplete data structure (§3.2). <em>(Representative.)</em></li>\n<li><strong>Ranking from query-log aggregation</strong> <code>[CONV]</code>: frequency/trending from logged queries via stream/batch (§3.4, 9.6/18.1). <em>(Representative.)</em></li>\n<li><strong>Prefix-sharded + cached</strong> <code>[CONV]</code>: shard by prefix + cache reads (§3.5, 7.3/Part 6). <em>(Representative.)</em></li>\n<li><strong>Distinct from full search</strong> <code>[CONV]</code>: autocomplete = prefix→precomputed-top-K, not full-text ranking at query time (18.7). <em>(Representative.)</em></li>\n</ul>\n<hr>\n<h2>7. Implementation Details</h2>\n<ul>\n<li><strong>Data structure</strong> (§3.2): <strong>trie with precomputed top-K per node</strong> (or prefix→top-K in a KV store — 6.6); query = walk-to-prefix → return top-K (fast).</li>\n<li><strong>Precompute + cache</strong> (§3.3, Part 6): precompute top-K offline; <strong>cache aggressively</strong> (Redis/CDN — 6.6/18.4) for ms latency (17.2); <strong>no ranking at query time</strong>.</li>\n<li><strong>Ranking + freshness</strong> (§3.4, 9.6/18.1): aggregate query frequencies from logs (stream/batch); recompute top-K periodically; trending via recent-window aggregation (9.6); async/eventual (10.5).</li>\n<li><strong>Scale</strong> (§3.5): shard by prefix (7.3); cache + replicas for reads (7.5/Part 6).</li>\n<li><strong>Memory</strong> (§3.6): prune low-popularity prefixes/completions; compress (radix tree).</li>\n<li><strong>Correctness:</strong> fuzzy/typo handling (like 18.7); multi-word.</li>\n</ul>\n<hr>\n<h2>8–14. (Advantages / disadvantages / mistakes / questions / pitfalls / optimizations)</h2>\n<p><strong>Advantages:</strong> ms latency (precompute + cache); read-heavy → cache-friendly; ranking done offline; trending via log aggregation; scalable (prefix sharding).\r\n<strong>Disadvantages/cautions:</strong> trie memory (prune/compress); freshness lag (eventual); personalization complexity; precompute pipeline.\r\n<strong>Common mistakes:</strong> ranking at query time (too slow — should precompute); no caching (read-heavy latency-critical → must cache); full-text search engine for autocomplete (overkill/slow — use precomputed top-K); unbounded trie memory (prune); ignoring latency (the whole point — 17.2).\r\n<strong>Interview Qs:</strong> 🟢 What data structure + why precompute? 🟡 Precompute + cache for ms latency; ranking/freshness from logs? 🔴 Trie memory + sharding + trending updates? ⚫ Full design (trie/prefix→top-K + cache + log-aggregation freshness + sharding).\r\n<strong>Production pitfalls:</strong> slow suggestions (ranking at query time / no cache); trie memory blowout (no pruning); stale suggestions (infrequent recompute); tail latency spikes (17.2).\r\n<strong>Optimizations:</strong> precomputed top-K per prefix + aggressive cache/CDN; offline ranking from query-log aggregation (9.6); prefix sharding (7.3); prune/compress trie; trending via streaming windows (9.6).</p>\n<hr>\n<h2>15. Summary</h2>\n<p><strong>Autocomplete/typeahead</strong> returns the <strong>top-K most relevant completions</strong> for a <strong>prefix</strong>, firing on <strong>every keystroke</strong> → it is <strong>extremely latency-critical</strong> (17.2 — must feel instant, low-ms), <strong>read-heavy</strong>, and <strong>top-K-ranked</strong> — a <strong>precompute + cache</strong> design distinct from full-text search (18.7). The <strong>key signal</strong> — latency-critical + read-heavy + top-K per prefix — dictates <strong>precompute the top-K per prefix + cache aggressively</strong> so the query is a <strong>lookup, not a computation</strong>. The <strong>data structure</strong> is a <strong>trie (prefix tree)</strong> with the <strong>top-K completions precomputed and stored at each prefix node</strong> → a query <strong>walks to the prefix node and returns its precomputed top-K</strong> (<strong>O(prefix length)</strong> + constant — extremely fast, <strong>no ranking at query time</strong>); an equivalent is a <strong>prefix→top-K map in a KV store/cache</strong> (6.6 — simpler to shard). <strong>Precompute + cache</strong> is the strategy: <strong>precompute top-K offline</strong> (batch — 9.7) and <strong>serve from an in-memory cache</strong> (Redis/CDN — 6.6/18.4 — highly cacheable, read-heavy, popular prefixes → near-100% hit) for <strong>~ms latency</strong> — doing <strong>zero ranking at query time</strong> (latency — 17.2 — drives everything). <strong>Ranking + freshness</strong> come from <strong>query logs</strong>: user queries are <strong>logged as events</strong> (18.1) → <strong>stream/batch aggregation</strong> (9.6/9.7) computes <strong>query frequencies</strong> → <strong>periodically recompute top-K per prefix</strong> and update the trie/cache, with <strong>trending</strong> surfacing via recent-window aggregation (9.6) — so autocomplete <strong>learns from what people search</strong> (a data pipeline like recommendations — 18.5), with <strong>async/eventual</strong> updates (10.5 — fine). <strong>Scale</strong> via <strong>sharding by prefix</strong> (7.3) + cache/replicas for reads (Part 6/7.5). <strong>Deep dives</strong>: <strong>latency</strong> (the whole design — precompute + cache + no query-time ranking + watch tail — 17.2), <strong>trie memory</strong> (<strong>prune</strong> low-popularity prefixes/completions, compress via radix tree, or prefix→top-K in KV), <strong>freshness vs cost</strong> (recompute frequency; streaming for trending), <strong>personalization</strong> (blend global + user-specific), and <strong>correctness</strong> (typos/fuzzy — like 18.7, multi-word). There's <strong>no extreme bottleneck</strong> — precompute + cache dissolves the latency concern; <strong>trie memory + the freshness pipeline</strong> are the interesting parts. Autocomplete = <strong>precomputed top-K per prefix (trie / prefix→top-K) + aggressive caching (Part 6) for ms latency (17.2) + ranking/freshness from query-log aggregation (9.6/18.1) + prefix sharding (7.3)</strong> — precompute + cache is the whole game.</p>\n<hr>\n<h2>16. Revision Notes (flashcard-ready)</h2>\n<ul>\n<li><strong>Q:</strong> Key signal? <strong>A:</strong> Latency-critical (per keystroke, ms — 17.2) + read-heavy + top-K per prefix → precompute + cache.</li>\n<li><strong>Q:</strong> Data structure? <strong>A:</strong> Trie (prefix tree) with precomputed top-K at each node (or prefix→top-K in a KV store/cache).</li>\n<li><strong>Q:</strong> Query path? <strong>A:</strong> Walk to the prefix node → return precomputed top-K → O(prefix length), NO ranking at query time.</li>\n<li><strong>Q:</strong> The core strategy? <strong>A:</strong> Precompute top-K offline + serve from cache → query is a fast lookup; zero query-time ranking (latency — 17.2).</li>\n<li><strong>Q:</strong> Ranking/freshness? <strong>A:</strong> Aggregate query frequencies from logged queries (9.6/18.1) → periodically recompute top-K; trending via recent windows.</li>\n<li><strong>Q:</strong> Why precompute (not rank live)? <strong>A:</strong> Fires per keystroke → must be ms → no time to rank → precompute + cache.</li>\n<li><strong>Q:</strong> Scale? <strong>A:</strong> Shard by prefix (7.3) + cache/replicas for reads (Part 6).</li>\n<li><strong>Q:</strong> Trie memory? <strong>A:</strong> Prune low-popularity prefixes/completions; compress (radix tree); or prefix→top-K in KV.</li>\n<li><strong>Q:</strong> vs full search (18.7)? <strong>A:</strong> Autocomplete = prefix→precomputed-top-K (lookup); full search = inverted index + query-time relevance ranking.</li>\n<li><strong>Q:</strong> Consistency of updates? <strong>A:</strong> Async/eventual (10.5) — periodic recompute; fine.</li>\n</ul>\n<hr>\n<h2>17. Further Reading + Knowledge-Graph Links</h2>\n<p><strong>Foundations:</strong> [Part 6 Caching] · [17.2 Tail Latency] · [9.6 Stream Processing] · [18.7 Search] · [7.3 Sharding] · [18.1 Log] · [18.4 CDN].\r\n<strong>External:</strong> Autocomplete/typeahead design treatments; trie data structure. <em>(Representative.)</em></p>\n<blockquote>\n<p><strong>Knowledge-graph:</strong> trie/precomputed-top-K + <code>Part 6 caching</code> + <code>17.2 latency</code> + <code>9.6 log aggregation</code> + <code>7.3 prefix sharding</code> → <strong><code>19.1.9 autocomplete</code></strong> — precompute + cache (distinct from <code>18.7 full search</code>).</p>\n</blockquote>"
  },
  "_id": "lessons/part-19-interview-designs/19.1.9-design-search-autocomplete-typeahead.md",
  "_raw": {
    "sourceFilePath": "lessons/part-19-interview-designs/19.1.9-design-search-autocomplete-typeahead.md",
    "sourceFileName": "19.1.9-design-search-autocomplete-typeahead.md",
    "sourceFileDir": "lessons/part-19-interview-designs",
    "contentType": "markdown",
    "flattenedPath": "lessons/part-19-interview-designs/19.1.9-design-search-autocomplete-typeahead"
  },
  "type": "Lesson",
  "title": "Design Search Autocomplete / Typeahead",
  "description": "",
  "slug": "part-19-interview-designs/19.1.9-design-search-autocomplete-typeahead",
  "url": "/learn/part-19-interview-designs/19.1.9-design-search-autocomplete-typeahead",
  "readingTime": 10,
  "flashcards": [
    {
      "q": "Key signal?",
      "a": "Latency-critical (per keystroke, ms — 17.2) + read-heavy + top-K per prefix → precompute + cache."
    },
    {
      "q": "Data structure?",
      "a": "Trie (prefix tree) with precomputed top-K at each node (or prefix→top-K in a KV store/cache)."
    },
    {
      "q": "Query path?",
      "a": "Walk to the prefix node → return precomputed top-K → O(prefix length), NO ranking at query time."
    },
    {
      "q": "The core strategy?",
      "a": "Precompute top-K offline + serve from cache → query is a fast lookup; zero query-time ranking (latency — 17.2)."
    },
    {
      "q": "Ranking/freshness?",
      "a": "Aggregate query frequencies from logged queries (9.6/18.1) → periodically recompute top-K; trending via recent windows."
    },
    {
      "q": "Why precompute (not rank live)?",
      "a": "Fires per keystroke → must be ms → no time to rank → precompute + cache."
    },
    {
      "q": "Scale?",
      "a": "Shard by prefix (7.3) + cache/replicas for reads (Part 6)."
    },
    {
      "q": "Trie memory?",
      "a": "Prune low-popularity prefixes/completions; compress (radix tree); or prefix→top-K in KV."
    },
    {
      "q": "vs full search (18.7)?",
      "a": "Autocomplete = prefix→precomputed-top-K (lookup); full search = inverted index + query-time relevance ranking."
    },
    {
      "q": "Consistency of updates?",
      "a": "Async/eventual (10.5) — periodic recompute; fine."
    }
  ],
  "metadata": {
    "partNum": 0,
    "lessonId": "",
    "difficulty": "foundational",
    "prerequisites": [
      "[Part 6 Caching]",
      "[17.2 Tail Latency]",
      "[9.6 Stream Processing]",
      "[18.7 Search]",
      "[7.3 Sharding]",
      "[1.3.1 Framework]."
    ],
    "unlocks": [
      "[19.2.7 Recommendations]",
      "[Part 20 Capstone]."
    ]
  }
}