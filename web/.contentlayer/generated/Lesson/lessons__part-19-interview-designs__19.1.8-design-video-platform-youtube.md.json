{
  "body": {
    "raw": "# Lesson 19.1.8 — Design a Video Platform (YouTube)\r\n\r\n> Part 19 · Module 19.1 (Volume 1) · Difficulty: 🔴 · *Interview design*\r\n>\r\n> **Prerequisites:** [4.1.3 Object Storage], [9.1 Messaging], [18.4 CDN], [18.5 Streaming], [Part 6 Caching], [1.3.1 Framework].\r\n> **Unlocks:** [19.2.7 Recommendations], [Part 20 Capstone].\r\n\r\n---\r\n\r\n## 1. Learning Objectives\r\n\r\nAfter this lesson you will be able to:\r\n\r\n- Design a **video platform** (YouTube): **upload + transcoding pipeline**, **storage + CDN delivery** (18.4/18.5), **metadata + view counts**, and **recommendations**.\r\n- Explain the **transcoding pipeline** (async — 9.1: encode to multiple resolutions/bitrates for ABR — 18.5).\r\n- Apply **CDN + adaptive bitrate** for delivery (18.5/18.4) and **separate video (CDN) from metadata (services)** (18.5).\r\n- Handle **view counts at scale** (high-write → async aggregation), and metadata/search (18.7).\r\n- Recognize it as a composition of the **streaming architecture** (18.5) + upload/transcoding.\r\n\r\n---\r\n\r\n## 2. Problem statement\r\n\r\nDesign a **video platform** (YouTube-style): users **upload videos**, which are **processed** and made available for **streaming** to viewers worldwide, with **metadata** (title/description), **search** (18.7), **view counts / likes**, and **recommendations** (18.5). It composes the **streaming architecture** (18.5) with an **upload + transcoding pipeline** — the write side that 18.5 (delivery-focused) didn't detail.\r\n\r\n---\r\n\r\n## 3. The design (framework — 1.3.1)\r\n\r\n### 3.1 Requirements\r\n\r\n`[BP]`\r\n- **Functional:** upload video; **transcode** (multiple resolutions/formats); **stream** (adaptive bitrate — 18.5); metadata (title/desc/tags); **search** (18.7); **view counts + likes**; **recommendations** (18.5).\r\n- **Non-functional:** **massive storage + bandwidth** (video is huge), **low-latency global streaming** (CDN — 18.4), **read-heavy** (views ≫ uploads), **highly available**, **scalable upload/transcoding**.\r\n- `[BP]` **Key signals:** **video = huge + bandwidth-heavy → CDN + object storage** (18.4/4.1.3); **uploads need async transcoding** (9.1); **read-heavy streaming** (18.5). → compose 18.5 (delivery) + a transcoding pipeline.\r\n\r\n### 3.2 The upload + transcoding pipeline (the write side)\r\n\r\n`[CS]` Uploading a video triggers an **async processing pipeline** (9.1) `[CS]`:\r\n1. **Upload:** client uploads the raw video (chunked/resumable — like 19.1.7) to **object storage** (4.1.3).\r\n2. **Enqueue a transcoding job** (9.1): the upload triggers a job → don't block the user (async — the video is \"processing\").\r\n3. **Transcoding (the heavy step):** transcode the raw video into **multiple resolutions + bitrates** (240p…4K) and **formats/segments** for **adaptive bitrate streaming** (18.5 — HLS/DASH: segmented, multi-bitrate) → CPU-intensive, **parallelized** across a fleet of workers (17.3 — CPU-bound → parallelism), often **per-segment** (split → transcode segments in parallel → assemble).\r\n4. **Store outputs** (4.1.3) + **update metadata** (video ready, available renditions).\r\n5. **Distribute to CDN** (18.4/18.5): **pre-position** popular content at the edge; content served from the CDN, not the origin.\r\n- `[BP]` **The upload/transcoding pipeline is async + parallel** (9.1/17.3): upload to object storage → enqueue → transcode to multi-bitrate segments (CPU-heavy, parallel) → store + CDN. This is the write side 18.5 (which focused on delivery) implies.\r\n\r\n### 3.3 Delivery — CDN + adaptive bitrate (18.5/18.4)\r\n\r\n`[BP]` (See 18.5 for full detail):\r\n- Video **streamed from the CDN edge** (18.4 — pre-positioned near viewers) via **adaptive bitrate** (18.5 — player picks bitrate for its bandwidth, segment by segment).\r\n- **Separate video (CDN data plane) from metadata (services)** (18.5) — bytes bypass the app.\r\n- `[BP]` Delivery = **18.5's video subsystem** (CDN + ABR + pre-positioning).\r\n\r\n### 3.4 Metadata + view counts\r\n\r\n`[BP]`\r\n- **Metadata:** video info (title/desc/tags/renditions/uploader) in a **DB** (5.x) — read-heavy → cache (Part 6); **search** via an inverted index (18.7).\r\n- **View counts (high-write):** every view increments a counter → **millions of increments** → **don't do a synchronous DB write per view** (like 19.1.1 analytics) → **emit view events** (async — 9.1/18.1) → **aggregate** (batch/stream — 9.6/9.7) → periodically update the displayed count (eventually consistent — 10.5). Same for likes.\r\n- `[BP]` **View counts are a high-write aggregation** → async events + aggregation (18.1/9.6), not per-view DB writes. Metadata is read-heavy → cached (Part 6).\r\n\r\n### 3.5 Recommendations + search\r\n\r\n`[BP]`\r\n- **Recommendations** (18.5): watch/interaction events → log → batch/stream + ML → cached recs (18.5 §3.5).\r\n- **Search** (18.7): metadata + captions indexed in a search engine (inverted index — 18.7).\r\n- `[BP]` Reuse 18.5 (recs) + 18.7 (search).\r\n\r\n### 3.6 Deep dives + bottlenecks\r\n\r\n`[BP]`\r\n- **Transcoding scale** (§3.2, 17.3): CPU-bound → large parallel worker fleet; per-segment parallelism; autoscale (13.5) for upload spikes.\r\n- **Storage/bandwidth cost** (17.6): video is huge → **CDN offload** (18.4) + tiered storage (16.2/4.1.3 — hot on fast, cold on cheap); this is a major **cost** driver (17.6).\r\n- **View-count accuracy vs cost** (§3.4): async aggregation (eventually consistent — 10.5); approximate for popular videos.\r\n- **Global delivery** (18.4/13.8): CDN + multi-region.\r\n- **Live streaming** (extension): lower-latency pipeline (segment + distribute in near-real-time).\r\n- **Bottleneck:** transcoding (parallel fleet) + bandwidth/storage cost (CDN + tiered storage). Delivery scales via CDN (18.4); metadata via cache + sharding.\r\n- `[BP]` **The lesson:** a video platform = **18.5's streaming architecture (CDN + ABR delivery, recs) + an async parallel transcoding pipeline (9.1/17.3) + object storage (4.1.3) + async view-count aggregation (18.1) + search (18.7)**. The write side (transcoding) + delivery (CDN/ABR) + cost are the interesting parts.\r\n\r\n---\r\n\r\n## 4. Visual Intuition\r\n\r\n```mermaid\r\nflowchart TB\r\n    UP[\"Upload raw video (chunked/resumable)\"] --> OBJ[(\"Object storage — 4.1.3\")]\r\n    OBJ --> JOB[\"Enqueue transcoding job (9.1) — async\"]\r\n    JOB --> TRANS[\"Transcode → multi-bitrate segments (240p…4K, ABR — 18.5). CPU-bound → parallel fleet (17.3), per-segment\"]\r\n    TRANS --> OUT[(\"Store renditions (4.1.3) + update metadata (ready)\")]\r\n    OUT --> CDN[\"Distribute to CDN (18.4/18.5): pre-position near viewers\"]\r\n    VIEWER[\"Viewer streams (ABR) from CDN edge (bytes bypass app — 18.5)\"]\r\n    VIEWER -.view event (async — 18.1/9.1).-> AGG[\"Aggregate view counts (batch/stream — 9.6)\"]\r\n    note[\"Async parallel transcoding (write) + CDN/ABR delivery (read) + async view-count aggregation\"]\r\n```\r\n\r\n---\r\n\r\n## 5. Real-World Analogy\r\n\r\nThink of a **TV production + broadcast operation**: producing content once, then broadcasting it to millions.\r\n\r\n- **Upload + transcoding = producing broadcast-ready versions:** a creator submits a **raw master tape** (upload to object storage). The studio then **produces multiple versions** — HD, SD, mobile-quality (transcoding to multi-bitrate) — a **slow, compute-heavy job** done by a **large team working in parallel** (parallel transcoding fleet), often by **splitting the tape into reels and processing them simultaneously** (per-segment parallelism). This happens **after** the creator submits (async — they don't wait); the video shows \"processing.\"\r\n- **Delivery = broadcasting from local stations:** the finished versions are **shipped to thousands of local broadcast stations** (CDN — pre-positioned) so viewers watch from a **nearby station**, and each viewer's device **picks the quality matching their connection** (adaptive bitrate). The **actual video signal doesn't route through headquarters** (video/CDN separate from the control services).\r\n- **View counts = tallying viewership without stopping the broadcast:** counting **millions of views** isn't done by **updating a master ledger on every single view** (that would bury the accountants) — instead, stations **report viewership periodically** and it's **tallied up** (async aggregation), showing an **approximate, slightly-delayed count**.\r\n- **Cost = the dominant concern:** producing and broadcasting **video is enormously expensive** (storage + bandwidth), so you **keep popular content at local stations** (CDN offload) and **archive rarely-watched content to cheap deep storage** (tiered storage).\r\n\r\n---\r\n\r\n## 6. Industry Example\r\n\r\n- **YouTube-style platform** `[CONV]`: async transcoding pipeline + CDN/ABR delivery + metadata/search + recommendations (§3.2/3.3/3.5). *(Representative.)*\r\n- **Async parallel transcoding** `[CONV]`: per-segment parallel transcoding to multi-bitrate renditions (§3.2, 9.1/17.3). *(Representative.)*\r\n- **CDN + adaptive bitrate delivery** `[CONV]`: pre-positioned edge delivery, ABR (§3.3, 18.4/18.5). *(Representative.)*\r\n- **Async view-count aggregation** `[CONV]`: view events → aggregation, eventually-consistent counts (§3.4, 18.1/9.6). *(Representative.)*\r\n- **Object storage + tiered storage for cost** `[CONV]`: huge video storage, tiered by popularity (§3.6, 4.1.3/16.2/17.6). *(Representative.)*\r\n\r\n---\r\n\r\n## 7. Implementation Details\r\n\r\n- **Upload → object storage** (4.1.3, chunked/resumable — like 19.1.7) → **enqueue transcoding job** (9.1, async).\r\n- **Transcode** (§3.2, 17.3): multi-bitrate segments for ABR (18.5); **CPU-bound → parallel worker fleet** (per-segment); autoscale (13.5) for upload spikes.\r\n- **Delivery** (§3.3, 18.4/18.5): CDN + pre-positioning + ABR; **separate video (CDN) from metadata (services)**.\r\n- **Metadata** (5.x) — read-heavy → cache (Part 6); **search** via inverted index (18.7).\r\n- **View counts/likes** (§3.4): **async view events** (18.1/9.1) → **aggregate** (9.6) → eventually-consistent counts (10.5); don't write per view.\r\n- **Recommendations** (18.5): events → log → batch/stream + ML → cached recs.\r\n- **Cost** (17.6): CDN offload + tiered storage (16.2/4.1.3) — video is the cost driver.\r\n\r\n---\r\n\r\n## 8–14. (Advantages / disadvantages / mistakes / questions / pitfalls / optimizations)\r\n\r\n**Advantages:** scalable global delivery (CDN + ABR); async parallel transcoding; read-heavy metadata cached; view counts scale via async aggregation; cost-managed (CDN offload + tiered storage).\r\n**Disadvantages/cautions:** transcoding is compute-heavy (cost — 17.6); video storage/bandwidth cost dominates (17.6); eventually-consistent counts; complex pipeline.\r\n**Common mistakes:** synchronous transcoding (blocking upload); streaming video through app services (should be CDN — 18.5); per-view DB writes (should be async aggregation); no ABR (buffering); ignoring cost (huge bandwidth/storage bills — 17.6).\r\n**Interview Qs:** 🟢 Why async transcoding + CDN delivery? 🟡 The transcoding pipeline (parallel, per-segment, multi-bitrate)? View counts at scale? 🔴 CDN/ABR delivery + cost management + separate video/metadata? ⚫ Full design = 18.5 streaming + transcoding pipeline + view aggregation + search/recs.\r\n**Production pitfalls:** transcoding backlog under upload spikes (autoscale — 13.5); bandwidth/storage cost blowout (CDN + tiered storage — 17.6); buffering (no ABR); per-view write overload; video served from origin (not CDN).\r\n**Optimizations:** async parallel per-segment transcoding; CDN + pre-positioning + ABR (18.5); async view-count aggregation (18.1); tiered storage + CDN offload (cost — 17.6); metadata cache (Part 6); autoscaling transcoding (13.5).\r\n\r\n---\r\n\r\n## 15. Summary\r\n\r\nA **video platform** (YouTube) lets users **upload videos** that are **processed and streamed** to viewers worldwide, with metadata, search (18.7), view counts, and recommendations (18.5) — it **composes the streaming architecture** (18.5) with an **upload + transcoding pipeline** (the write side 18.5's delivery-focus omitted). **Key signals:** **video is huge + bandwidth-heavy → CDN + object storage** (18.4/4.1.3); **uploads need async processing** (9.1); **streaming is read-heavy** (18.5). The **upload + transcoding pipeline** (async — 9.1): the client uploads the raw video (chunked/resumable — like 19.1.7) to **object storage** (4.1.3), which **enqueues a transcoding job** (async — the user doesn't wait; the video is \"processing\"); **transcoding** (the heavy, CPU-bound step — 17.3) produces **multiple resolutions + bitrates** and **segmented formats for adaptive bitrate streaming** (18.5 — HLS/DASH), **parallelized** across a worker fleet (17.3 — often **per-segment**: split → transcode in parallel → assemble); outputs are **stored** (4.1.3) + metadata updated (ready) + **distributed to the CDN** (18.4/18.5 — pre-positioned near viewers). **Delivery** is **18.5's video subsystem**: video **streamed from the CDN edge** via **adaptive bitrate** (player picks bitrate for its bandwidth), with **video (CDN data plane) separate from metadata (services)** (bytes bypass the app). **Metadata** (title/desc/renditions) is read-heavy → **cached** (Part 6) + **searchable** via an inverted index (18.7). **View counts** are a **high-write aggregation** — millions of increments → **not a synchronous DB write per view** (like 19.1.1) → **emit view events** (async — 18.1/9.1) → **aggregate** (batch/stream — 9.6) → periodically update the displayed (eventually-consistent — 10.5) count. **Recommendations** reuse 18.5's ML pipeline. **Deep dives**: **transcoding scale** (parallel fleet + autoscaling for spikes — 13.5), **storage/bandwidth cost** (the dominant driver — 17.6 → **CDN offload** + **tiered storage** — hot on fast, cold/rarely-watched on cheap — 16.2/4.1.3), **view-count accuracy vs cost** (async/approximate — 10.5), and **global delivery** (CDN + multi-region — 13.8). The **bottleneck** is **transcoding** (parallel fleet) + **bandwidth/storage cost** (CDN + tiered storage); delivery scales via CDN, metadata via cache + sharding. A video platform = **18.5's streaming architecture (CDN + ABR delivery + recs) + an async parallel transcoding pipeline (9.1/17.3) + object storage (4.1.3) + async view-count aggregation (18.1) + search (18.7)** — with transcoding (write), CDN/ABR (delivery), and cost as the interesting parts.\r\n\r\n---\r\n\r\n## 16. Revision Notes (flashcard-ready)\r\n\r\n- **Q:** This design = ? **A:** 18.5 streaming architecture (delivery/recs) + an upload/transcoding pipeline (the write side).\r\n- **Q:** Transcoding pipeline? **A:** Upload→object storage→enqueue job (async — 9.1)→transcode to multi-bitrate segments (CPU-bound, parallel fleet, per-segment — 17.3)→store + CDN.\r\n- **Q:** Delivery? **A:** CDN edge (pre-positioned — 18.4) + adaptive bitrate (18.5); video separate from metadata (bytes bypass app).\r\n- **Q:** View counts at scale? **A:** Async view events (18.1/9.1) → aggregate (9.6) → eventually-consistent count; NOT per-view DB writes.\r\n- **Q:** Video storage? **A:** Object storage (4.1.3); tiered (hot/cold by popularity — 16.2) for cost (17.6).\r\n- **Q:** Why is video CPU-bound transcoding parallelized? **A:** CPU-bound → parallelism (17.3), per-segment across a worker fleet; autoscale for spikes (13.5).\r\n- **Q:** Metadata + search? **A:** DB + cache (read-heavy — Part 6); search via inverted index (18.7).\r\n- **Q:** Recommendations? **A:** Reuse 18.5's ML pipeline (events → log → batch/stream + ML → cached recs).\r\n- **Q:** The dominant concern? **A:** Cost (17.6) — video storage + bandwidth → CDN offload + tiered storage.\r\n- **Q:** Bottleneck? **A:** Transcoding (parallel fleet) + bandwidth/storage cost; delivery scales via CDN.\r\n\r\n---\r\n\r\n## 17. Further Reading + Knowledge-Graph Links\r\n\r\n**Foundations:** [18.5 Streaming] · [18.4 CDN] · [4.1.3 Object Storage] · [9.1 Messaging] · [17.3 Parallelism] · [18.7 Search] · [18.1 Log] · [17.6 Cost].\r\n**External:** YouTube-style video-platform design treatments. *(Representative.)*\r\n\r\n> **Knowledge-graph:** `18.5 streaming` + async transcoding (`9.1/17.3`) + `4.1.3 object storage` + `18.4 CDN/ABR` + `18.1 view aggregation` + `18.7 search` → **`19.1.8 video platform (YouTube)`**.\r\n",
    "html": "<h1>Lesson 19.1.8 — Design a Video Platform (YouTube)</h1>\n<blockquote>\n<p>Part 19 · Module 19.1 (Volume 1) · Difficulty: 🔴 · <em>Interview design</em></p>\n<p><strong>Prerequisites:</strong> [4.1.3 Object Storage], [9.1 Messaging], [18.4 CDN], [18.5 Streaming], [Part 6 Caching], [1.3.1 Framework].\r\n<strong>Unlocks:</strong> [19.2.7 Recommendations], [Part 20 Capstone].</p>\n</blockquote>\n<hr>\n<h2>1. Learning Objectives</h2>\n<p>After this lesson you will be able to:</p>\n<ul>\n<li>Design a <strong>video platform</strong> (YouTube): <strong>upload + transcoding pipeline</strong>, <strong>storage + CDN delivery</strong> (18.4/18.5), <strong>metadata + view counts</strong>, and <strong>recommendations</strong>.</li>\n<li>Explain the <strong>transcoding pipeline</strong> (async — 9.1: encode to multiple resolutions/bitrates for ABR — 18.5).</li>\n<li>Apply <strong>CDN + adaptive bitrate</strong> for delivery (18.5/18.4) and <strong>separate video (CDN) from metadata (services)</strong> (18.5).</li>\n<li>Handle <strong>view counts at scale</strong> (high-write → async aggregation), and metadata/search (18.7).</li>\n<li>Recognize it as a composition of the <strong>streaming architecture</strong> (18.5) + upload/transcoding.</li>\n</ul>\n<hr>\n<h2>2. Problem statement</h2>\n<p>Design a <strong>video platform</strong> (YouTube-style): users <strong>upload videos</strong>, which are <strong>processed</strong> and made available for <strong>streaming</strong> to viewers worldwide, with <strong>metadata</strong> (title/description), <strong>search</strong> (18.7), <strong>view counts / likes</strong>, and <strong>recommendations</strong> (18.5). It composes the <strong>streaming architecture</strong> (18.5) with an <strong>upload + transcoding pipeline</strong> — the write side that 18.5 (delivery-focused) didn't detail.</p>\n<hr>\n<h2>3. The design (framework — 1.3.1)</h2>\n<h3>3.1 Requirements</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Functional:</strong> upload video; <strong>transcode</strong> (multiple resolutions/formats); <strong>stream</strong> (adaptive bitrate — 18.5); metadata (title/desc/tags); <strong>search</strong> (18.7); <strong>view counts + likes</strong>; <strong>recommendations</strong> (18.5).</li>\n<li><strong>Non-functional:</strong> <strong>massive storage + bandwidth</strong> (video is huge), <strong>low-latency global streaming</strong> (CDN — 18.4), <strong>read-heavy</strong> (views ≫ uploads), <strong>highly available</strong>, <strong>scalable upload/transcoding</strong>.</li>\n<li><code>[BP]</code> <strong>Key signals:</strong> <strong>video = huge + bandwidth-heavy → CDN + object storage</strong> (18.4/4.1.3); <strong>uploads need async transcoding</strong> (9.1); <strong>read-heavy streaming</strong> (18.5). → compose 18.5 (delivery) + a transcoding pipeline.</li>\n</ul>\n<h3>3.2 The upload + transcoding pipeline (the write side)</h3>\n<p><code>[CS]</code> Uploading a video triggers an <strong>async processing pipeline</strong> (9.1) <code>[CS]</code>:</p>\n<ol>\n<li><strong>Upload:</strong> client uploads the raw video (chunked/resumable — like 19.1.7) to <strong>object storage</strong> (4.1.3).</li>\n<li><strong>Enqueue a transcoding job</strong> (9.1): the upload triggers a job → don't block the user (async — the video is \"processing\").</li>\n<li><strong>Transcoding (the heavy step):</strong> transcode the raw video into <strong>multiple resolutions + bitrates</strong> (240p…4K) and <strong>formats/segments</strong> for <strong>adaptive bitrate streaming</strong> (18.5 — HLS/DASH: segmented, multi-bitrate) → CPU-intensive, <strong>parallelized</strong> across a fleet of workers (17.3 — CPU-bound → parallelism), often <strong>per-segment</strong> (split → transcode segments in parallel → assemble).</li>\n<li><strong>Store outputs</strong> (4.1.3) + <strong>update metadata</strong> (video ready, available renditions).</li>\n<li><strong>Distribute to CDN</strong> (18.4/18.5): <strong>pre-position</strong> popular content at the edge; content served from the CDN, not the origin.</li>\n</ol>\n<ul>\n<li><code>[BP]</code> <strong>The upload/transcoding pipeline is async + parallel</strong> (9.1/17.3): upload to object storage → enqueue → transcode to multi-bitrate segments (CPU-heavy, parallel) → store + CDN. This is the write side 18.5 (which focused on delivery) implies.</li>\n</ul>\n<h3>3.3 Delivery — CDN + adaptive bitrate (18.5/18.4)</h3>\n<p><code>[BP]</code> (See 18.5 for full detail):</p>\n<ul>\n<li>Video <strong>streamed from the CDN edge</strong> (18.4 — pre-positioned near viewers) via <strong>adaptive bitrate</strong> (18.5 — player picks bitrate for its bandwidth, segment by segment).</li>\n<li><strong>Separate video (CDN data plane) from metadata (services)</strong> (18.5) — bytes bypass the app.</li>\n<li><code>[BP]</code> Delivery = <strong>18.5's video subsystem</strong> (CDN + ABR + pre-positioning).</li>\n</ul>\n<h3>3.4 Metadata + view counts</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Metadata:</strong> video info (title/desc/tags/renditions/uploader) in a <strong>DB</strong> (5.x) — read-heavy → cache (Part 6); <strong>search</strong> via an inverted index (18.7).</li>\n<li><strong>View counts (high-write):</strong> every view increments a counter → <strong>millions of increments</strong> → <strong>don't do a synchronous DB write per view</strong> (like 19.1.1 analytics) → <strong>emit view events</strong> (async — 9.1/18.1) → <strong>aggregate</strong> (batch/stream — 9.6/9.7) → periodically update the displayed count (eventually consistent — 10.5). Same for likes.</li>\n<li><code>[BP]</code> <strong>View counts are a high-write aggregation</strong> → async events + aggregation (18.1/9.6), not per-view DB writes. Metadata is read-heavy → cached (Part 6).</li>\n</ul>\n<h3>3.5 Recommendations + search</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Recommendations</strong> (18.5): watch/interaction events → log → batch/stream + ML → cached recs (18.5 §3.5).</li>\n<li><strong>Search</strong> (18.7): metadata + captions indexed in a search engine (inverted index — 18.7).</li>\n<li><code>[BP]</code> Reuse 18.5 (recs) + 18.7 (search).</li>\n</ul>\n<h3>3.6 Deep dives + bottlenecks</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Transcoding scale</strong> (§3.2, 17.3): CPU-bound → large parallel worker fleet; per-segment parallelism; autoscale (13.5) for upload spikes.</li>\n<li><strong>Storage/bandwidth cost</strong> (17.6): video is huge → <strong>CDN offload</strong> (18.4) + tiered storage (16.2/4.1.3 — hot on fast, cold on cheap); this is a major <strong>cost</strong> driver (17.6).</li>\n<li><strong>View-count accuracy vs cost</strong> (§3.4): async aggregation (eventually consistent — 10.5); approximate for popular videos.</li>\n<li><strong>Global delivery</strong> (18.4/13.8): CDN + multi-region.</li>\n<li><strong>Live streaming</strong> (extension): lower-latency pipeline (segment + distribute in near-real-time).</li>\n<li><strong>Bottleneck:</strong> transcoding (parallel fleet) + bandwidth/storage cost (CDN + tiered storage). Delivery scales via CDN (18.4); metadata via cache + sharding.</li>\n<li><code>[BP]</code> <strong>The lesson:</strong> a video platform = <strong>18.5's streaming architecture (CDN + ABR delivery, recs) + an async parallel transcoding pipeline (9.1/17.3) + object storage (4.1.3) + async view-count aggregation (18.1) + search (18.7)</strong>. The write side (transcoding) + delivery (CDN/ABR) + cost are the interesting parts.</li>\n</ul>\n<hr>\n<h2>4. Visual Intuition</h2>\n<pre><code class=\"language-mermaid\">flowchart TB\r\n    UP[\"Upload raw video (chunked/resumable)\"] --> OBJ[(\"Object storage — 4.1.3\")]\r\n    OBJ --> JOB[\"Enqueue transcoding job (9.1) — async\"]\r\n    JOB --> TRANS[\"Transcode → multi-bitrate segments (240p…4K, ABR — 18.5). CPU-bound → parallel fleet (17.3), per-segment\"]\r\n    TRANS --> OUT[(\"Store renditions (4.1.3) + update metadata (ready)\")]\r\n    OUT --> CDN[\"Distribute to CDN (18.4/18.5): pre-position near viewers\"]\r\n    VIEWER[\"Viewer streams (ABR) from CDN edge (bytes bypass app — 18.5)\"]\r\n    VIEWER -.view event (async — 18.1/9.1).-> AGG[\"Aggregate view counts (batch/stream — 9.6)\"]\r\n    note[\"Async parallel transcoding (write) + CDN/ABR delivery (read) + async view-count aggregation\"]\n</code></pre>\n<hr>\n<h2>5. Real-World Analogy</h2>\n<p>Think of a <strong>TV production + broadcast operation</strong>: producing content once, then broadcasting it to millions.</p>\n<ul>\n<li><strong>Upload + transcoding = producing broadcast-ready versions:</strong> a creator submits a <strong>raw master tape</strong> (upload to object storage). The studio then <strong>produces multiple versions</strong> — HD, SD, mobile-quality (transcoding to multi-bitrate) — a <strong>slow, compute-heavy job</strong> done by a <strong>large team working in parallel</strong> (parallel transcoding fleet), often by <strong>splitting the tape into reels and processing them simultaneously</strong> (per-segment parallelism). This happens <strong>after</strong> the creator submits (async — they don't wait); the video shows \"processing.\"</li>\n<li><strong>Delivery = broadcasting from local stations:</strong> the finished versions are <strong>shipped to thousands of local broadcast stations</strong> (CDN — pre-positioned) so viewers watch from a <strong>nearby station</strong>, and each viewer's device <strong>picks the quality matching their connection</strong> (adaptive bitrate). The <strong>actual video signal doesn't route through headquarters</strong> (video/CDN separate from the control services).</li>\n<li><strong>View counts = tallying viewership without stopping the broadcast:</strong> counting <strong>millions of views</strong> isn't done by <strong>updating a master ledger on every single view</strong> (that would bury the accountants) — instead, stations <strong>report viewership periodically</strong> and it's <strong>tallied up</strong> (async aggregation), showing an <strong>approximate, slightly-delayed count</strong>.</li>\n<li><strong>Cost = the dominant concern:</strong> producing and broadcasting <strong>video is enormously expensive</strong> (storage + bandwidth), so you <strong>keep popular content at local stations</strong> (CDN offload) and <strong>archive rarely-watched content to cheap deep storage</strong> (tiered storage).</li>\n</ul>\n<hr>\n<h2>6. Industry Example</h2>\n<ul>\n<li><strong>YouTube-style platform</strong> <code>[CONV]</code>: async transcoding pipeline + CDN/ABR delivery + metadata/search + recommendations (§3.2/3.3/3.5). <em>(Representative.)</em></li>\n<li><strong>Async parallel transcoding</strong> <code>[CONV]</code>: per-segment parallel transcoding to multi-bitrate renditions (§3.2, 9.1/17.3). <em>(Representative.)</em></li>\n<li><strong>CDN + adaptive bitrate delivery</strong> <code>[CONV]</code>: pre-positioned edge delivery, ABR (§3.3, 18.4/18.5). <em>(Representative.)</em></li>\n<li><strong>Async view-count aggregation</strong> <code>[CONV]</code>: view events → aggregation, eventually-consistent counts (§3.4, 18.1/9.6). <em>(Representative.)</em></li>\n<li><strong>Object storage + tiered storage for cost</strong> <code>[CONV]</code>: huge video storage, tiered by popularity (§3.6, 4.1.3/16.2/17.6). <em>(Representative.)</em></li>\n</ul>\n<hr>\n<h2>7. Implementation Details</h2>\n<ul>\n<li><strong>Upload → object storage</strong> (4.1.3, chunked/resumable — like 19.1.7) → <strong>enqueue transcoding job</strong> (9.1, async).</li>\n<li><strong>Transcode</strong> (§3.2, 17.3): multi-bitrate segments for ABR (18.5); <strong>CPU-bound → parallel worker fleet</strong> (per-segment); autoscale (13.5) for upload spikes.</li>\n<li><strong>Delivery</strong> (§3.3, 18.4/18.5): CDN + pre-positioning + ABR; <strong>separate video (CDN) from metadata (services)</strong>.</li>\n<li><strong>Metadata</strong> (5.x) — read-heavy → cache (Part 6); <strong>search</strong> via inverted index (18.7).</li>\n<li><strong>View counts/likes</strong> (§3.4): <strong>async view events</strong> (18.1/9.1) → <strong>aggregate</strong> (9.6) → eventually-consistent counts (10.5); don't write per view.</li>\n<li><strong>Recommendations</strong> (18.5): events → log → batch/stream + ML → cached recs.</li>\n<li><strong>Cost</strong> (17.6): CDN offload + tiered storage (16.2/4.1.3) — video is the cost driver.</li>\n</ul>\n<hr>\n<h2>8–14. (Advantages / disadvantages / mistakes / questions / pitfalls / optimizations)</h2>\n<p><strong>Advantages:</strong> scalable global delivery (CDN + ABR); async parallel transcoding; read-heavy metadata cached; view counts scale via async aggregation; cost-managed (CDN offload + tiered storage).\r\n<strong>Disadvantages/cautions:</strong> transcoding is compute-heavy (cost — 17.6); video storage/bandwidth cost dominates (17.6); eventually-consistent counts; complex pipeline.\r\n<strong>Common mistakes:</strong> synchronous transcoding (blocking upload); streaming video through app services (should be CDN — 18.5); per-view DB writes (should be async aggregation); no ABR (buffering); ignoring cost (huge bandwidth/storage bills — 17.6).\r\n<strong>Interview Qs:</strong> 🟢 Why async transcoding + CDN delivery? 🟡 The transcoding pipeline (parallel, per-segment, multi-bitrate)? View counts at scale? 🔴 CDN/ABR delivery + cost management + separate video/metadata? ⚫ Full design = 18.5 streaming + transcoding pipeline + view aggregation + search/recs.\r\n<strong>Production pitfalls:</strong> transcoding backlog under upload spikes (autoscale — 13.5); bandwidth/storage cost blowout (CDN + tiered storage — 17.6); buffering (no ABR); per-view write overload; video served from origin (not CDN).\r\n<strong>Optimizations:</strong> async parallel per-segment transcoding; CDN + pre-positioning + ABR (18.5); async view-count aggregation (18.1); tiered storage + CDN offload (cost — 17.6); metadata cache (Part 6); autoscaling transcoding (13.5).</p>\n<hr>\n<h2>15. Summary</h2>\n<p>A <strong>video platform</strong> (YouTube) lets users <strong>upload videos</strong> that are <strong>processed and streamed</strong> to viewers worldwide, with metadata, search (18.7), view counts, and recommendations (18.5) — it <strong>composes the streaming architecture</strong> (18.5) with an <strong>upload + transcoding pipeline</strong> (the write side 18.5's delivery-focus omitted). <strong>Key signals:</strong> <strong>video is huge + bandwidth-heavy → CDN + object storage</strong> (18.4/4.1.3); <strong>uploads need async processing</strong> (9.1); <strong>streaming is read-heavy</strong> (18.5). The <strong>upload + transcoding pipeline</strong> (async — 9.1): the client uploads the raw video (chunked/resumable — like 19.1.7) to <strong>object storage</strong> (4.1.3), which <strong>enqueues a transcoding job</strong> (async — the user doesn't wait; the video is \"processing\"); <strong>transcoding</strong> (the heavy, CPU-bound step — 17.3) produces <strong>multiple resolutions + bitrates</strong> and <strong>segmented formats for adaptive bitrate streaming</strong> (18.5 — HLS/DASH), <strong>parallelized</strong> across a worker fleet (17.3 — often <strong>per-segment</strong>: split → transcode in parallel → assemble); outputs are <strong>stored</strong> (4.1.3) + metadata updated (ready) + <strong>distributed to the CDN</strong> (18.4/18.5 — pre-positioned near viewers). <strong>Delivery</strong> is <strong>18.5's video subsystem</strong>: video <strong>streamed from the CDN edge</strong> via <strong>adaptive bitrate</strong> (player picks bitrate for its bandwidth), with <strong>video (CDN data plane) separate from metadata (services)</strong> (bytes bypass the app). <strong>Metadata</strong> (title/desc/renditions) is read-heavy → <strong>cached</strong> (Part 6) + <strong>searchable</strong> via an inverted index (18.7). <strong>View counts</strong> are a <strong>high-write aggregation</strong> — millions of increments → <strong>not a synchronous DB write per view</strong> (like 19.1.1) → <strong>emit view events</strong> (async — 18.1/9.1) → <strong>aggregate</strong> (batch/stream — 9.6) → periodically update the displayed (eventually-consistent — 10.5) count. <strong>Recommendations</strong> reuse 18.5's ML pipeline. <strong>Deep dives</strong>: <strong>transcoding scale</strong> (parallel fleet + autoscaling for spikes — 13.5), <strong>storage/bandwidth cost</strong> (the dominant driver — 17.6 → <strong>CDN offload</strong> + <strong>tiered storage</strong> — hot on fast, cold/rarely-watched on cheap — 16.2/4.1.3), <strong>view-count accuracy vs cost</strong> (async/approximate — 10.5), and <strong>global delivery</strong> (CDN + multi-region — 13.8). The <strong>bottleneck</strong> is <strong>transcoding</strong> (parallel fleet) + <strong>bandwidth/storage cost</strong> (CDN + tiered storage); delivery scales via CDN, metadata via cache + sharding. A video platform = <strong>18.5's streaming architecture (CDN + ABR delivery + recs) + an async parallel transcoding pipeline (9.1/17.3) + object storage (4.1.3) + async view-count aggregation (18.1) + search (18.7)</strong> — with transcoding (write), CDN/ABR (delivery), and cost as the interesting parts.</p>\n<hr>\n<h2>16. Revision Notes (flashcard-ready)</h2>\n<ul>\n<li><strong>Q:</strong> This design = ? <strong>A:</strong> 18.5 streaming architecture (delivery/recs) + an upload/transcoding pipeline (the write side).</li>\n<li><strong>Q:</strong> Transcoding pipeline? <strong>A:</strong> Upload→object storage→enqueue job (async — 9.1)→transcode to multi-bitrate segments (CPU-bound, parallel fleet, per-segment — 17.3)→store + CDN.</li>\n<li><strong>Q:</strong> Delivery? <strong>A:</strong> CDN edge (pre-positioned — 18.4) + adaptive bitrate (18.5); video separate from metadata (bytes bypass app).</li>\n<li><strong>Q:</strong> View counts at scale? <strong>A:</strong> Async view events (18.1/9.1) → aggregate (9.6) → eventually-consistent count; NOT per-view DB writes.</li>\n<li><strong>Q:</strong> Video storage? <strong>A:</strong> Object storage (4.1.3); tiered (hot/cold by popularity — 16.2) for cost (17.6).</li>\n<li><strong>Q:</strong> Why is video CPU-bound transcoding parallelized? <strong>A:</strong> CPU-bound → parallelism (17.3), per-segment across a worker fleet; autoscale for spikes (13.5).</li>\n<li><strong>Q:</strong> Metadata + search? <strong>A:</strong> DB + cache (read-heavy — Part 6); search via inverted index (18.7).</li>\n<li><strong>Q:</strong> Recommendations? <strong>A:</strong> Reuse 18.5's ML pipeline (events → log → batch/stream + ML → cached recs).</li>\n<li><strong>Q:</strong> The dominant concern? <strong>A:</strong> Cost (17.6) — video storage + bandwidth → CDN offload + tiered storage.</li>\n<li><strong>Q:</strong> Bottleneck? <strong>A:</strong> Transcoding (parallel fleet) + bandwidth/storage cost; delivery scales via CDN.</li>\n</ul>\n<hr>\n<h2>17. Further Reading + Knowledge-Graph Links</h2>\n<p><strong>Foundations:</strong> [18.5 Streaming] · [18.4 CDN] · [4.1.3 Object Storage] · [9.1 Messaging] · [17.3 Parallelism] · [18.7 Search] · [18.1 Log] · [17.6 Cost].\r\n<strong>External:</strong> YouTube-style video-platform design treatments. <em>(Representative.)</em></p>\n<blockquote>\n<p><strong>Knowledge-graph:</strong> <code>18.5 streaming</code> + async transcoding (<code>9.1/17.3</code>) + <code>4.1.3 object storage</code> + <code>18.4 CDN/ABR</code> + <code>18.1 view aggregation</code> + <code>18.7 search</code> → <strong><code>19.1.8 video platform (YouTube)</code></strong>.</p>\n</blockquote>"
  },
  "_id": "lessons/part-19-interview-designs/19.1.8-design-video-platform-youtube.md",
  "_raw": {
    "sourceFilePath": "lessons/part-19-interview-designs/19.1.8-design-video-platform-youtube.md",
    "sourceFileName": "19.1.8-design-video-platform-youtube.md",
    "sourceFileDir": "lessons/part-19-interview-designs",
    "contentType": "markdown",
    "flattenedPath": "lessons/part-19-interview-designs/19.1.8-design-video-platform-youtube"
  },
  "type": "Lesson",
  "title": "Design a Video Platform (YouTube)",
  "description": "",
  "slug": "part-19-interview-designs/19.1.8-design-video-platform-youtube",
  "url": "/learn/part-19-interview-designs/19.1.8-design-video-platform-youtube",
  "readingTime": 10,
  "flashcards": [
    {
      "q": "This design = ?",
      "a": "18.5 streaming architecture (delivery/recs) + an upload/transcoding pipeline (the write side)."
    },
    {
      "q": "Transcoding pipeline?",
      "a": "Upload→object storage→enqueue job (async — 9.1)→transcode to multi-bitrate segments (CPU-bound, parallel fleet, per-segment — 17.3)→store + CDN."
    },
    {
      "q": "Delivery?",
      "a": "CDN edge (pre-positioned — 18.4) + adaptive bitrate (18.5); video separate from metadata (bytes bypass app)."
    },
    {
      "q": "View counts at scale?",
      "a": "Async view events (18.1/9.1) → aggregate (9.6) → eventually-consistent count; NOT per-view DB writes."
    },
    {
      "q": "Video storage?",
      "a": "Object storage (4.1.3); tiered (hot/cold by popularity — 16.2) for cost (17.6)."
    },
    {
      "q": "Why is video CPU-bound transcoding parallelized?",
      "a": "CPU-bound → parallelism (17.3), per-segment across a worker fleet; autoscale for spikes (13.5)."
    },
    {
      "q": "Metadata + search?",
      "a": "DB + cache (read-heavy — Part 6); search via inverted index (18.7)."
    },
    {
      "q": "Recommendations?",
      "a": "Reuse 18.5's ML pipeline (events → log → batch/stream + ML → cached recs)."
    },
    {
      "q": "The dominant concern?",
      "a": "Cost (17.6) — video storage + bandwidth → CDN offload + tiered storage."
    },
    {
      "q": "Bottleneck?",
      "a": "Transcoding (parallel fleet) + bandwidth/storage cost; delivery scales via CDN."
    }
  ],
  "metadata": {
    "partNum": 0,
    "lessonId": "",
    "difficulty": "foundational",
    "prerequisites": [
      "[4.1.3 Object Storage]",
      "[9.1 Messaging]",
      "[18.4 CDN]",
      "[18.5 Streaming]",
      "[Part 6 Caching]",
      "[1.3.1 Framework]."
    ],
    "unlocks": [
      "[19.2.7 Recommendations]",
      "[Part 20 Capstone]."
    ]
  }
}