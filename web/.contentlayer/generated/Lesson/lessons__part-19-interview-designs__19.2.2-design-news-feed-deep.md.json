{
  "body": {
    "raw": "# Lesson 19.2.2 — Design a News Feed System (Deep)\r\n\r\n> Part 19 · Module 19.2 (Volume 2) · Difficulty: 🔴 · *Interview design*\r\n>\r\n> **Prerequisites:** [19.1.5 News Feed/Instagram], [18.8 Chat at Scale (fan-out)], [Part 6 Caching], [7.3 Sharding], [7.4 Hotspots/Skew], [Part 9 Messaging].\r\n> **Unlocks:** [19.2.7 Recommendations], [Part 20 Capstone].\r\n\r\n---\r\n\r\n## 1. Learning Objectives\r\n\r\nAfter this lesson you will be able to:\r\n\r\n- Design a **news feed** end-to-end at depth (framework — 1.3.1), building on **19.1.5** — going deeper on **fan-out strategy**, the **celebrity/hot-key problem** (7.4), **ranking**, and **pagination**.\r\n- Choose between **fan-out-on-write (push)**, **fan-out-on-read (pull)**, and the **hybrid** — and justify by workload.\r\n- Solve the **celebrity problem** (a user with millions of followers breaks pure push — 7.4).\r\n- Design **feed ranking** (chronological → ML-ranked — hooks into 19.2.7) and **stable pagination**.\r\n- Handle deep dives: **read vs write amplification**, **feed storage/caching**, **consistency** (eventual — 10.5), and **the write-fan-out bottleneck**.\r\n\r\n---\r\n\r\n## 2. Problem statement\r\n\r\nDesign a **news feed** (Twitter/X timeline, Facebook feed, Instagram home) at depth: when a user opens the app, show a **ranked, recent list of posts from accounts they follow**, updated as new posts arrive. This extends **19.1.5** — the crux is the **fan-out strategy** (how a post reaches millions of followers' feeds) and the **celebrity/hot-key** problem (7.4). It's the archetypal **massive-read-fan-out + write-amplification** design.\r\n\r\n---\r\n\r\n## 3. The design (framework — 1.3.1)\r\n\r\n### 3.1 Requirements\r\n\r\n`[BP]`\r\n- **Functional:** post creation; a **home feed** = merged, ranked posts from followed accounts; **pagination** (infinite scroll); near-real-time updates.\r\n- **Non-functional:** **read-heavy** (feed loads ≫ posts — the dominant path); **low latency** feed load (users wait); **eventual consistency** OK (a post appearing a few seconds late is fine — 10.5); highly available; huge scale (fan-out amplification).\r\n- `[BP]` **Key signal:** it's about **getting posts to followers efficiently** — a **fan-out + read-scaling** problem where the **follower-graph skew** (celebrities — 7.4) dominates the design.\r\n\r\n### 3.2 Estimation (1.1.4)\r\n\r\n`[BP]` Illustrative: reads (feed loads) vastly outnumber writes (posts) — say **100:1+**. But a **single post by a celebrity with 100M followers** implies **100M feed insertions** if you push → **write amplification** is the killer (7.4). → The design revolves around **not doing 100M writes per celebrity post**.\r\n\r\n### 3.3 API + data model\r\n\r\n`[BP]`\r\n- `POST /posts`, `GET /feed?cursor=…` (paginated), `POST /follow`.\r\n- **Data:** `posts` (by postId, sharded — 7.3), `follows` (edges: follower→followee), and **precomputed feeds** (`userId → list of postIds`, cached — Part 6). Use **cursor-based pagination** (not offset) for stable infinite scroll over a changing list (§3.6).\r\n\r\n### 3.4 The core: fan-out strategy (the crux — 19.1.5, deeper)\r\n\r\n`[CS]` How a new post reaches followers' feeds `[BP]`:\r\n- **Fan-out-on-write (push):** when you post, **immediately insert the postId into every follower's precomputed feed** (in cache/store). **Feed read = cheap** (just fetch your precomputed list — great for the read-heavy path). **Cost:** a post triggers **as many writes as you have followers** → **write amplification**; catastrophic for **celebrities** (100M writes per post — 7.4) and wasteful for **inactive followers**.\r\n- **Fan-out-on-read (pull):** store only each user's own posts; when a follower opens their feed, **query all followees' recent posts and merge/rank at read time**. **Post = cheap** (one write); **read = expensive** (scatter-gather across many followees — 18.7-style — every feed load). Bad for the read-heavy path, but immune to the celebrity write problem.\r\n- **Hybrid (the answer):** **push for normal users** (cheap, fast reads) but **pull for celebrities** — don't fan-out a celebrity's post to 100M feeds; instead, **at read time, merge in the (few) celebrities you follow** on top of your pushed feed. Combines cheap reads (mostly precomputed) with no celebrity write explosion (7.4). `[BP]` **This is the standard senior answer.**\r\n\r\n### 3.5 HLD\r\n\r\n`[BP]`\r\n- **Post service** → writes the post (7.3) → emits a \"new post\" event (Part 9).\r\n- **Fan-out service** (async worker consuming the event — Part 9): for a normal author, **push** postId into each active follower's feed (cache — Part 6); for a **celebrity** (follower count above a threshold), **skip push** (mark for pull).\r\n- **Feed service (read):** fetch the user's **precomputed feed** (cache — Part 6), **merge in followed celebrities' recent posts** (pull), **rank** (§3.6), paginate (cursor), return.\r\n- **Feed store:** per-user feed lists in a fast store/cache (Redis — 6.6), capped length (only recent N — 6.4), rebuildable from posts + follows.\r\n\r\n### 3.6 Deep dives + bottlenecks\r\n\r\n`[BP]`\r\n- **Celebrity/hot-key** (7.4): the make-or-break — **hybrid push/pull** avoids the 100M-write fan-out; celebrity posts merged at read (§3.4). (Same skew lesson as 7.4/18.6.)\r\n- **Write amplification** (§3.2): push trades read cost for write cost; hybrid + async fan-out (Part 9) + skipping inactive users bounds it.\r\n- **Ranking:** chronological (simplest) → **ML-ranked** (engagement-predicting model — hooks into **19.2.7** recommendations); rank at read over the merged candidate set. Keep candidate generation (fan-out) separate from ranking (18.5-style online/offline split).\r\n- **Pagination:** **cursor-based** (opaque cursor = position/timestamp+id), not offset — stable under a feed that's constantly growing (offset would skip/duplicate — a classic mistake).\r\n- **Consistency:** **eventual** (10.5) — a post may take seconds to appear in all feeds (async fan-out); acceptable. Read-your-writes for your **own** posts (show them immediately — 10.3).\r\n- **Feed storage/caching:** feeds are **cached, capped, rebuildable** derived data (Part 6) — losing them just means recompute; source of truth is posts + follows.\r\n- **Bottleneck:** the **write-side fan-out** (celebrity amplification — 7.4) → hybrid + async workers + caps. Reads are cheap (precomputed + cache).\r\n- `[BP]` **The lesson:** news feed = **hybrid fan-out (push normal / pull celebrities — 7.4) + async fan-out workers (Part 9) + cached precomputed feeds (Part 6) + read-time merge/rank + cursor pagination**. The whole design bends around the **follower-graph skew**.\r\n\r\n---\r\n\r\n## 4. Visual Intuition\r\n\r\n```mermaid\r\nflowchart TB\r\n    POST[\"New post\"] --> EV[\"Event (Part 9)\"]\r\n    EV --> FO[\"Fan-out worker (async)\"]\r\n    FO -->|normal author: PUSH postId| FEEDS[(\"Per-user feed cache (Redis — 6.6), capped\")]\r\n    FO -.celebrity: SKIP push (mark pull — 7.4).-> X[\"(no 100M writes)\"]\r\n    USER[\"Feed load\"] --> FS[\"Feed service (read)\"]\r\n    FS --> FEEDS\r\n    FS -->|MERGE followed celebrities' recent posts| CELEB[(\"Celebrity posts\")]\r\n    FS --> RANK[\"Rank (chrono → ML — 19.2.7) + cursor paginate\"] --> OUT[\"Feed\"]\r\n    note[\"Hybrid: push for most, pull for celebrities; async fan-out; cached feeds; merge+rank at read\"]\r\n```\r\n\r\n---\r\n\r\n## 5. Real-World Analogy\r\n\r\nThink of **delivering a newsletter to subscribers, where most authors have a few subscribers but a few superstars have millions**.\r\n\r\n- **Push (fan-out-on-write) = pre-stuffing every subscriber's mailbox** the moment an author publishes. Reading your mail is instant (it's already there), but if a **superstar with 100 million subscribers** publishes, you'd have to stuff 100 million mailboxes at once — impossible in real time.\r\n- **Pull (fan-out-on-read) = assembling your newsletter only when you check the mail** — the courier runs to every author you follow and gathers their latest. One publish is cheap, but assembling on every read is slow.\r\n- **Hybrid = pre-stuff for normal authors, fetch-on-read for superstars.** Your mailbox is mostly pre-filled (fast), and when you open it, the courier just grabs the handful of superstars you follow and slots their latest in. Cheap reads, no impossible superstar stuffing.\r\n- **Ranking = the editor arranging the newsletter** by what you'll care about most, not strictly by arrival time.\r\n- **Cursor pagination = a bookmark, not a page number:** since new items keep arriving at the top, \"give me page 2\" (offset) would skip or repeat items; a bookmark (\"continue after this exact item\") stays stable.\r\n\r\n---\r\n\r\n## 6. Industry Example\r\n\r\n- **Twitter/Facebook/Instagram hybrid fan-out** `[CONV]`: push for most users, pull for high-follower accounts (§3.4, 7.4). *(Representative.)*\r\n- **Async fan-out via a log/queue** `[CONV]`: post events consumed by fan-out workers (§3.5, Part 9/18.1). *(Representative.)*\r\n- **Cached precomputed feeds (Redis)** `[CONV]`: capped, rebuildable per-user feed lists (§3.5, 6.6). *(Representative.)*\r\n- **ML-ranked feeds** `[CONV]`: engagement ranking over merged candidates (§3.6, 19.2.7/18.5). *(Representative.)*\r\n- **Cursor-based pagination** `[CONV]`: stable infinite scroll (§3.6). *(Representative.)*\r\n\r\n---\r\n\r\n## 7. Implementation Details\r\n\r\n- **Hybrid fan-out** (push normal / pull celebrities by follower-count threshold — 7.4) via **async workers** (Part 9) (§3.4/3.5).\r\n- **Precomputed feeds** in Redis (6.6), capped to recent N (6.4), rebuildable from posts+follows (§3.5).\r\n- **Read path:** fetch precomputed feed + merge followed celebrities' recent posts + rank + cursor-paginate (§3.5/3.6).\r\n- **Ranking:** chronological → ML (separate candidate generation from ranking — 18.5/19.2.7).\r\n- **Eventual consistency** (10.5) with read-your-writes for own posts (10.3).\r\n- **Cursor pagination** (not offset) for stability (§3.6).\r\n\r\n---\r\n\r\n## 8–14. (Condensed)\r\n\r\n**Advantages:** cheap fast reads (precomputed), no celebrity write explosion (hybrid), decoupled async fan-out, ranking pluggable.\r\n**Disadvantages/cautions:** eventual consistency (posts appear with lag); feed storage cost (precomputed lists); hybrid adds read-path complexity (merge); ranking needs a separate ML system (19.2.7).\r\n**When NOT to use push:** for celebrities / low-active-follower ratios — pull or hybrid instead (7.4).\r\n**Common mistakes:** pure push (celebrity write explosion); pure pull (slow reads on a read-heavy path); offset pagination (skips/dupes on a growing feed); synchronous fan-out (blocks the post request); unbounded feed lists.\r\n**Interview Qs:** 🟢 Read-heavy or write-heavy, and why? 🟡 Push vs pull vs hybrid — tradeoffs? 🔴 How do you solve the celebrity problem? Ranking + pagination? ⚫ Full design: hybrid fan-out, async workers, cached feeds, read-time merge+rank, cursor pagination, eventual consistency, and the write-amplification bottleneck.\r\n**Production pitfalls:** celebrity hot-keys (7.4); fan-out backlog under viral events (queue lag — 9.9); feed-cache stampede (6.7); inconsistent ranking causing feed \"jumping.\"\r\n**Optimizations:** skip inactive followers on push; cap + expire feeds (6.4); batch fan-out writes; separate candidate-generation from ranking; edge/CDN for media (18.4); precompute celebrity recent-posts cache.\r\n\r\n---\r\n\r\n## 15. Summary\r\n\r\nA **news feed** (Twitter/Facebook/Instagram) is the archetypal **massive-read-fan-out + write-amplification** design; this deep treatment extends **19.1.5**, centering on the **fan-out strategy** and the **celebrity/hot-key problem (7.4)**. It's **read-heavy** (feed loads ≫ posts, ~100:1+), **latency-sensitive** on reads, and **eventually consistent** is fine (a post appearing seconds late is acceptable — 10.5). The **core decision** is how a post reaches followers: **fan-out-on-write (push)** inserts the postId into every follower's **precomputed feed** immediately (cheap reads, but **write amplification** — a celebrity's post = millions of writes — 7.4); **fan-out-on-read (pull)** stores only own posts and **merges followees' posts at read time** (cheap writes, expensive reads — bad for the read-heavy path); the **hybrid** (the senior answer) **pushes for normal users** but **pulls for celebrities** — at read time it merges the few followed celebrities' recent posts on top of the pushed feed, getting cheap reads with no celebrity write explosion. The **HLD**: a post emits an event (Part 9); an **async fan-out worker** pushes to active followers' feed caches (Redis — 6.6, capped/rebuildable — 6.4) but **skips celebrities**; the **read path** fetches the precomputed feed, merges followed-celebrity posts, **ranks** (chronological → ML — hooks into **19.2.7**, keeping candidate-generation separate from ranking à la 18.5), and **cursor-paginates** (opaque cursor, **not offset** — stable over a growing feed). **Deep dives:** the celebrity hot-key (hybrid — 7.4), write amplification (async + caps), ranking (ML), pagination (cursor), consistency (eventual + read-your-writes for own posts — 10.3), and feed storage (cached, capped, rebuildable derived data — Part 6). The **bottleneck is the write-side fan-out** (celebrity amplification), dissolved by hybrid + async workers + caps; reads stay cheap (precomputed + cached). The whole design **bends around the follower-graph skew**.\r\n\r\n---\r\n\r\n## 16. Revision Notes (flashcard-ready)\r\n\r\n- **Q:** Read-heavy or write-heavy? **A:** Read-heavy (feed loads ≫ posts) — but write-side fan-out amplification is the hard part.\r\n- **Q:** Push (fan-out-on-write)? **A:** Insert postId into every follower's precomputed feed on post → cheap reads, write amplification (bad for celebrities).\r\n- **Q:** Pull (fan-out-on-read)? **A:** Merge followees' posts at read time → cheap writes, expensive reads (bad for read-heavy path).\r\n- **Q:** Hybrid (the answer)? **A:** Push for normal users, pull for celebrities (merge their recent posts at read) — cheap reads, no celebrity write explosion (7.4).\r\n- **Q:** Celebrity problem? **A:** A post to 100M followers = 100M writes if pushed → skip push, pull at read.\r\n- **Q:** Fan-out mechanism? **A:** Async workers consuming post events (Part 9); precomputed feeds cached (Redis), capped, rebuildable.\r\n- **Q:** Pagination? **A:** Cursor-based (opaque position), not offset — stable over a growing feed.\r\n- **Q:** Consistency? **A:** Eventual (posts appear with lag); read-your-writes for your own posts (10.3).\r\n- **Q:** Ranking? **A:** Chronological → ML-ranked; separate candidate generation (fan-out) from ranking (19.2.7/18.5).\r\n\r\n---\r\n\r\n## 17. Further Reading + Knowledge-Graph Links\r\n\r\n**Foundations:** [19.1.5 News Feed/Instagram] · [7.4 Hotspots/Skew] · [Part 6 Caching] · [Part 9 Messaging] · [18.5 Recommendations] · [19.2.7 Recommendation System].\r\n**External:** Standard feed/timeline system-design treatments; fan-out literature. *(Representative.)*\r\n\r\n> **Knowledge-graph:** `19.1.5 feed` + `7.4 skew` + `Part 6 cache` + `Part 9 async` → **`19.2.2 news feed (deep)`** (hybrid fan-out, celebrity pull, cached feeds, cursor pagination).\r\n",
    "html": "<h1>Lesson 19.2.2 — Design a News Feed System (Deep)</h1>\n<blockquote>\n<p>Part 19 · Module 19.2 (Volume 2) · Difficulty: 🔴 · <em>Interview design</em></p>\n<p><strong>Prerequisites:</strong> [19.1.5 News Feed/Instagram], [18.8 Chat at Scale (fan-out)], [Part 6 Caching], [7.3 Sharding], [7.4 Hotspots/Skew], [Part 9 Messaging].\r\n<strong>Unlocks:</strong> [19.2.7 Recommendations], [Part 20 Capstone].</p>\n</blockquote>\n<hr>\n<h2>1. Learning Objectives</h2>\n<p>After this lesson you will be able to:</p>\n<ul>\n<li>Design a <strong>news feed</strong> end-to-end at depth (framework — 1.3.1), building on <strong>19.1.5</strong> — going deeper on <strong>fan-out strategy</strong>, the <strong>celebrity/hot-key problem</strong> (7.4), <strong>ranking</strong>, and <strong>pagination</strong>.</li>\n<li>Choose between <strong>fan-out-on-write (push)</strong>, <strong>fan-out-on-read (pull)</strong>, and the <strong>hybrid</strong> — and justify by workload.</li>\n<li>Solve the <strong>celebrity problem</strong> (a user with millions of followers breaks pure push — 7.4).</li>\n<li>Design <strong>feed ranking</strong> (chronological → ML-ranked — hooks into 19.2.7) and <strong>stable pagination</strong>.</li>\n<li>Handle deep dives: <strong>read vs write amplification</strong>, <strong>feed storage/caching</strong>, <strong>consistency</strong> (eventual — 10.5), and <strong>the write-fan-out bottleneck</strong>.</li>\n</ul>\n<hr>\n<h2>2. Problem statement</h2>\n<p>Design a <strong>news feed</strong> (Twitter/X timeline, Facebook feed, Instagram home) at depth: when a user opens the app, show a <strong>ranked, recent list of posts from accounts they follow</strong>, updated as new posts arrive. This extends <strong>19.1.5</strong> — the crux is the <strong>fan-out strategy</strong> (how a post reaches millions of followers' feeds) and the <strong>celebrity/hot-key</strong> problem (7.4). It's the archetypal <strong>massive-read-fan-out + write-amplification</strong> design.</p>\n<hr>\n<h2>3. The design (framework — 1.3.1)</h2>\n<h3>3.1 Requirements</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Functional:</strong> post creation; a <strong>home feed</strong> = merged, ranked posts from followed accounts; <strong>pagination</strong> (infinite scroll); near-real-time updates.</li>\n<li><strong>Non-functional:</strong> <strong>read-heavy</strong> (feed loads ≫ posts — the dominant path); <strong>low latency</strong> feed load (users wait); <strong>eventual consistency</strong> OK (a post appearing a few seconds late is fine — 10.5); highly available; huge scale (fan-out amplification).</li>\n<li><code>[BP]</code> <strong>Key signal:</strong> it's about <strong>getting posts to followers efficiently</strong> — a <strong>fan-out + read-scaling</strong> problem where the <strong>follower-graph skew</strong> (celebrities — 7.4) dominates the design.</li>\n</ul>\n<h3>3.2 Estimation (1.1.4)</h3>\n<p><code>[BP]</code> Illustrative: reads (feed loads) vastly outnumber writes (posts) — say <strong>100:1+</strong>. But a <strong>single post by a celebrity with 100M followers</strong> implies <strong>100M feed insertions</strong> if you push → <strong>write amplification</strong> is the killer (7.4). → The design revolves around <strong>not doing 100M writes per celebrity post</strong>.</p>\n<h3>3.3 API + data model</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><code>POST /posts</code>, <code>GET /feed?cursor=…</code> (paginated), <code>POST /follow</code>.</li>\n<li><strong>Data:</strong> <code>posts</code> (by postId, sharded — 7.3), <code>follows</code> (edges: follower→followee), and <strong>precomputed feeds</strong> (<code>userId → list of postIds</code>, cached — Part 6). Use <strong>cursor-based pagination</strong> (not offset) for stable infinite scroll over a changing list (§3.6).</li>\n</ul>\n<h3>3.4 The core: fan-out strategy (the crux — 19.1.5, deeper)</h3>\n<p><code>[CS]</code> How a new post reaches followers' feeds <code>[BP]</code>:</p>\n<ul>\n<li><strong>Fan-out-on-write (push):</strong> when you post, <strong>immediately insert the postId into every follower's precomputed feed</strong> (in cache/store). <strong>Feed read = cheap</strong> (just fetch your precomputed list — great for the read-heavy path). <strong>Cost:</strong> a post triggers <strong>as many writes as you have followers</strong> → <strong>write amplification</strong>; catastrophic for <strong>celebrities</strong> (100M writes per post — 7.4) and wasteful for <strong>inactive followers</strong>.</li>\n<li><strong>Fan-out-on-read (pull):</strong> store only each user's own posts; when a follower opens their feed, <strong>query all followees' recent posts and merge/rank at read time</strong>. <strong>Post = cheap</strong> (one write); <strong>read = expensive</strong> (scatter-gather across many followees — 18.7-style — every feed load). Bad for the read-heavy path, but immune to the celebrity write problem.</li>\n<li><strong>Hybrid (the answer):</strong> <strong>push for normal users</strong> (cheap, fast reads) but <strong>pull for celebrities</strong> — don't fan-out a celebrity's post to 100M feeds; instead, <strong>at read time, merge in the (few) celebrities you follow</strong> on top of your pushed feed. Combines cheap reads (mostly precomputed) with no celebrity write explosion (7.4). <code>[BP]</code> <strong>This is the standard senior answer.</strong></li>\n</ul>\n<h3>3.5 HLD</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Post service</strong> → writes the post (7.3) → emits a \"new post\" event (Part 9).</li>\n<li><strong>Fan-out service</strong> (async worker consuming the event — Part 9): for a normal author, <strong>push</strong> postId into each active follower's feed (cache — Part 6); for a <strong>celebrity</strong> (follower count above a threshold), <strong>skip push</strong> (mark for pull).</li>\n<li><strong>Feed service (read):</strong> fetch the user's <strong>precomputed feed</strong> (cache — Part 6), <strong>merge in followed celebrities' recent posts</strong> (pull), <strong>rank</strong> (§3.6), paginate (cursor), return.</li>\n<li><strong>Feed store:</strong> per-user feed lists in a fast store/cache (Redis — 6.6), capped length (only recent N — 6.4), rebuildable from posts + follows.</li>\n</ul>\n<h3>3.6 Deep dives + bottlenecks</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Celebrity/hot-key</strong> (7.4): the make-or-break — <strong>hybrid push/pull</strong> avoids the 100M-write fan-out; celebrity posts merged at read (§3.4). (Same skew lesson as 7.4/18.6.)</li>\n<li><strong>Write amplification</strong> (§3.2): push trades read cost for write cost; hybrid + async fan-out (Part 9) + skipping inactive users bounds it.</li>\n<li><strong>Ranking:</strong> chronological (simplest) → <strong>ML-ranked</strong> (engagement-predicting model — hooks into <strong>19.2.7</strong> recommendations); rank at read over the merged candidate set. Keep candidate generation (fan-out) separate from ranking (18.5-style online/offline split).</li>\n<li><strong>Pagination:</strong> <strong>cursor-based</strong> (opaque cursor = position/timestamp+id), not offset — stable under a feed that's constantly growing (offset would skip/duplicate — a classic mistake).</li>\n<li><strong>Consistency:</strong> <strong>eventual</strong> (10.5) — a post may take seconds to appear in all feeds (async fan-out); acceptable. Read-your-writes for your <strong>own</strong> posts (show them immediately — 10.3).</li>\n<li><strong>Feed storage/caching:</strong> feeds are <strong>cached, capped, rebuildable</strong> derived data (Part 6) — losing them just means recompute; source of truth is posts + follows.</li>\n<li><strong>Bottleneck:</strong> the <strong>write-side fan-out</strong> (celebrity amplification — 7.4) → hybrid + async workers + caps. Reads are cheap (precomputed + cache).</li>\n<li><code>[BP]</code> <strong>The lesson:</strong> news feed = <strong>hybrid fan-out (push normal / pull celebrities — 7.4) + async fan-out workers (Part 9) + cached precomputed feeds (Part 6) + read-time merge/rank + cursor pagination</strong>. The whole design bends around the <strong>follower-graph skew</strong>.</li>\n</ul>\n<hr>\n<h2>4. Visual Intuition</h2>\n<pre><code class=\"language-mermaid\">flowchart TB\r\n    POST[\"New post\"] --> EV[\"Event (Part 9)\"]\r\n    EV --> FO[\"Fan-out worker (async)\"]\r\n    FO -->|normal author: PUSH postId| FEEDS[(\"Per-user feed cache (Redis — 6.6), capped\")]\r\n    FO -.celebrity: SKIP push (mark pull — 7.4).-> X[\"(no 100M writes)\"]\r\n    USER[\"Feed load\"] --> FS[\"Feed service (read)\"]\r\n    FS --> FEEDS\r\n    FS -->|MERGE followed celebrities' recent posts| CELEB[(\"Celebrity posts\")]\r\n    FS --> RANK[\"Rank (chrono → ML — 19.2.7) + cursor paginate\"] --> OUT[\"Feed\"]\r\n    note[\"Hybrid: push for most, pull for celebrities; async fan-out; cached feeds; merge+rank at read\"]\n</code></pre>\n<hr>\n<h2>5. Real-World Analogy</h2>\n<p>Think of <strong>delivering a newsletter to subscribers, where most authors have a few subscribers but a few superstars have millions</strong>.</p>\n<ul>\n<li><strong>Push (fan-out-on-write) = pre-stuffing every subscriber's mailbox</strong> the moment an author publishes. Reading your mail is instant (it's already there), but if a <strong>superstar with 100 million subscribers</strong> publishes, you'd have to stuff 100 million mailboxes at once — impossible in real time.</li>\n<li><strong>Pull (fan-out-on-read) = assembling your newsletter only when you check the mail</strong> — the courier runs to every author you follow and gathers their latest. One publish is cheap, but assembling on every read is slow.</li>\n<li><strong>Hybrid = pre-stuff for normal authors, fetch-on-read for superstars.</strong> Your mailbox is mostly pre-filled (fast), and when you open it, the courier just grabs the handful of superstars you follow and slots their latest in. Cheap reads, no impossible superstar stuffing.</li>\n<li><strong>Ranking = the editor arranging the newsletter</strong> by what you'll care about most, not strictly by arrival time.</li>\n<li><strong>Cursor pagination = a bookmark, not a page number:</strong> since new items keep arriving at the top, \"give me page 2\" (offset) would skip or repeat items; a bookmark (\"continue after this exact item\") stays stable.</li>\n</ul>\n<hr>\n<h2>6. Industry Example</h2>\n<ul>\n<li><strong>Twitter/Facebook/Instagram hybrid fan-out</strong> <code>[CONV]</code>: push for most users, pull for high-follower accounts (§3.4, 7.4). <em>(Representative.)</em></li>\n<li><strong>Async fan-out via a log/queue</strong> <code>[CONV]</code>: post events consumed by fan-out workers (§3.5, Part 9/18.1). <em>(Representative.)</em></li>\n<li><strong>Cached precomputed feeds (Redis)</strong> <code>[CONV]</code>: capped, rebuildable per-user feed lists (§3.5, 6.6). <em>(Representative.)</em></li>\n<li><strong>ML-ranked feeds</strong> <code>[CONV]</code>: engagement ranking over merged candidates (§3.6, 19.2.7/18.5). <em>(Representative.)</em></li>\n<li><strong>Cursor-based pagination</strong> <code>[CONV]</code>: stable infinite scroll (§3.6). <em>(Representative.)</em></li>\n</ul>\n<hr>\n<h2>7. Implementation Details</h2>\n<ul>\n<li><strong>Hybrid fan-out</strong> (push normal / pull celebrities by follower-count threshold — 7.4) via <strong>async workers</strong> (Part 9) (§3.4/3.5).</li>\n<li><strong>Precomputed feeds</strong> in Redis (6.6), capped to recent N (6.4), rebuildable from posts+follows (§3.5).</li>\n<li><strong>Read path:</strong> fetch precomputed feed + merge followed celebrities' recent posts + rank + cursor-paginate (§3.5/3.6).</li>\n<li><strong>Ranking:</strong> chronological → ML (separate candidate generation from ranking — 18.5/19.2.7).</li>\n<li><strong>Eventual consistency</strong> (10.5) with read-your-writes for own posts (10.3).</li>\n<li><strong>Cursor pagination</strong> (not offset) for stability (§3.6).</li>\n</ul>\n<hr>\n<h2>8–14. (Condensed)</h2>\n<p><strong>Advantages:</strong> cheap fast reads (precomputed), no celebrity write explosion (hybrid), decoupled async fan-out, ranking pluggable.\r\n<strong>Disadvantages/cautions:</strong> eventual consistency (posts appear with lag); feed storage cost (precomputed lists); hybrid adds read-path complexity (merge); ranking needs a separate ML system (19.2.7).\r\n<strong>When NOT to use push:</strong> for celebrities / low-active-follower ratios — pull or hybrid instead (7.4).\r\n<strong>Common mistakes:</strong> pure push (celebrity write explosion); pure pull (slow reads on a read-heavy path); offset pagination (skips/dupes on a growing feed); synchronous fan-out (blocks the post request); unbounded feed lists.\r\n<strong>Interview Qs:</strong> 🟢 Read-heavy or write-heavy, and why? 🟡 Push vs pull vs hybrid — tradeoffs? 🔴 How do you solve the celebrity problem? Ranking + pagination? ⚫ Full design: hybrid fan-out, async workers, cached feeds, read-time merge+rank, cursor pagination, eventual consistency, and the write-amplification bottleneck.\r\n<strong>Production pitfalls:</strong> celebrity hot-keys (7.4); fan-out backlog under viral events (queue lag — 9.9); feed-cache stampede (6.7); inconsistent ranking causing feed \"jumping.\"\r\n<strong>Optimizations:</strong> skip inactive followers on push; cap + expire feeds (6.4); batch fan-out writes; separate candidate-generation from ranking; edge/CDN for media (18.4); precompute celebrity recent-posts cache.</p>\n<hr>\n<h2>15. Summary</h2>\n<p>A <strong>news feed</strong> (Twitter/Facebook/Instagram) is the archetypal <strong>massive-read-fan-out + write-amplification</strong> design; this deep treatment extends <strong>19.1.5</strong>, centering on the <strong>fan-out strategy</strong> and the <strong>celebrity/hot-key problem (7.4)</strong>. It's <strong>read-heavy</strong> (feed loads ≫ posts, ~100:1+), <strong>latency-sensitive</strong> on reads, and <strong>eventually consistent</strong> is fine (a post appearing seconds late is acceptable — 10.5). The <strong>core decision</strong> is how a post reaches followers: <strong>fan-out-on-write (push)</strong> inserts the postId into every follower's <strong>precomputed feed</strong> immediately (cheap reads, but <strong>write amplification</strong> — a celebrity's post = millions of writes — 7.4); <strong>fan-out-on-read (pull)</strong> stores only own posts and <strong>merges followees' posts at read time</strong> (cheap writes, expensive reads — bad for the read-heavy path); the <strong>hybrid</strong> (the senior answer) <strong>pushes for normal users</strong> but <strong>pulls for celebrities</strong> — at read time it merges the few followed celebrities' recent posts on top of the pushed feed, getting cheap reads with no celebrity write explosion. The <strong>HLD</strong>: a post emits an event (Part 9); an <strong>async fan-out worker</strong> pushes to active followers' feed caches (Redis — 6.6, capped/rebuildable — 6.4) but <strong>skips celebrities</strong>; the <strong>read path</strong> fetches the precomputed feed, merges followed-celebrity posts, <strong>ranks</strong> (chronological → ML — hooks into <strong>19.2.7</strong>, keeping candidate-generation separate from ranking à la 18.5), and <strong>cursor-paginates</strong> (opaque cursor, <strong>not offset</strong> — stable over a growing feed). <strong>Deep dives:</strong> the celebrity hot-key (hybrid — 7.4), write amplification (async + caps), ranking (ML), pagination (cursor), consistency (eventual + read-your-writes for own posts — 10.3), and feed storage (cached, capped, rebuildable derived data — Part 6). The <strong>bottleneck is the write-side fan-out</strong> (celebrity amplification), dissolved by hybrid + async workers + caps; reads stay cheap (precomputed + cached). The whole design <strong>bends around the follower-graph skew</strong>.</p>\n<hr>\n<h2>16. Revision Notes (flashcard-ready)</h2>\n<ul>\n<li><strong>Q:</strong> Read-heavy or write-heavy? <strong>A:</strong> Read-heavy (feed loads ≫ posts) — but write-side fan-out amplification is the hard part.</li>\n<li><strong>Q:</strong> Push (fan-out-on-write)? <strong>A:</strong> Insert postId into every follower's precomputed feed on post → cheap reads, write amplification (bad for celebrities).</li>\n<li><strong>Q:</strong> Pull (fan-out-on-read)? <strong>A:</strong> Merge followees' posts at read time → cheap writes, expensive reads (bad for read-heavy path).</li>\n<li><strong>Q:</strong> Hybrid (the answer)? <strong>A:</strong> Push for normal users, pull for celebrities (merge their recent posts at read) — cheap reads, no celebrity write explosion (7.4).</li>\n<li><strong>Q:</strong> Celebrity problem? <strong>A:</strong> A post to 100M followers = 100M writes if pushed → skip push, pull at read.</li>\n<li><strong>Q:</strong> Fan-out mechanism? <strong>A:</strong> Async workers consuming post events (Part 9); precomputed feeds cached (Redis), capped, rebuildable.</li>\n<li><strong>Q:</strong> Pagination? <strong>A:</strong> Cursor-based (opaque position), not offset — stable over a growing feed.</li>\n<li><strong>Q:</strong> Consistency? <strong>A:</strong> Eventual (posts appear with lag); read-your-writes for your own posts (10.3).</li>\n<li><strong>Q:</strong> Ranking? <strong>A:</strong> Chronological → ML-ranked; separate candidate generation (fan-out) from ranking (19.2.7/18.5).</li>\n</ul>\n<hr>\n<h2>17. Further Reading + Knowledge-Graph Links</h2>\n<p><strong>Foundations:</strong> [19.1.5 News Feed/Instagram] · [7.4 Hotspots/Skew] · [Part 6 Caching] · [Part 9 Messaging] · [18.5 Recommendations] · [19.2.7 Recommendation System].\r\n<strong>External:</strong> Standard feed/timeline system-design treatments; fan-out literature. <em>(Representative.)</em></p>\n<blockquote>\n<p><strong>Knowledge-graph:</strong> <code>19.1.5 feed</code> + <code>7.4 skew</code> + <code>Part 6 cache</code> + <code>Part 9 async</code> → <strong><code>19.2.2 news feed (deep)</code></strong> (hybrid fan-out, celebrity pull, cached feeds, cursor pagination).</p>\n</blockquote>"
  },
  "_id": "lessons/part-19-interview-designs/19.2.2-design-news-feed-deep.md",
  "_raw": {
    "sourceFilePath": "lessons/part-19-interview-designs/19.2.2-design-news-feed-deep.md",
    "sourceFileName": "19.2.2-design-news-feed-deep.md",
    "sourceFileDir": "lessons/part-19-interview-designs",
    "contentType": "markdown",
    "flattenedPath": "lessons/part-19-interview-designs/19.2.2-design-news-feed-deep"
  },
  "type": "Lesson",
  "title": "Design a News Feed System (Deep)",
  "description": "",
  "slug": "part-19-interview-designs/19.2.2-design-news-feed-deep",
  "url": "/learn/part-19-interview-designs/19.2.2-design-news-feed-deep",
  "readingTime": 10,
  "flashcards": [
    {
      "q": "Read-heavy or write-heavy?",
      "a": "Read-heavy (feed loads ≫ posts) — but write-side fan-out amplification is the hard part."
    },
    {
      "q": "Push (fan-out-on-write)?",
      "a": "Insert postId into every follower's precomputed feed on post → cheap reads, write amplification (bad for celebrities)."
    },
    {
      "q": "Pull (fan-out-on-read)?",
      "a": "Merge followees' posts at read time → cheap writes, expensive reads (bad for read-heavy path)."
    },
    {
      "q": "Hybrid (the answer)?",
      "a": "Push for normal users, pull for celebrities (merge their recent posts at read) — cheap reads, no celebrity write explosion (7.4)."
    },
    {
      "q": "Celebrity problem?",
      "a": "A post to 100M followers = 100M writes if pushed → skip push, pull at read."
    },
    {
      "q": "Fan-out mechanism?",
      "a": "Async workers consuming post events (Part 9); precomputed feeds cached (Redis), capped, rebuildable."
    },
    {
      "q": "Pagination?",
      "a": "Cursor-based (opaque position), not offset — stable over a growing feed."
    },
    {
      "q": "Consistency?",
      "a": "Eventual (posts appear with lag); read-your-writes for your own posts (10.3)."
    },
    {
      "q": "Ranking?",
      "a": "Chronological → ML-ranked; separate candidate generation (fan-out) from ranking (19.2.7/18.5)."
    }
  ],
  "metadata": {
    "partNum": 0,
    "lessonId": "",
    "difficulty": "foundational",
    "prerequisites": [
      "[19.1.5 News Feed/Instagram]",
      "[18.8 Chat at Scale (fan-out)]",
      "[Part 6 Caching]",
      "[7.3 Sharding]",
      "[7.4 Hotspots/Skew]",
      "[Part 9 Messaging]."
    ],
    "unlocks": [
      "[19.2.7 Recommendations]",
      "[Part 20 Capstone]."
    ]
  }
}