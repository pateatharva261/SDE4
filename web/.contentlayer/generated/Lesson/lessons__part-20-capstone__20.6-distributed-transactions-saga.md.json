{
  "body": {
    "raw": "# Lesson 20.6 — Distributed Transactions & Saga\r\n\r\n> Part 20 · Enterprise Capstone · Difficulty: ⚫ · *Capstone*\r\n>\r\n> **Prerequisites:** [11.6 Distributed Transactions 2PC/3PC], [11.7 Sagas], [12.5 Saga & Outbox], [11.5 Idempotency], [19.2.3 Payment System], [20.4 Portfolio/Ledger].\r\n> **Unlocks:** [20.7 CQRS/Event Sourcing], [20.11 Locks].\r\n\r\n---\r\n\r\n## 1. Learning Objectives\r\n\r\nAfter this lesson you will be able to:\r\n\r\n- Design **cross-context business transactions** (a trade, a funded purchase, a transfer) that span **multiple services** in the platform (20.1).\r\n- Explain why **2PC is avoided** (11.6 — blocking, fragile, CP) and **sagas** (11.7) are preferred for cross-service consistency.\r\n- Design a concrete **trade/purchase saga** with **compensating transactions** and choose **orchestration vs choreography** (11.7).\r\n- Solve the **dual-write problem** with the **transactional outbox** (12.5) and ensure steps are **idempotent** (11.5).\r\n- Handle the lack of isolation (**semantic locks / reservations**) and reason about **eventual consistency** across contexts.\r\n\r\n---\r\n\r\n## 2. Motivation\r\n\r\nBecause the platform is decomposed into **bounded contexts with database-per-service** (20.1/12.4), a single business operation — e.g., **\"buy $10,000 of a fund\"** — spans **Payments, Portfolio, Ledger, and Compliance** contexts. There's **no cross-service ACID transaction** (12.4). Yet the operation must be **atomic in effect** (all-or-nothing) and **correct** (money — 20.4). This is the **saga** problem (11.7), applied to the platform's most important flows.\r\n\r\n---\r\n\r\n## 3. The design (framework — 1.3.1)\r\n\r\n### 3.1 Why not 2PC (11.6)\r\n\r\n`[BP]`\r\n- **Two-phase commit (2PC)** gives cross-service atomicity but is **blocking** (a coordinator or participant crash can leave others locked — the blocking problem — 11.6), **fragile/CP**, and **couples availability** of all participants. It also doesn't work with **external systems** (banking rails don't join your 2PC).\r\n- `[BP]` **Avoid 2PC** for cross-service business transactions (11.6). Use **sagas** — local transactions + compensations — which trade **atomicity + isolation** for **availability + autonomy** (11.7).\r\n\r\n### 3.2 The saga (11.7)\r\n\r\n`[CS]` A **saga** = a sequence of **local transactions**, each in one service/context, where a failure triggers **compensating transactions** that semantically undo prior steps `[BP]`:\r\n- Each step commits **locally** (its own ACID transaction — 12.4) and emits an event / calls the next step.\r\n- On failure, run **compensations in reverse** (e.g., refund a charge, release a reservation, reverse a ledger entry via a new compensating entry — 20.4).\r\n- **No isolation** (11.7): intermediate states are visible → use **semantic locks / reservations** (mark funds \"reserved,\" an order \"pending\") to prevent conflicting concurrent operations, and design compensations to be **safe + idempotent** (11.5).\r\n- `[BP]` **Saga = local txns + compensations, eventual consistency, no isolation → semantic locks + idempotent compensations.**\r\n\r\n### 3.3 Example: the \"funded purchase\" saga\r\n\r\n`[BP]` Buying $10,000 of a fund:\r\n1. **Compliance check** (suitability/AML — 20.3/20.11) — validate the order is allowed.\r\n2. **Reserve funds** (Payments/Portfolio) — a **reservation** (semantic lock), compensation = release.\r\n3. **Execute the order** (route to broker/matching — possibly external) — compensation = cancel/reverse if not filled.\r\n4. **Post to the ledger** (double-entry, immutable — 20.4) — compensation = compensating entry.\r\n5. **Update the portfolio read model** + **notify** (eventual — 20.4/19.1.4).\r\n- If any step fails, **compensate** the completed prior steps in reverse. **Irreversible steps last** (11.7) — do reversible/internal steps before the hard-to-undo external execution where possible.\r\n\r\n### 3.4 Orchestration vs choreography (11.7)\r\n\r\n`[BP]`\r\n- **Orchestration:** a central **saga orchestrator** drives the steps + compensations. **Pros:** explicit, auditable, easy to reason about + monitor (good for **complex financial flows** — you want a clear, traceable state machine). **Cons:** the orchestrator is a component to build/own.\r\n- **Choreography:** services react to each other's events, no central coordinator. **Pros:** decoupled. **Cons:** the flow is **emergent** (hard to follow/debug), risky for complex money flows.\r\n- `[BP]` **Recommendation:** **orchestration for the money-critical sagas** (trades, purchases, transfers) — the auditability + explicit state machine matter for finance + compliance (20.7 audit). Choreography is fine for simpler, looser event reactions (e.g., notifications).\r\n\r\n### 3.5 Dual writes + idempotency (12.5 / 11.5)\r\n\r\n`[BP]`\r\n- **Dual-write problem:** a step must **update its DB and emit an event/trigger the next step atomically** — but a DB commit + a separate message publish isn't atomic (crash between them → lost/duplicate). Solve with the **transactional outbox** (12.5): write the state change + an outbox row in **one local transaction**; a relay publishes the event (or CDC tails it — 9.8). **No dual writes.**\r\n- **Idempotency everywhere** (11.5): steps + compensations may be retried (at-least-once — 9.4) → dedup by keys so retries are safe → **exactly-once effects**. Essential for saga correctness.\r\n- `[BP]` **Outbox (no dual writes) + idempotent steps/compensations** make the saga reliable + recoverable.\r\n\r\n### 3.6 Deep dives + bottlenecks\r\n\r\n`[BP]`\r\n- **No isolation** (§3.2): semantic locks/reservations prevent anomalies (e.g., double-spending reserved funds); design for visible intermediate states.\r\n- **Compensation correctness:** some steps are **hard/impossible to undo** (an executed market trade) → **irreversible-last** + reserve-then-commit patterns; where truly irreversible, compensate by a **corrective business action** (sell back, adjust).\r\n- **Consistency:** the saga is **eventually consistent** across contexts (10.5) — acceptable for the cross-context flow, while each **local** step (esp. the ledger post) is **strongly consistent** (20.4). Invariant-bound data stays within one context/transaction (12.4).\r\n- **Observability** (20.12): sagas need **tracing + state visibility** (which step, stuck sagas, compensation failures) — orchestration helps.\r\n- **Bottleneck / risk:** long-running sagas + compensation edge cases; mitigate with timeouts, monitoring, and idempotent retries.\r\n- `[BP]` **The lesson:** cross-context business transactions = **sagas (not 2PC) — local ACID steps + compensations, orchestrated for money-critical flows, with semantic locks (no isolation), the transactional outbox (no dual writes), and idempotent steps/compensations (exactly-once effects)** — eventually consistent across contexts, strongly consistent within the ledger step.\r\n\r\n---\r\n\r\n## 4. Visual Intuition\r\n\r\n```mermaid\r\nflowchart TB\r\n    START[\"Funded purchase request {idempotencyKey}\"] --> ORCH[\"Saga orchestrator (11.7) — explicit, auditable state machine\"]\r\n    ORCH --> S1[\"1. Compliance check (20.3/20.11)\"]\r\n    S1 --> S2[\"2. Reserve funds (semantic lock) — comp: release\"]\r\n    S2 --> S3[\"3. Execute order (external) — comp: cancel/reverse (irreversible-last)\"]\r\n    S3 --> S4[\"4. Post to ledger (ACID, immutable — 20.4) — comp: compensating entry\"]\r\n    S4 --> S5[\"5. Update portfolio read model + notify (eventual)\"]\r\n    S3 -. failure .-> COMP[\"Compensate completed steps in reverse (idempotent — 11.5)\"]\r\n    ORCH -. each step: outbox (12.5) — no dual writes .-> BUS[\"Event bus (Part 9)\"]\r\n    note[\"Saga not 2PC. Local ACID + compensations. Orchestrated for money flows. Semantic locks (no isolation) + outbox + idempotency.\"]\r\n```\r\n\r\n---\r\n\r\n## 5. Real-World Analogy\r\n\r\nThink of **booking a complex trip through a travel agent** — flight, hotel, car — where there's **no single \"book everything at once\" button**.\r\n\r\n- **No 2PC = you can't freeze all three vendors while you decide:** the airline, hotel, and rental company are **independent businesses**; you can't hold them all in a locked limbo until every piece confirms. Trying to would leave everyone waiting and blocked.\r\n- **Saga = book one at a time, with cancellation policies:** the agent books the flight, then the hotel, then the car — **each a committed step** — and if the car falls through, they **cancel the hotel and flight** (compensating actions) to unwind the trip.\r\n- **Semantic locks = reservations:** while arranging, the agent **holds** a seat and a room (a reservation others can't grab) rather than leaving them open to double-booking.\r\n- **Orchestration = the agent runs the checklist:** one coordinator drives the sequence and the cancellations, so there's a **clear, traceable record** of where the booking stands — much better for something important than each vendor just reacting to the others (choreography), where no one can say what state you're in.\r\n- **Idempotency = a booking reference so retries don't double-book:** if the agent resends a request, the vendor recognizes the reference and doesn't book twice.\r\n- **Irreversible-last = pay the non-refundable deposit only after everything else is locked in.**\r\n\r\n---\r\n\r\n## 6. Industry Example\r\n\r\n- **Sagas over 2PC for microservices** `[CONV]`: local transactions + compensations for cross-service consistency (§3.1/3.2, 11.7). *(Representative.)*\r\n- **Orchestrated sagas for financial workflows** `[CONV]`: explicit auditable state machines for trades/payments (§3.4, 11.7). *(Representative.)*\r\n- **Transactional outbox + CDC** `[CONV]`: atomic state-change + event, no dual writes (§3.5, 12.5/9.8). *(Representative.)*\r\n- **Semantic locks / reservations** `[CONV]`: reserve funds/inventory to handle the lack of isolation (§3.2, 11.7). *(Representative.)*\r\n\r\n---\r\n\r\n## 7. Implementation Details\r\n\r\n- **Avoid 2PC** cross-service (11.6); use **sagas** (11.7) (§3.1).\r\n- **Orchestrated saga** for money-critical flows (trades/purchases/transfers): local ACID steps + compensations, irreversible-last (§3.3/3.4).\r\n- **Semantic locks/reservations** for the no-isolation problem (§3.2); **transactional outbox** for no dual writes (12.5/9.8) (§3.5).\r\n- **Idempotent** steps + compensations (11.5) → exactly-once effects (9.4) (§3.5).\r\n- **Eventually consistent** across contexts (10.5); ledger step strongly consistent (20.4); trace + monitor sagas (20.12) (§3.6).\r\n\r\n---\r\n\r\n## 8–14. (Condensed)\r\n\r\n**Advantages:** cross-service atomic-in-effect operations without blocking 2PC; autonomy + availability; auditable orchestrated flow; recoverable (idempotent + outbox); works with external systems.\r\n**Disadvantages/cautions:** no isolation (semantic locks needed); eventual consistency across contexts; compensation logic is complex + must be idempotent; some steps hard to undo (irreversible-last); orchestrator to own.\r\n**When NOT to:** don't use 2PC for cross-service business transactions; don't use choreography for complex money flows (unauditable); don't skip semantic locks or idempotency.\r\n**Common mistakes:** attempting distributed ACID across services; dual writes (use outbox); non-idempotent compensations (double-undo); no reservations (concurrent conflicts); irreversible step first; unmonitored stuck sagas.\r\n**Interview Qs:** 🟢 Why not 2PC across services? 🟡 What is a saga + compensating transaction? 🔴 Orchestration vs choreography for a trade — which and why? How do you handle no-isolation + dual writes? ⚫ Design the full funded-purchase saga with compensations, semantic locks, outbox, idempotency, and consistency reasoning.\r\n**Production pitfalls:** compensation failures (retry/idempotency); stuck/long-running sagas (timeouts + monitoring); visible intermediate states causing anomalies; lost/duplicate events (outbox); irreversible-step ordering mistakes.\r\n**Optimizations:** orchestrator with a durable state machine; idempotency keys per step; outbox + CDC (9.8); reservations with TTL; observability/tracing for saga state (20.12); irreversible-last ordering.\r\n\r\n---\r\n\r\n## 15. Summary\r\n\r\nBecause the platform is decomposed into **bounded contexts with database-per-service** (20.1/12.4), a single business operation — e.g., **\"buy $10,000 of a fund\"** — spans **Compliance, Payments, Portfolio, and Ledger** contexts with **no cross-service ACID transaction**, yet must be **atomic in effect and correct** (money — 20.4). **2PC is avoided** (11.6): it's **blocking** (coordinator/participant crashes leave others locked — the blocking problem), **fragile/CP**, couples all participants' availability, and doesn't work with **external systems** (banking rails don't join your 2PC). Instead, use a **saga** (11.7): a sequence of **local ACID transactions**, each in one context, where a failure triggers **compensating transactions** that semantically undo prior steps — trading **atomicity + isolation** for **availability + autonomy**. The concrete **funded-purchase saga**: compliance check → **reserve funds** (a semantic lock; compensation = release) → **execute the order** (external; compensation = cancel/reverse) → **post to the immutable ledger** (compensation = a compensating entry — 20.4) → update the portfolio read model + notify — with **irreversible steps last** (11.7) and **compensations in reverse** on failure. Because sagas have **no isolation** (intermediate states are visible), use **semantic locks / reservations** (reserved funds, pending orders) to prevent conflicting concurrent operations, and design compensations to be **safe + idempotent** (11.5). For **money-critical flows**, prefer **orchestration** (a central orchestrator drives steps + compensations — explicit, auditable, monitorable — essential for finance/compliance/audit — 20.7) over **choreography** (decoupled but emergent/hard-to-debug — fine only for simpler event reactions like notifications). Solve the **dual-write problem** with the **transactional outbox** (12.5): write the state change + an outbox row in one local transaction and let a relay/CDC publish the event (9.8) — **no dual writes** — and make every step + compensation **idempotent** (retries are safe → **exactly-once effects** — 9.4). The saga is **eventually consistent across contexts** (acceptable — 10.5) while each **local step (especially the ledger post) is strongly consistent** (20.4), keeping invariant-bound data within one context (12.4); sagas need **tracing + state observability** (20.12) to catch stuck sagas + compensation failures. In one line: **sagas not 2PC — local ACID steps + idempotent compensations, orchestrated for money flows, with semantic locks (no isolation) and the transactional outbox (no dual writes) — eventually consistent across contexts, strongly consistent in the ledger step**.\r\n\r\n---\r\n\r\n## 16. Revision Notes (flashcard-ready)\r\n\r\n- **Q:** Why not 2PC across services? **A:** Blocking (crash locks participants), fragile/CP, couples availability, doesn't span external systems (11.6).\r\n- **Q:** What is a saga? **A:** A sequence of local ACID transactions with compensating transactions on failure; eventual consistency, no isolation (11.7).\r\n- **Q:** Handle the no-isolation problem? **A:** Semantic locks / reservations (reserved funds, pending orders) + idempotent compensations.\r\n- **Q:** Orchestration vs choreography? **A:** Orchestration (central, explicit, auditable) for money flows; choreography (emergent) only for simple event reactions.\r\n- **Q:** Dual-write problem + fix? **A:** DB commit + separate publish isn't atomic → transactional outbox (one local txn + relay/CDC) — no dual writes (12.5).\r\n- **Q:** Why idempotency in sagas? **A:** Steps/compensations get retried (at-least-once) → dedup for exactly-once effects (11.5/9.4).\r\n- **Q:** Irreversible-last? **A:** Do reversible/internal steps before hard-to-undo external ones (11.7).\r\n- **Q:** Consistency of a saga? **A:** Eventually consistent across contexts; each local step (ledger) strongly consistent (20.4).\r\n- **Q:** Compensating an executed trade? **A:** Can't literally undo → corrective business action (sell back/adjust) via a compensating ledger entry.\r\n\r\n---\r\n\r\n## 17. Further Reading + Knowledge-Graph Links\r\n\r\n**Foundations:** [11.6 2PC/3PC] · [11.7 Sagas] · [12.5 Saga & Outbox] · [11.5 Idempotency] · [19.2.3 Payment System] · [20.4 Portfolio/Ledger] · [9.8 CDC/Outbox].\r\n**External:** Garcia-Molina & Salem (Sagas); Richardson *Microservices Patterns*. *(Representative.)*\r\n\r\n> **Knowledge-graph:** `11.7 saga` + `12.5 outbox` + `11.5 idempotency` + `20.4 ledger` → **`20.6 distributed transactions`** (orchestrated sagas + semantic locks + outbox + idempotency; not 2PC) → feeds 20.7 audit.\r\n",
    "html": "<h1>Lesson 20.6 — Distributed Transactions &#x26; Saga</h1>\n<blockquote>\n<p>Part 20 · Enterprise Capstone · Difficulty: ⚫ · <em>Capstone</em></p>\n<p><strong>Prerequisites:</strong> [11.6 Distributed Transactions 2PC/3PC], [11.7 Sagas], [12.5 Saga &#x26; Outbox], [11.5 Idempotency], [19.2.3 Payment System], [20.4 Portfolio/Ledger].\r\n<strong>Unlocks:</strong> [20.7 CQRS/Event Sourcing], [20.11 Locks].</p>\n</blockquote>\n<hr>\n<h2>1. Learning Objectives</h2>\n<p>After this lesson you will be able to:</p>\n<ul>\n<li>Design <strong>cross-context business transactions</strong> (a trade, a funded purchase, a transfer) that span <strong>multiple services</strong> in the platform (20.1).</li>\n<li>Explain why <strong>2PC is avoided</strong> (11.6 — blocking, fragile, CP) and <strong>sagas</strong> (11.7) are preferred for cross-service consistency.</li>\n<li>Design a concrete <strong>trade/purchase saga</strong> with <strong>compensating transactions</strong> and choose <strong>orchestration vs choreography</strong> (11.7).</li>\n<li>Solve the <strong>dual-write problem</strong> with the <strong>transactional outbox</strong> (12.5) and ensure steps are <strong>idempotent</strong> (11.5).</li>\n<li>Handle the lack of isolation (<strong>semantic locks / reservations</strong>) and reason about <strong>eventual consistency</strong> across contexts.</li>\n</ul>\n<hr>\n<h2>2. Motivation</h2>\n<p>Because the platform is decomposed into <strong>bounded contexts with database-per-service</strong> (20.1/12.4), a single business operation — e.g., <strong>\"buy $10,000 of a fund\"</strong> — spans <strong>Payments, Portfolio, Ledger, and Compliance</strong> contexts. There's <strong>no cross-service ACID transaction</strong> (12.4). Yet the operation must be <strong>atomic in effect</strong> (all-or-nothing) and <strong>correct</strong> (money — 20.4). This is the <strong>saga</strong> problem (11.7), applied to the platform's most important flows.</p>\n<hr>\n<h2>3. The design (framework — 1.3.1)</h2>\n<h3>3.1 Why not 2PC (11.6)</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Two-phase commit (2PC)</strong> gives cross-service atomicity but is <strong>blocking</strong> (a coordinator or participant crash can leave others locked — the blocking problem — 11.6), <strong>fragile/CP</strong>, and <strong>couples availability</strong> of all participants. It also doesn't work with <strong>external systems</strong> (banking rails don't join your 2PC).</li>\n<li><code>[BP]</code> <strong>Avoid 2PC</strong> for cross-service business transactions (11.6). Use <strong>sagas</strong> — local transactions + compensations — which trade <strong>atomicity + isolation</strong> for <strong>availability + autonomy</strong> (11.7).</li>\n</ul>\n<h3>3.2 The saga (11.7)</h3>\n<p><code>[CS]</code> A <strong>saga</strong> = a sequence of <strong>local transactions</strong>, each in one service/context, where a failure triggers <strong>compensating transactions</strong> that semantically undo prior steps <code>[BP]</code>:</p>\n<ul>\n<li>Each step commits <strong>locally</strong> (its own ACID transaction — 12.4) and emits an event / calls the next step.</li>\n<li>On failure, run <strong>compensations in reverse</strong> (e.g., refund a charge, release a reservation, reverse a ledger entry via a new compensating entry — 20.4).</li>\n<li><strong>No isolation</strong> (11.7): intermediate states are visible → use <strong>semantic locks / reservations</strong> (mark funds \"reserved,\" an order \"pending\") to prevent conflicting concurrent operations, and design compensations to be <strong>safe + idempotent</strong> (11.5).</li>\n<li><code>[BP]</code> <strong>Saga = local txns + compensations, eventual consistency, no isolation → semantic locks + idempotent compensations.</strong></li>\n</ul>\n<h3>3.3 Example: the \"funded purchase\" saga</h3>\n<p><code>[BP]</code> Buying $10,000 of a fund:</p>\n<ol>\n<li><strong>Compliance check</strong> (suitability/AML — 20.3/20.11) — validate the order is allowed.</li>\n<li><strong>Reserve funds</strong> (Payments/Portfolio) — a <strong>reservation</strong> (semantic lock), compensation = release.</li>\n<li><strong>Execute the order</strong> (route to broker/matching — possibly external) — compensation = cancel/reverse if not filled.</li>\n<li><strong>Post to the ledger</strong> (double-entry, immutable — 20.4) — compensation = compensating entry.</li>\n<li><strong>Update the portfolio read model</strong> + <strong>notify</strong> (eventual — 20.4/19.1.4).</li>\n</ol>\n<ul>\n<li>If any step fails, <strong>compensate</strong> the completed prior steps in reverse. <strong>Irreversible steps last</strong> (11.7) — do reversible/internal steps before the hard-to-undo external execution where possible.</li>\n</ul>\n<h3>3.4 Orchestration vs choreography (11.7)</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Orchestration:</strong> a central <strong>saga orchestrator</strong> drives the steps + compensations. <strong>Pros:</strong> explicit, auditable, easy to reason about + monitor (good for <strong>complex financial flows</strong> — you want a clear, traceable state machine). <strong>Cons:</strong> the orchestrator is a component to build/own.</li>\n<li><strong>Choreography:</strong> services react to each other's events, no central coordinator. <strong>Pros:</strong> decoupled. <strong>Cons:</strong> the flow is <strong>emergent</strong> (hard to follow/debug), risky for complex money flows.</li>\n<li><code>[BP]</code> <strong>Recommendation:</strong> <strong>orchestration for the money-critical sagas</strong> (trades, purchases, transfers) — the auditability + explicit state machine matter for finance + compliance (20.7 audit). Choreography is fine for simpler, looser event reactions (e.g., notifications).</li>\n</ul>\n<h3>3.5 Dual writes + idempotency (12.5 / 11.5)</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>Dual-write problem:</strong> a step must <strong>update its DB and emit an event/trigger the next step atomically</strong> — but a DB commit + a separate message publish isn't atomic (crash between them → lost/duplicate). Solve with the <strong>transactional outbox</strong> (12.5): write the state change + an outbox row in <strong>one local transaction</strong>; a relay publishes the event (or CDC tails it — 9.8). <strong>No dual writes.</strong></li>\n<li><strong>Idempotency everywhere</strong> (11.5): steps + compensations may be retried (at-least-once — 9.4) → dedup by keys so retries are safe → <strong>exactly-once effects</strong>. Essential for saga correctness.</li>\n<li><code>[BP]</code> <strong>Outbox (no dual writes) + idempotent steps/compensations</strong> make the saga reliable + recoverable.</li>\n</ul>\n<h3>3.6 Deep dives + bottlenecks</h3>\n<p><code>[BP]</code></p>\n<ul>\n<li><strong>No isolation</strong> (§3.2): semantic locks/reservations prevent anomalies (e.g., double-spending reserved funds); design for visible intermediate states.</li>\n<li><strong>Compensation correctness:</strong> some steps are <strong>hard/impossible to undo</strong> (an executed market trade) → <strong>irreversible-last</strong> + reserve-then-commit patterns; where truly irreversible, compensate by a <strong>corrective business action</strong> (sell back, adjust).</li>\n<li><strong>Consistency:</strong> the saga is <strong>eventually consistent</strong> across contexts (10.5) — acceptable for the cross-context flow, while each <strong>local</strong> step (esp. the ledger post) is <strong>strongly consistent</strong> (20.4). Invariant-bound data stays within one context/transaction (12.4).</li>\n<li><strong>Observability</strong> (20.12): sagas need <strong>tracing + state visibility</strong> (which step, stuck sagas, compensation failures) — orchestration helps.</li>\n<li><strong>Bottleneck / risk:</strong> long-running sagas + compensation edge cases; mitigate with timeouts, monitoring, and idempotent retries.</li>\n<li><code>[BP]</code> <strong>The lesson:</strong> cross-context business transactions = <strong>sagas (not 2PC) — local ACID steps + compensations, orchestrated for money-critical flows, with semantic locks (no isolation), the transactional outbox (no dual writes), and idempotent steps/compensations (exactly-once effects)</strong> — eventually consistent across contexts, strongly consistent within the ledger step.</li>\n</ul>\n<hr>\n<h2>4. Visual Intuition</h2>\n<pre><code class=\"language-mermaid\">flowchart TB\r\n    START[\"Funded purchase request {idempotencyKey}\"] --> ORCH[\"Saga orchestrator (11.7) — explicit, auditable state machine\"]\r\n    ORCH --> S1[\"1. Compliance check (20.3/20.11)\"]\r\n    S1 --> S2[\"2. Reserve funds (semantic lock) — comp: release\"]\r\n    S2 --> S3[\"3. Execute order (external) — comp: cancel/reverse (irreversible-last)\"]\r\n    S3 --> S4[\"4. Post to ledger (ACID, immutable — 20.4) — comp: compensating entry\"]\r\n    S4 --> S5[\"5. Update portfolio read model + notify (eventual)\"]\r\n    S3 -. failure .-> COMP[\"Compensate completed steps in reverse (idempotent — 11.5)\"]\r\n    ORCH -. each step: outbox (12.5) — no dual writes .-> BUS[\"Event bus (Part 9)\"]\r\n    note[\"Saga not 2PC. Local ACID + compensations. Orchestrated for money flows. Semantic locks (no isolation) + outbox + idempotency.\"]\n</code></pre>\n<hr>\n<h2>5. Real-World Analogy</h2>\n<p>Think of <strong>booking a complex trip through a travel agent</strong> — flight, hotel, car — where there's <strong>no single \"book everything at once\" button</strong>.</p>\n<ul>\n<li><strong>No 2PC = you can't freeze all three vendors while you decide:</strong> the airline, hotel, and rental company are <strong>independent businesses</strong>; you can't hold them all in a locked limbo until every piece confirms. Trying to would leave everyone waiting and blocked.</li>\n<li><strong>Saga = book one at a time, with cancellation policies:</strong> the agent books the flight, then the hotel, then the car — <strong>each a committed step</strong> — and if the car falls through, they <strong>cancel the hotel and flight</strong> (compensating actions) to unwind the trip.</li>\n<li><strong>Semantic locks = reservations:</strong> while arranging, the agent <strong>holds</strong> a seat and a room (a reservation others can't grab) rather than leaving them open to double-booking.</li>\n<li><strong>Orchestration = the agent runs the checklist:</strong> one coordinator drives the sequence and the cancellations, so there's a <strong>clear, traceable record</strong> of where the booking stands — much better for something important than each vendor just reacting to the others (choreography), where no one can say what state you're in.</li>\n<li><strong>Idempotency = a booking reference so retries don't double-book:</strong> if the agent resends a request, the vendor recognizes the reference and doesn't book twice.</li>\n<li><strong>Irreversible-last = pay the non-refundable deposit only after everything else is locked in.</strong></li>\n</ul>\n<hr>\n<h2>6. Industry Example</h2>\n<ul>\n<li><strong>Sagas over 2PC for microservices</strong> <code>[CONV]</code>: local transactions + compensations for cross-service consistency (§3.1/3.2, 11.7). <em>(Representative.)</em></li>\n<li><strong>Orchestrated sagas for financial workflows</strong> <code>[CONV]</code>: explicit auditable state machines for trades/payments (§3.4, 11.7). <em>(Representative.)</em></li>\n<li><strong>Transactional outbox + CDC</strong> <code>[CONV]</code>: atomic state-change + event, no dual writes (§3.5, 12.5/9.8). <em>(Representative.)</em></li>\n<li><strong>Semantic locks / reservations</strong> <code>[CONV]</code>: reserve funds/inventory to handle the lack of isolation (§3.2, 11.7). <em>(Representative.)</em></li>\n</ul>\n<hr>\n<h2>7. Implementation Details</h2>\n<ul>\n<li><strong>Avoid 2PC</strong> cross-service (11.6); use <strong>sagas</strong> (11.7) (§3.1).</li>\n<li><strong>Orchestrated saga</strong> for money-critical flows (trades/purchases/transfers): local ACID steps + compensations, irreversible-last (§3.3/3.4).</li>\n<li><strong>Semantic locks/reservations</strong> for the no-isolation problem (§3.2); <strong>transactional outbox</strong> for no dual writes (12.5/9.8) (§3.5).</li>\n<li><strong>Idempotent</strong> steps + compensations (11.5) → exactly-once effects (9.4) (§3.5).</li>\n<li><strong>Eventually consistent</strong> across contexts (10.5); ledger step strongly consistent (20.4); trace + monitor sagas (20.12) (§3.6).</li>\n</ul>\n<hr>\n<h2>8–14. (Condensed)</h2>\n<p><strong>Advantages:</strong> cross-service atomic-in-effect operations without blocking 2PC; autonomy + availability; auditable orchestrated flow; recoverable (idempotent + outbox); works with external systems.\r\n<strong>Disadvantages/cautions:</strong> no isolation (semantic locks needed); eventual consistency across contexts; compensation logic is complex + must be idempotent; some steps hard to undo (irreversible-last); orchestrator to own.\r\n<strong>When NOT to:</strong> don't use 2PC for cross-service business transactions; don't use choreography for complex money flows (unauditable); don't skip semantic locks or idempotency.\r\n<strong>Common mistakes:</strong> attempting distributed ACID across services; dual writes (use outbox); non-idempotent compensations (double-undo); no reservations (concurrent conflicts); irreversible step first; unmonitored stuck sagas.\r\n<strong>Interview Qs:</strong> 🟢 Why not 2PC across services? 🟡 What is a saga + compensating transaction? 🔴 Orchestration vs choreography for a trade — which and why? How do you handle no-isolation + dual writes? ⚫ Design the full funded-purchase saga with compensations, semantic locks, outbox, idempotency, and consistency reasoning.\r\n<strong>Production pitfalls:</strong> compensation failures (retry/idempotency); stuck/long-running sagas (timeouts + monitoring); visible intermediate states causing anomalies; lost/duplicate events (outbox); irreversible-step ordering mistakes.\r\n<strong>Optimizations:</strong> orchestrator with a durable state machine; idempotency keys per step; outbox + CDC (9.8); reservations with TTL; observability/tracing for saga state (20.12); irreversible-last ordering.</p>\n<hr>\n<h2>15. Summary</h2>\n<p>Because the platform is decomposed into <strong>bounded contexts with database-per-service</strong> (20.1/12.4), a single business operation — e.g., <strong>\"buy $10,000 of a fund\"</strong> — spans <strong>Compliance, Payments, Portfolio, and Ledger</strong> contexts with <strong>no cross-service ACID transaction</strong>, yet must be <strong>atomic in effect and correct</strong> (money — 20.4). <strong>2PC is avoided</strong> (11.6): it's <strong>blocking</strong> (coordinator/participant crashes leave others locked — the blocking problem), <strong>fragile/CP</strong>, couples all participants' availability, and doesn't work with <strong>external systems</strong> (banking rails don't join your 2PC). Instead, use a <strong>saga</strong> (11.7): a sequence of <strong>local ACID transactions</strong>, each in one context, where a failure triggers <strong>compensating transactions</strong> that semantically undo prior steps — trading <strong>atomicity + isolation</strong> for <strong>availability + autonomy</strong>. The concrete <strong>funded-purchase saga</strong>: compliance check → <strong>reserve funds</strong> (a semantic lock; compensation = release) → <strong>execute the order</strong> (external; compensation = cancel/reverse) → <strong>post to the immutable ledger</strong> (compensation = a compensating entry — 20.4) → update the portfolio read model + notify — with <strong>irreversible steps last</strong> (11.7) and <strong>compensations in reverse</strong> on failure. Because sagas have <strong>no isolation</strong> (intermediate states are visible), use <strong>semantic locks / reservations</strong> (reserved funds, pending orders) to prevent conflicting concurrent operations, and design compensations to be <strong>safe + idempotent</strong> (11.5). For <strong>money-critical flows</strong>, prefer <strong>orchestration</strong> (a central orchestrator drives steps + compensations — explicit, auditable, monitorable — essential for finance/compliance/audit — 20.7) over <strong>choreography</strong> (decoupled but emergent/hard-to-debug — fine only for simpler event reactions like notifications). Solve the <strong>dual-write problem</strong> with the <strong>transactional outbox</strong> (12.5): write the state change + an outbox row in one local transaction and let a relay/CDC publish the event (9.8) — <strong>no dual writes</strong> — and make every step + compensation <strong>idempotent</strong> (retries are safe → <strong>exactly-once effects</strong> — 9.4). The saga is <strong>eventually consistent across contexts</strong> (acceptable — 10.5) while each <strong>local step (especially the ledger post) is strongly consistent</strong> (20.4), keeping invariant-bound data within one context (12.4); sagas need <strong>tracing + state observability</strong> (20.12) to catch stuck sagas + compensation failures. In one line: <strong>sagas not 2PC — local ACID steps + idempotent compensations, orchestrated for money flows, with semantic locks (no isolation) and the transactional outbox (no dual writes) — eventually consistent across contexts, strongly consistent in the ledger step</strong>.</p>\n<hr>\n<h2>16. Revision Notes (flashcard-ready)</h2>\n<ul>\n<li><strong>Q:</strong> Why not 2PC across services? <strong>A:</strong> Blocking (crash locks participants), fragile/CP, couples availability, doesn't span external systems (11.6).</li>\n<li><strong>Q:</strong> What is a saga? <strong>A:</strong> A sequence of local ACID transactions with compensating transactions on failure; eventual consistency, no isolation (11.7).</li>\n<li><strong>Q:</strong> Handle the no-isolation problem? <strong>A:</strong> Semantic locks / reservations (reserved funds, pending orders) + idempotent compensations.</li>\n<li><strong>Q:</strong> Orchestration vs choreography? <strong>A:</strong> Orchestration (central, explicit, auditable) for money flows; choreography (emergent) only for simple event reactions.</li>\n<li><strong>Q:</strong> Dual-write problem + fix? <strong>A:</strong> DB commit + separate publish isn't atomic → transactional outbox (one local txn + relay/CDC) — no dual writes (12.5).</li>\n<li><strong>Q:</strong> Why idempotency in sagas? <strong>A:</strong> Steps/compensations get retried (at-least-once) → dedup for exactly-once effects (11.5/9.4).</li>\n<li><strong>Q:</strong> Irreversible-last? <strong>A:</strong> Do reversible/internal steps before hard-to-undo external ones (11.7).</li>\n<li><strong>Q:</strong> Consistency of a saga? <strong>A:</strong> Eventually consistent across contexts; each local step (ledger) strongly consistent (20.4).</li>\n<li><strong>Q:</strong> Compensating an executed trade? <strong>A:</strong> Can't literally undo → corrective business action (sell back/adjust) via a compensating ledger entry.</li>\n</ul>\n<hr>\n<h2>17. Further Reading + Knowledge-Graph Links</h2>\n<p><strong>Foundations:</strong> [11.6 2PC/3PC] · [11.7 Sagas] · [12.5 Saga &#x26; Outbox] · [11.5 Idempotency] · [19.2.3 Payment System] · [20.4 Portfolio/Ledger] · [9.8 CDC/Outbox].\r\n<strong>External:</strong> Garcia-Molina &#x26; Salem (Sagas); Richardson <em>Microservices Patterns</em>. <em>(Representative.)</em></p>\n<blockquote>\n<p><strong>Knowledge-graph:</strong> <code>11.7 saga</code> + <code>12.5 outbox</code> + <code>11.5 idempotency</code> + <code>20.4 ledger</code> → <strong><code>20.6 distributed transactions</code></strong> (orchestrated sagas + semantic locks + outbox + idempotency; not 2PC) → feeds 20.7 audit.</p>\n</blockquote>"
  },
  "_id": "lessons/part-20-capstone/20.6-distributed-transactions-saga.md",
  "_raw": {
    "sourceFilePath": "lessons/part-20-capstone/20.6-distributed-transactions-saga.md",
    "sourceFileName": "20.6-distributed-transactions-saga.md",
    "sourceFileDir": "lessons/part-20-capstone",
    "contentType": "markdown",
    "flattenedPath": "lessons/part-20-capstone/20.6-distributed-transactions-saga"
  },
  "type": "Lesson",
  "title": "Distributed Transactions & Saga",
  "description": "",
  "slug": "part-20-capstone/20.6-distributed-transactions-saga",
  "url": "/learn/part-20-capstone/20.6-distributed-transactions-saga",
  "readingTime": 10,
  "flashcards": [
    {
      "q": "Why not 2PC across services?",
      "a": "Blocking (crash locks participants), fragile/CP, couples availability, doesn't span external systems (11.6)."
    },
    {
      "q": "What is a saga?",
      "a": "A sequence of local ACID transactions with compensating transactions on failure; eventual consistency, no isolation (11.7)."
    },
    {
      "q": "Handle the no-isolation problem?",
      "a": "Semantic locks / reservations (reserved funds, pending orders) + idempotent compensations."
    },
    {
      "q": "Orchestration vs choreography?",
      "a": "Orchestration (central, explicit, auditable) for money flows; choreography (emergent) only for simple event reactions."
    },
    {
      "q": "Dual-write problem + fix?",
      "a": "DB commit + separate publish isn't atomic → transactional outbox (one local txn + relay/CDC) — no dual writes (12.5)."
    },
    {
      "q": "Why idempotency in sagas?",
      "a": "Steps/compensations get retried (at-least-once) → dedup for exactly-once effects (11.5/9.4)."
    },
    {
      "q": "Irreversible-last?",
      "a": "Do reversible/internal steps before hard-to-undo external ones (11.7)."
    },
    {
      "q": "Consistency of a saga?",
      "a": "Eventually consistent across contexts; each local step (ledger) strongly consistent (20.4)."
    },
    {
      "q": "Compensating an executed trade?",
      "a": "Can't literally undo → corrective business action (sell back/adjust) via a compensating ledger entry."
    }
  ],
  "metadata": {
    "partNum": 0,
    "lessonId": "",
    "difficulty": "foundational",
    "prerequisites": [
      "[11.6 Distributed Transactions 2PC/3PC]",
      "[11.7 Sagas]",
      "[12.5 Saga & Outbox]",
      "[11.5 Idempotency]",
      "[19.2.3 Payment System]",
      "[20.4 Portfolio/Ledger]."
    ],
    "unlocks": [
      "[20.7 CQRS/Event Sourcing]",
      "[20.11 Locks]."
    ]
  }
}